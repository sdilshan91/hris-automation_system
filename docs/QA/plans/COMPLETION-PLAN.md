# HRM — Completion Plan (living)

> **This is the ONE living completion plan.** It rolls over in place — do not create a new
> dated `COMPLETION-PLAN-<date>.md`; instead add a dated entry to the **Changelog** below and
> re-sort the plan body. Full snapshots of superseded plans are archived under
> [`archive/`](archive/). Built from a read-only survey of the findings ledger, code TODO/stub
> markers, the RLS flip state, and the story/test-status ledgers. **Living document — re-sort as
> reality changes.** Base: `test/local-subdomains`. **RLS is committed OFF; flip is RE-VALIDATED GO** (see P4).

## 🗓 Changelog (rollover history)

| Date | Event | Full snapshot |
|------|-------|---------------|
| **2026-07-24 — arc-p (#434–#440) + USER-OPS — ▶ ACTIVE** | **★ arc-p: closed the 7 arc-o residual/decision items, then delivered the two USER-OPS asks (RLS local flip + encryption key-rotation) end-to-end on the running stack.** **Residuals + 2 decision gates (#434–437, user-decided):** **DF-61-conc + DF-61-pg #434** — per-runId payroll-reprocess interlock (Postgres SESSION advisory lock keyed on runId, Npgsql-only, loser no-ops; user chose the mutex over an xmin token) + real-PG reconcile/marker arm · **DF-64-wk + DF-64-pg #435** — weekly-cap advisory double-count fix (exclude the staged-but-unflushed superseded OT record from the `AsNoTracking` sum) + real-PG FK-cascade arm (Approved record + its immutable `OvertimeApprovalHistory` both survive the correction path) · **DF-63-rls #436** — RLS-ON coverage arm for `AutoClockOutJob` shift resolution (real hrm_app role + `app.current_tenant` GUC + FORCE RLS; 479/539/569 discriminator proves the own-tenant shift is read under the GUC) · **DF-65-pg + DF-62-parity #437** — real-PG double-forfeiture arm (+ deterministic `PoolRowTickOffset` µs-truncation proof) + the DF-62-parity divergence-guard characterization test (user chose guard+document, NO money-math change: encashment-gate `BalanceAfter` vs year-end engine-`ProratedEntitlementDays` diverge by exactly the accrual-vs-entitlement gap — an accepted employee-detriment edge, no double-pay). **USER-OPS delivered (#438–440):** **RLS local flip** — `hris_dev_db` flipped RLS-ON (roles `hrm_app`/`hrm_owner`, `REASSIGN OWNED`, user-secrets repoint, `Rls:Enabled=true`); live-validated (e2e 200 / cross-tenant 403 / DB-level GUC fail-closed 0→7); reversible via `Rls:Enabled=false`. **Encryption key-rotation tooling #438** — closed the P7 "no bulk re-encryption job" gap: `EncryptedFieldRegistry` (both-direction drift guards) + `FieldEncryptionMaintenanceService` (RLS-safe per-tenant sweep, includes soft-deleted so retirement can't destroy restorable PII) + `POST/GET /api/v1/system/encryption/*` (`Tenant.Lifecycle`) + `EncryptionKeyAgeWatchdogJob` (Cron.Weekly, system table `encryption_key_activation`, WARN age≥`RotationCadenceDays` default 90) + runbook; **user chose QUARTERLY cadence, NOT weekly** (weekly = un-re-encrypted backlog + key sprawl). **DF-enc-nationalid-backfill #439** — flipped national_id into the startup back-fill (defence-in-depth PII heal); also fixed a pre-existing cross-class cached-model test flake (`EncryptorAwareModelCacheKeyFactory` keys on `GetType().Name` → distinct-type wrapper). **DF-enc-http-authz + DF-enc-rls-grant #440** — real HTTP 403-for-Support authz arm (TC-PLT-007) on the fleet-wide re-encrypt endpoints; **fixed a real harness bug it surfaced (DF-enc-httptest-hermeticity):** `ApiTestFactory` inherited the dev's RLS-ON user-secrets (`Rls:Enabled`/`PrivilegedConnection`) → login 500 → the whole Integration.Http suite red for any RLS-on dev; now forced hermetic (44/44 green). **★ LESSON (held): the 2-auditor gate again caught material defects pre-merge** (DF-64-wk seed collisions, DF-65-pg weak tick-offset arm, #438 soft-deleted-tenant PII-loss + reverse-drift gap); and the "cheap" DF-enc-http-authz follow-up is what exposed the suite-wide harness breakage — the argument for doing the LOW arm, not filing it forever. **New residuals filed:** DF-61-conc-retry/-approval-race/-slip · DF-63-shiftdoc/-clockseam · DF-65-pg-encash · DF-enc-httptest-hermeticity (fixed) · DF-plt-tc-structure (parked, decision). **▶ Now-open = LOW should-haves + decision-parks only; the sole original USER-OPS item left is ClamAV-prod (stand up clamd — infra, no "key").** | _(this file + DEFERRED-FOLLOWUPS.md + AUDITOR-FINDINGS.md + memory topic files [[rls-local-flip]]/[[encryption-key-rotation]])_ |
| **2026-07-22 — arc-o (#416–#433)** | **★ arc-o: the last L/structural features + all small residuals + ALL 4 decision gates — the deferred-followups queue is COMPLETE.** **L/structural (#416–427, detail in DEFERRED-FOLLOWUPS.md):** DF-1 #416 date-only `timestamptz`→`date` remodel (kills the `.Date`-Kind bug class) · DF-59 #417 · DF-54 #418 · DF-53 #419 · DF-58 #421 payroll reconcile sweep · DF-57 #422 · DF-37 #423 ISSUE-280 encashment keying · DF-55 #424 · DF-23 #425 multi-location geofence · DF-56 #426 per-shift work-minute knobs · **DF-19 #427** pool-aware carry-forward (persisted `ConsumedDays`; enforcer caught a CRITICAL 'half-wired money' near-miss — the counter was bumped only by leave-approval while encashment+LOP bypassed it → over-forfeit; fixed via ONE shared `PooledLeaveLedger.AppendDeductionAsync` routed through all 4 consumption paths). **Small residuals (this session):** **DF-63 #428** auto-clock-out now shift-knob-aware (`IShiftService` from the runner scope; no pay impact) · **DF-60 #429** FE bare-date wire-format contract spec · **DF-28** verified already-closed (`BrandingAssetUrls.Servable` null-when-empty; no code) · DF-19 real-PG VERIFIED (`FinalSettlementPostgresTests` 3/3). **ALL 4 DECISION GATES RESOLVED (user-decided):** **DF-65 #430** year-end sweep now supersedes the prior carry bucket — a DETERMINISTIC double-forfeiture (employee detriment) for any type with `CarryForwardExpiryMonths≥12`; my first cut (route-forfeit-alone) was INCOMPLETE (missed the common `accrual<limit` + no-forfeit-all-carried cases) → switched to (a)+(b) terminalize · **DF-64 #431** regularized OT days now produce a payable `OvertimeRecord` (Pending→normal approval, user-decided); **enforcer caught a ship-blocking HIGH-1** — my unguarded `RemoveRange` could delete an Approved/payroll-ready OT record + cascade its immutable `OvertimeApprovalHistory` → added a terminal-status guard, re-audit PASS · **DF-61 #432** payroll-rerun dropped-enqueue self-heals via a `ReprocessRequestedAt` dedup marker + sweep (closes the status-indistinguishability gap; the enqueue→start concurrency window is UNCHANGED — doc-overstatement I made was corrected) · **DF-62 #433** encashment pay-policy ruled: (1) keep RAW monthly BASIC + (2) block-non-Encashable/null-limit-whole-balance-only-when-Encashable = ALREADY the behavior (both no-code; clause 2 pinned by 2 mutation-proven contract arms) + (3) gate-vs-year-end parity deferred post-DF-65. **★ LESSON (held): the 2-auditor gate caught BOTH money bugs I introduced this arc (DF-64 HIGH-1, DF-65 near-miss) before merge — the CAL-8 'a green suite of my own arms is not evidence' pattern.** **New TCs bound (traits):** TC-ATT-162 (DF-63), TC-ATT-006-64 (DF-64), TC-PAY-003-16 (DF-61), TC-PAY-010-09 (DF-62). **New residuals filed:** DF-63-rls · DF-64-pg/wk · DF-65-pg · DF-61-pg/conc · DF-62-parity. **▶ Queue COMPLETE — remaining = only LOW/needs-infra/decision residuals + user-ops (RLS prod flip · encryption-key rotation · ClamAV prod).** | _(this file + DEFERRED-FOLLOWUPS.md + AUDITOR-FINDINGS.md)_ |
| **2026-07-21 — arc-n (#391–#415)** | **★ arc-n: ops-validation + the full M/L backlog (Tier A + Tier B + DF-50) shipped and merged to base.** **Ops-validation (#391–402):** RLS dev-enabled + validated (20/20 across the 5 Testcontainers suites), ClamAV/EICAR verified against real clamd (#401), k6 dashboard-at-scale confirmed (50k / 50-VU / 5-min — widgets **p95 192.9ms** < 800, 0% errors); prod flips stay the ops runbook. **Tier A (near-clones):** **DF-20 #403** tenant-configurable leave cancellation-window · **DF-6 #404** dashboard-PDF tenant-logo embed · **DF-31 #405** per-employee payslip retry. **ISSUE-173 (US-PAY-008):** **FR-3 #406** SLA auto-escalation to backup approver · **FR-6 #407** approval delegation. **Tier B 100%:** **DF-21 #408** my-balance N+1 batched · **DF-22 #409** per-location policy on tenant-wide sweeps · **DF-8 #410** full reporting-chain breadcrumb on `GET /employees/{id}` (**closes ISSUE-218**) · **DF-4 #411** leave-entitlement recalc durability (recon-corrected: NOT onboarding; shipped as an idempotent reconciliation SWEEP, not an outbox) · **DF-9 #412** custom-fields FE render pass · **DF-7 #413** distributed (Redis) magic-link limiter, **regression fix #414**. **Tier C:** **DF-50 #415** dashboard cold-start warmup + parallelize via a CHILD-SCOPE-per-widget runner (NOT IDbContextFactory; each child scope re-seeds the tenant before querying). **New TCs bound:** TC-LV-267 (DF-21), TC-LV-268 (DF-4), TC-ATT-162 (DF-22), TC-CHR-338 (DF-8), TC-CHR-339 (DF-9), TC-REC-011 (DF-7), TC-RPT-005-50 (DF-50). **Follow-ups filed:** DF-53/54/55/56/57/58/59. **▶ Remaining backlog = Tier C only** (DF-19, DF-1, DF-37, DF-23, LocationId epic) — design-first, one at a time; see `ML-BACKLOG-PLAN.md` + `DEFERRED-FOLLOWUPS.md`. | _(this file + DEFERRED-FOLLOWUPS.md + ML-BACKLOG-PLAN.md)_ |
| **2026-07-19 (c)** | **★ #383–390 arc shipped + merged to base — QA backfill + 7 feature/fix deliveries, then docs reconciled to the shipped code.** **#383** QA TC backfill (IEEE-829 docs for the #371–382 fix campaign — 14 TCs bound to their green xUnit/Karma arms). **#384** NTF-006 delivery seams: lockout email now real-delivered (DF-40 DONE), applicant/offer **magic-links** now issued+embedded via new `PortalLinkBuilder` + `applicant_portal_link` event (US-REC-007 DF-42 / US-REC-008 DF-41 DONE). **#385** chronic-lateness **escalation** (US-ATT-008 FR-7). **#386** employee **profile-edit** + net-new tenant-scoped `EmployeeEducation` / `EmployeeWorkHistory` / `EmployeeDependent` entities+tables + employee address columns + CRUD + `PATCH {id}/profile` save path (US-CHR-002 Education/Work-History/Dependents backend now EXISTS; DF-38/DF-39 DONE; FE edit re-enabled off the #380 read-only state). **#387** goal-**read** authz (self-read: HR override / owner / direct manager — DF-18) + **BUG-056 goal finalize/lock** (`GoalStatus.Finalized`, `POST /tenant/performance/goals/finalize` requires weights==100%, then immutable → 409 `goals_finalized`; re-open decision = DF-46, endpoint not yet built). **#388** org-tree FE nested-children rendering. **#389** net-new tenant-scoped **`SalaryGrade`** entity + CRUD (`/api/v1/tenant/salary-grades`, code/name/min-mid-max `decimal(18,2)`/currency/is_active) + FE + `JobTitle.GradeId` FK-validation (ISSUE-021 DONE; US-PAY-001 AC-K1 / US-CHR-005). **#390** dashboard `Employee.BirthMonthDay` column + index + interceptor + startup **backfill** migration `AddEmployeeBirthMonthDayIndex` (ISSUE-285a; index shipped, remainder = DF-50 widget parallelism + DF-51 k6). **Docs reconciled this pass:** BA STATUS deferred-AC rows + the two Theme-K rows (goal-finalize / SalaryGrade) flipped to delivered; US-PAY-001/US-PRF-001/US-CHR-005/US-CHR-002 stories updated; DEFERRED-FOLLOWUPS moved 9 `✅ DONE` rows (DF-13/17/18/33/38/39/40/41/42) into `## Done`; tech-doc data dictionary + `docs/vault/modules/performance.md` goal-state enum + `docs/DEV/UPGRADE-NOTES.md` birthday backfill. | _(this file + DEFERRED-FOLLOWUPS.md)_ |
| **2026-07-19 (b)** | **★ Deferred-backlog pass — cleared every clearly-deliverable/safe DF row and it kept uncovering PROD bugs.** After the arc-l decisions campaign, worked `docs/QA/DEFERRED-FOLLOWUPS.md` top-down. **Done:** DF-36 **#380** (FE `updateProfileSection` PATCHed a `sections/{section}` route the BE never exposed → EVERY inline profile-section save 404'd; rewired the 5 BE-backed sections onto the real `PATCH {id}/profile`, made the 3 unbacked sections (education/work-history/dependents) read-only). DF-32/35/30/25 **#381** (branding upload servable URL · delete dead `SendLockoutNotificationJob` · `LocalFileStorage` `..` path-guard +7 arms · memoize idle-timeout read). DF-34 **#382** (org-profile settings fields). **★★ 3 REAL PROD BUGS found while clearing these, all now fixed/filed:** (1) **DF-25 would have 500'd EVERY authenticated request in any Redis env** — `IMemoryCache` is NOT registered by default here (the EF 2nd-level cache only provides it on its memory-provider branch, SKIPPED when `ConnectionStrings:Redis` is set = base appsettings); the dev-config suite can't see it; **integration-enforcer** caught it → fixed with an unconditional `AddMemoryCache()` in Program.cs. (2) **ISSUE-322 (HIGH data-loss)** — the company org-profile save is a full-replace PUT but the FE sent only 7 of 11 fields, so every save WIPED `probationPeriodDays`/`defaultCountryCode`/`payslipFooterDisclaimer`/`payrollFromEmail`; fixed by round-tripping all fields (#382). (3) **ISSUE-319/320/321** — the whole employee-profile-EDIT feature is FE-built-ahead-of-BE (3 sections with no backend; contact/employment fields that don't map) → filed + partially fixed, residuals DF-38/39. BE 4422→**4429** / FE 3837→3844, green every merge. **★ LESSON: clearing "low" deferred items keeps surfacing shipping-in-prod defects, and the config-masked ones (DF-25) are invisible to the dev suite — run the auditors, not just the green suite.** **▶ Remaining DF rows are story-sized / decision-gated / test-coverage / ops (RLS flip) — flagged in DEFERRED-FOLLOWUPS.md with per-item recommendations; NOT auto-ground.** | _(this file + DEFERRED-FOLLOWUPS.md)_ |
| **2026-07-18→19 (a)** | **★★ "Fix ALL open bugs+issues MED→LOW, then every product/BA decision" campaign COMPLETE (arc-k + arc-l, PRs #352–#379).** Per-PR detail lives in `docs/QA/{TEST-FINDINGS,DEFERRED-FOLLOWUPS,LOW-TIER-DECISIONS}.md` (this plan's P-sections BELOW predate it). **arc-k (#352–372):** all MED + LOW bug/issue clusters, gated auto-merge, decision-with-rec; clear-engineering batch #371 (ISSUE-077/081/159/222/072 + SanitizeFileName), doc-align trio #372, FE follow-ups #369, etc. **arc-l (#373–379, "deliver all remaining"):** #373 DF-29 favicon+cross-tenant switcher-logo · #374 NTF-006 seams (229 payslip-sender / 063 lockout-tenant-name; 110+087-perlate STALE-already-wired) · #375 ISSUE-108 interview soft-warn · #376 ISSUE-246 reject-WebP · #377 **ISSUE-293 National ID = encrypted PII field + masked DTO + audited reveal + FE** (real-Postgres ciphertext proof; fixed a 12-test integ-DI break) · #378 **ISSUE-034 pro-rata day-count→month-fraction** (money, per-user decision, matches TC-LV-029) · #379 ISSUE-007 TenantUsers route split. Many findings found STALE-already-done on recon (grep code per ID first). **RLS:** BUILT+PROVEN+committed-OFF, suite-green, policy-complete; prod flip = user ops step (runbook `Rls/README.md`). **280/295 deferred → DF-37.** BE 4246→4422 / FE 3818→3844. | _(TEST-FINDINGS + DEFERRED-FOLLOWUPS)_ |
| **2026-07-17 (b)** | **★★ TC backfill for the date-Kind HIGHs (BUG-289/290) uncovered GREEN THEATER in already-merged "Postgres-verified" tests.** Backfilling TCs for the 2 HIGH date-Kind fixes, I found (1) BUG-290's Asset + Exit-interview sites had **NO real-Postgres arm at all** (only Offboarding did; the fix was asserted by code-read), and (2) — worse — the existing Onboarding-assign AND Offboarding-initiate Postgres arms were **green theater**: they fed a `Kind=Utc` input, and since **`DateTime.Date` PRESERVES Kind**, mutating the `SpecifyKind`/`UtcMidnight` fix still yielded Utc → no throw → the mutant SURVIVED. Both shipped as "mutation-verified / Postgres-proven" while proving nothing. **Fix:** added net-new `AssetExitInterviewDateKindPostgresTests` (2 arms), and corrected all existing arms to feed a **`Kind=Unspecified`** input (`new DateTime(y,m,d)`, as a date arrives from model binding). **Re-mutation now kills all 4** with the exact `Cannot write DateTime with Kind=Unspecified …` error. Bound TCs: TC-ONB-002-13 (assign) · TC-ONB-004-13 (asset, net-new) · TC-ONB-005-13 (offboarding) · TC-ONB-006-13 (exit, net-new) — each via `[Trait("TC", …)]`, matrix rows added, BUG-289/290 findings corrected. **★ LESSON (folded into memory): a date-Kind Postgres arm that feeds a Utc input is worthless — the read-back `.Kind==Utc` assertion is non-discriminating (Npgsql normalizes every timestamptz read to Utc), so the only load-bearing assertion is "the WRITE didn't throw", which requires an Unspecified input. Always mutation-verify.** **▶ NEXT: the remaining 5 zero-TC arc fixes** (ISSUE-314/142/230/126/130), then DF-11/ISSUE-316 (RejectionReason still strict). | _(this file)_ |
| **2026-07-17 — ▶ ACTIVE** | **★ Postgres-hardening arc-j COMPLETE (PRs #346–#348) + the TC/traceability debt it exposed, now paid.** **Fixes:** BUG-264 stale-closed · **ISSUE-308** (#346) `AttendanceSettings` lazy-create 23505 race → catch-on-conflict (8 parallel racers → one row) · **DF-2/ISSUE-300** (#347) full cumulative-PAYE **RUN** true-up on real Postgres — the last InMemory-only money-path gap; a fresh trace confirmed the run write path is date-Kind-clean, so BUG-289/290 had genuinely unblocked it · **ISSUE-231 + ENH-021** (#348) the enum-from-DB 500 class → `TolerantEnumToStringConverter<TEnum>` (Unknown sentinel + log; write side unchanged so translation is intact), applied to `applicant.stage/.source` + `payroll_run.status`. **⚠ `@integration-enforcer` caught a HIGH in my OWN #348 work:** `applicant_stage_history.from_stage/.to_stage` (read by the recruitment **dashboard**, higher traffic than the board) was still strict → one corrupt row 500s the whole dashboard; folded in + mutation-proved real (`Cannot convert 'LegacyPhoneScreen'`). All converters mutation-verified. **★ THE DEBT: I shipped #346–#348 (and — the sweep showed — most of #331–#345, incl. HIGH BUG-289/290) with real xUnit tests but NO IEEE 829 TC docs and ZERO `TRACEABILITY-MATRIX.md` rows.** A fix with no bound TC is invisible to `/test-all`/`/verify-fix`. **Paid for arc-j:** 5 TC docs (TC-ATT-157 · TC-PAY-016/017 · TC-REC-003-15 · TC-REC-009-14), each bound to its xUnit arm via `[Trait("TC", …)]`; matrix forward+detail rows + `last_updated`; a TEST-STATUS arc-j regression block (stories stay `[ ]` — the report-only loop hasn't executed them, that's its call). **Residuals filed properly** (not just DF rows): **ISSUE-316** (MED — `RejectionReason` still strict → board/detail can still 500; DF-11) · **ISSUE-317** (LOW — FE has no `Unknown` badge; DF-12). **▶ NEXT: the SYSTEMIC TC backfill** — 7 earlier-arc fixes have zero TCs; do **BUG-289 + BUG-290 (HIGH, date-Kind class) FIRST**, then ISSUE-314/142/230/126/130. Standing rule now: author the TC + matrix row *with* every fix. | _(this file)_ |
| **2026-07-16 (b) — ▶ ACTIVE** | **BUG-288 (HIGH) fixed — the fiscal-tenant onboarding blocker. PR #319 OPEN (awaiting merge).** CAL-8 made `Tenant.FiscalYearStartMonth` load-bearing but the WRITE path never changed: `TenantSettingsService.UpdateOrgProfileAsync` was a bare assignment validated only 1–12, so an admin flipping 1→4 would **silently re-date existing leave-ledger rows** (Jan–Mar label 2026 → resolves to 2025 under April; Apr–Dec unaffected, so PARTIAL + silent). **My own regression** — before CAL-8 the column was read by nothing so flipping it was a no-op. **Fix (BUG-288 option 1):** once a tenant has `leave_ledger` history, a CHANGE is rejected **422 `fiscal_year_locked_by_leave_history`**; provisioning stays open. Two tested subtleties: triggers on a CHANGE not the field's PRESENCE (full-replace PUT — an address edit resends the unchanged month), and the history check is **tenant-scoped** via the EF filter (an unfiltered `AnyAsync()` would let one tenant lock all). **Verification:** full suite **4090/4090** green; 4 arms; **mutation 3/3 killed** (guard-removed · guard-on-presence · unfiltered-check), anchors proven applied. **Deferred:** effective-dating (option 2) + ledger migration (option 3) — a mid-life basis SWITCH is now a support task, not silent corruption. **▶ NEXT after #319 merges: `ISSUE-313`** — 5 fiscal sites with no regression guard, **2 MONEY** (`LeaveEncashmentService`, `RealPayrollFnFIntegration`); do the money sites first with an F&F oracle. | _(this file)_ |
| **2026-07-16 (a)** | **★★ CAL-7 + CAL-8 shipped — the tenant+location configurable-calendar EPIC IS COMPLETE (CAL-1..8, PRs #310–#318).** **CAL-7 (#317, ISSUE-304 RESOLVED):** probation `Location ?? Tenant ?? 90` via 3 batched queries; `Location.ProbationPeriodDays` nullable **so a Location can stay silent** (a non-null default would make every Location an override and the tenant knob would never apply anywhere). ⚠ A **CS8714 shipped in CAL-6 hidden by incremental build** — only `--no-incremental` exposed it; that flag is now part of my verify gate. **CAL-8 (#318, ISSUE-305 RESOLVED):** new `HRM.Domain/Leave/LeaveYear.cs` (`BoundsFor`/`LabelFor`/`StartOf`) + **`ITenantLeaveYearResolver`** — ONE injected reader of `Tenant.FiscalYearStartMonth` — wired through **11 sites**. The plan named **4**; my sweep found 7; the enforcer found the 8 debit sites; my own re-sweep found an 11th (`LeaveDashboardService` Pending filter comparing `lr.StartDate.Year` to a fiscal LABEL). **★ THE STRUCTURAL FIX IS THE RESOLVER, NOT THE 11 EDITS:** ISSUE-305 was a **wiring** bug (a column read by nothing), and site-by-site conversion kept reproducing it — each pass fixed what I looked for and left what I didn't, now *disagreeing* with what I'd fixed. One injected resolver makes "did this site read the column?" answerable **from a constructor signature**, and collapsed **3 duplicate copies** of the lookup my first cut had created. Kept **required** (not trailing-optional) in the 4 leave services — 6 fixture edits accepted — because a silent `?? calendar` fallback is exactly how a missed wiring hides; trailing-optional only where the blast radius was real. Memoized **keyed by tenant id**, not a bare field: the job runners do scope-per-tenant, but if that ever changed a bare cache would apply one tenant's leave year to another's ledger (Critical Rule #1) — and it keeps the per-employee×leave-type accrual sweep from costing ~10k extra queries/day.  Month resolved **once per sweep** (these page over every employee × leave type; per-pair would be an N+1). The year **LABEL is unchanged** (still the calendar year it starts in) → the change is additive, **no ledger row renumbered**. **⚠ The cron was the un-fixable half, and the plan missed it:** `ProcessLeaveYearEndJob` ran `"0 2 1 1 *"` — **1 January only**. No constant swap inside the job could ever fix that, because a 1-Jan cron cannot fire on 1 April; an Apr–Mar tenant's year-end would simply never run. Now **daily** (`"0 2 * * *"`) + per-tenant `ClosingLeaveYearIfDue` (7-day grace so a missed/failed run catches up next day instead of deferring a tenant's whole year-end by 12 months). Safe only because the per-pair work was already idempotent (NFR-3) — the same property that justified the old "daily-or-later cadence is safe" note. `LeaveAccrualJob` needed no reschedule (already daily), only the per-tenant label. **⚠⚠ THE AUDITORS CAUGHT A REGRESSION I WAS ABOUT TO SHIP, AND MY "7/7 MUTANTS KILLED" WAS FALSE.** This is the arc's sharpest lesson, and it happened on **my own** hand-written code — not a delegated build. (1) **`@integration-enforcer` → DISCONNECTED (CRIT):** I converted the ledger's **CREDIT** side (accrual/carry-forward) and left the **DEBIT** side (`LeaveRequestService` ×6, `LopService` ×2, `LeaveEncashmentService`, `RealPayrollFnFIntegration`) on raw `.Year`. Both key the same `LeaveLedger.LeaveYear` int → an Apr–Mar tenant would read **balance 0 every Jan–Mar → every leave request blocked "insufficient balance"**, and **F&F would silently under-pay leave encashment on termination**. That is **STRICTLY WORSE than the pre-fix code**, which was wrong for fiscal tenants but at least agreed with itself. (2) **`@test-authenticator` → THEATRICAL:** **4 of my 7 sites had ZERO mutation resistance** (pro-rata, both `referenceDate`s, the accrual label, the `OccurredAt` stamps) — my campaign only mutated the sites I *thought* to mutate; **2 arms were tautologies** (incl. one whose docstring explicitly claimed to defeat the CAL-5/CAL-6 initializer trap and provably didn't — month 0 ≡ month 1, so NO test on that path can detect a missing default; the real guarantee is the migration's `defaultValue: 1`, which InMemory never runs); and **it found `LeaveYearEndJobRetryTests` RED 358 days a year** (my daily-cron rewrite meant `RunAsync` did nothing outside 1–7 Jan) — **my full-suite runs kept dying on testhost file locks, so "0 Errors" was never a green suite.** **★ WHY THE SUITE STAYED GREEN THROUGH ALL OF IT:** calendar tenants are unaffected (`LabelFor(d,1) == d.Year`) and every new arm tested the side I had converted. **A green suite is not evidence when the arms and the fix share an author's blind spot.** **★ AND ONE OF MY MUTATIONS WAS A SILENT NO-OP AGAIN** (anchor never matched → "survived", proving nothing). Every round since asserts the edit LANDED before trusting a verdict. **★★ THE AUDITOR THEN PROVED MY "7/7 MUTANTS KILLED" WAS TRUE-BUT-MEANINGLESS: 4 of 7 sites had ZERO resistance** — I had only mutated the sites I already believed were covered — **and one of my 4 "KILLED" claims was FALSE when I made it** (I mutated the *resolver*, not the debit conversion; the arm died from a different mutant). Closed the 3 highest-value gaps: `LeaveAccrualJob` (the CREDIT-side label, **which had no test at all**) → KILLED (5) · service→engine basis → KILLED (1) · DI registration deleted → KILLED (5). **★★ THE BEST FIX WAS NOT A TEST:** `CalculateProRata`'s `fiscalYearStartMonth` had a `= CalendarStartMonth` **default** — that default is what made a missing argument invisible (a wrong MONEY number credited to the ledger, suite green). **Removed it**, so the mutant is now a **compile error**. Same reasoning as deleting the `?? .Year` fallbacks: **prefer "won't build" to "won't pass"**. **★★ A BACKGROUND AGENT MUTATED MY WORKING TREE AND I COMMITTED IT:** the auditor's batch was live when I ran `git add`; commit `0cc05dea` captured **8 real mutants as production code** (accrual hardcoded to calendar, LOP debit back to `.Year`, pro-rata basis dropped ×3, `OccurredAt` stamps ×2, dashboard bounds). Caught only because it marks mutants `/*MUTANT-Mn*/` → `git grep MUTANT HEAD`. Amended out (`ce9bfd83`), each restore verified line-by-line. **The standing "verify branch before every commit" rule is NOT enough — agents edit FILES; read the staged diff, and quiesce agents before committing.** **⚠ SHIPPING WITH 5 KNOWN GAPS (correct by inspection, no regression guard):** `LopService` debit · dashboard Pending bounds + `ResolveLeaveYear` · **`LeaveEncashmentService`** and **`RealPayrollFnFIntegration`** (both MONEY — F&F silently under-pays leave encashment on termination). The enforcer swept all 13 sites and found none disagreeing, but today proved inspection-without-a-guard is exactly how the ledger split survived. **→ follow-up.** **★★★ VERIFICATION (final): FULL SUITE 4086/4086 GREEN, 22m46s, no abort** — on an **idle** machine. **⚠ THE SUITE WAS NEVER BROKEN — I WAS BREAKING IT.** I filed **ISSUE-312 as HIGH** ("the full suite cannot complete; pre-existing; reproduced on BASE in a clean worktree") and gave **three confident wrong root causes** in sequence (OOM in the Performance module → Testcontainers contention → …). All wrong: every abort (231/371/519/964/1373/1593 of ~4058) happened while I ran **2 background auditors + parallel builds against the same box**, and even my base-branch "proof" was contaminated the same way. Finding **downgraded to MED and retitled** to the one defect that survives: **an aborted run still prints `Passed!` and exits 0**, so whatever kills a run — contention, a crash, a kill — the operator is told it passed. That is what hid the red retry test from me for hours. **The plan's earlier "3991/3997 green" figures are fine; my claim that they weren't is WITHDRAWN.** Standing rule now: **serialize — auditors, then builds, then the suite.** **Arms:** 67 → **80** (34 `LeaveYearTests` incl. fiscal pro-rata · 23 window · 10 `FiscalLeaveYearIntegrationTests` · 6 `LeaveAccrualJobLeaveYearTests` · 7 `LeaveYearResolverDiRegistrationTests`); the retry test fixed by a **`TimeProvider` seam** (assertion untouched — never weaken a test to go green). **⚠ 2 plan claims CORRECTED (do not re-inherit them):** (1) ISSUE-305's "supersedes ISSUE-176" is **WRONG** — ISSUE-176 is **Payroll** (StatutoryDeduction YTD, LOW), a different subsystem; it stays **OPEN**. Payroll's similarly-named `FiscalYearResolver` is a different concern (effective-dating windows, not fiscal-year bounds) — checked, no duplication. (2) "Site 8 = `LeaveReportService:1055`" understated it: the sweep found **5** read-layer sites → filed **ISSUE-311** (LOW) rather than folded in silently, since it is read-only display defaults with no money impact. **Still open from the epic:** `LeaveEntitlementRule.LocationId` (plan task 4.3 / CAL-4c) · TC-LV-ISO-049 + TC-CHR-ISO-049 · the FE phase (P6). **▶ NEXT: reassess — the epic is closed; the P7 LOW tail (+ the newly-filed ISSUE-306/307/311) is the standing queue.** | _(this file)_ |
| **2026-07-15 (c)** | **★ CAL-1 working-calendar FOUNDATION built (US-ATT-011 AC-1 + AC-2) — gates CAL-2..8.** `Location.DefaultShiftId` (`Guid?` FK → `shift`, `OnDelete(Restrict)`, nullable, CLI migration `Attendance_LocationDefaultShift`; **no new RLS policy — a COLUMN on the already-policied `locations` table**, the NEW-TENANT-TABLE rule is table-scoped) + `ShiftScheduleResolver` extended to the **four-tier chain** (Employee `EmployeeShift` → `Location.DefaultShiftId` → tenant `Shift.IsDefault` → **Mon–Fri code default**) + `DefaultShiftId` threaded end-to-end through Location CRUD (DTOs/commands/handlers/`ILocationService`/`LocationService`/controller/**`Snapshot()`** so the BUG-018 audit doesn't silently omit a calendar change), validated same-tenant + active → **400 `invalid_default_shift`**. Resolver kept **`internal static`** (the plan's `new ShiftScheduleResolver(ctx)` sketches do not compile); **5 fixed queries** regardless of employee count — the assigned-shift and location-default-shift ids are unioned into ONE `db.Shifts` query, so tier 2 costs **zero** extra round-trips. **⚠ I caught 3 real defects that the delegated build would have shipped:** (1) **the dev agent left `if (false && …)` short-circuiting the ENTIRE Location tier** — the whole point of CAL-1 was dead code (its report was truncated mid-"restoring the real implementation"); (2) **integration-enforcer HOLE 1 (ship-blocking, in-lane):** a `Flexible` shift may legally carry EMPTY `WorkingDays` (validator only requires them for Single/Rotating), and `TryGetValue` **succeeds** on it → tier 2 **shadowed** tiers 3/4 with an empty set → `CountWorkingDays` reads empty as **"every calendar day"** → a 7-DAY working week, i.e. the exact bug CAL-1 exists to kill, re-entering by the front door. Fixed via `ToCalendar` (empty ⇒ *unresolved* ⇒ fall through) at every tier; the docblock's "NEVER returns an empty set" claim was false and now states both halves of the guarantee. (3) **test-authenticator: tier-3 was a fully surviving mutant** — the fallthrough arm seeded a Mon–Fri tenant default, byte-identical to the Mon–Fri code default, so deleting the entire `IsDefault` query left it green; re-seeded **Mon–Sat** so Saturday discriminates tier 3 from tier 4. **⚠ The 07-15(b) "resolver code-default is a proven no-op (~90%)" adjudication was FALSIFIED** — `ShiftService.DeleteAsync` guards only *explicit* `EmployeeShift` refs and the tenant default's user base has none, so the empty-set path is reachable via a supported op → **BUG-287** (extended by both auditors to cover `Location.DefaultShiftId` refs too; FK `Restrict` can't help — it's a SOFT delete). The Mon–Fri default is therefore a **real behaviour change and the right one** (degrades 7-day → Mon–Fri). **Verification:** `dotnet build` 0 errors; **16/16 new arms green on real Postgres/Testcontainers**; **two independent stash-checks** — disabling tier 2 reddened exactly the 4 tier-2 arms, reverting the `ToCalendar` fix reddened exactly the 2 empty-`WorkingDays` arms (arms proven non-tautological); full BE suite green. Auditors: enforcer **CONNECTED**, authenticator **AUTHENTIC** (11/12 → 12/12 after my tier-3 fix); I added 4 arms myself (2 empty-`WorkingDays`, expired/future-dated assignment, latest-`EffectiveFrom`-wins — tier-1 effective-dating had **zero** coverage). TC-ATT-145/146/147/148/149 + ISO-014 → `automated`. **Auto-healed:** BUG-287 (extended), **ISSUE-306** (resolver has no `IsActive` filter — write-time refuses an inactive shift, read-time keeps using one deactivated later; needs-decision, latent today), **ISSUE-307** (Flexible shifts bypass `WorkingDays.NotEmpty` — contained by `ToCalendar`, still creatable). **▶ NEXT: CAL-2 (BUG-284, leave day-count via the resolver).** | _(this file)_ |
| **2026-07-15 (b)** | **Session grooming + re-prioritization — the HIGH band RE-OPENED; "next = P7 LOW tail" is now STALE.** Full re-survey of the ledgers (not memory): **218 open findings — 0 CRIT · 2 HIGH · 40 MED · 156 LOW · 20 ENH** (the header's "244 open / 14 HIGH" is stale; that band cleared across #262–299). The **only 2 open HIGH are BUG-284 + BUG-285** — both filed 07-15(a), both money/entitlement, both the same root class (hardcoded work-week). They outrank the entire P7 LOW tail, which is hereby demoted. Docs branches **#307 (calendar epic) + #308 (template extraction) MERGED** by a concurrent session mid-grooming; both plans now on base. ⚠ The (a) entry's "TEST-FINDINGS.md left UNCOMMITTED" note is **STALE** — BUG-284/285/286 + ISSUE-304/305 + the ISSUE-303 flip are all committed on base; that reconcile is DONE. **User confirmed all 3 epic drivers are REAL** (non-Mon–Fri Gulf tenant · Apr–Mar fiscal tenant · multi-location holidays) → the 5 findings are **live defects, not latent**, and the Location tier is load-bearing → **user chose: run the FULL epic (Phases 1–4)**; FE template-extraction **DEFERRED** (13 PRs of churn, collides with the epic's FE phase, concurrent session live). **Two money-path defaults the plan buried were adjudicated (see 🔑 below):** resolver-fallback flip **de-risked to a no-op** (evidence: `DbInitializer.EnsureDefaultShiftAsync` seeds a Mon–Fri `IsDefault` shift per tenant on startup → the empty-set path is unreachable; ~90%) → adopt as a defensive backstop; **`ExcludeHolidaysFromWorkingDays` → user DECIDED default FALSE + effective-dated switch** (F&F precedent: never retroactive) → this re-scopes task 2.5 to a versioned policy (`TenantFnFPolicy` pattern), NOT a plain `AttendanceSettings` bool. **Epic re-split 4 → 8 PRs** (the plan's 4 understates Phase 2; 2.4 is the largest single chunk and least detailed). **▶ SESSION TARGET (user-chosen): PR1 Phase-1 foundation → PR2 BUG-284 → PR3 BUG-285/286, then reassess** — money bugs land early, not buried mid-epic. | _(this file)_ |
| **2026-07-15 (a)** | **Tenant + Location configurable working-calendar epic — DESIGN + BA/QA landed (NOT yet built).** Study of hardcoded working-calendar/policy values → 4-phase epic. Spec [`superpowers/specs/2026-07-14-tenant-location-configurable-calendar-design.md`](../../superpowers/specs/2026-07-14-tenant-location-configurable-calendar-design.md) + plan [`superpowers/plans/2026-07-15-tenant-location-configurable-calendar.md`](../../superpowers/plans/2026-07-15-tenant-location-configurable-calendar.md). Core = ONE four-tier resolver **Employee → Location → Tenant → code-default** (new `Location.DefaultShiftId`), consumed by leave/OT/payroll so no module hardcodes Mon–Fri/Sat–Sun again; supports single- AND multi-branch tenants (Colombo Mon–Fri + Dubai Sun–Thu + EU 4-day). Filed **BUG-284** (leave Mon–Fri hardcode → wrong balances), **BUG-285** (OT weekend Sat/Sun), **BUG-286** (OT holiday ignores LocationId), **ISSUE-304** (probation 90d hardcode), **ISSUE-305** (leave-year ignores `FiscalYearStartMonth`; supersedes ISSUE-176). New **US-ATT-011** (location working calendar, AC-1..5) + **US-CHR-013** (Employee FTE + WorkArrangement) + patched US-ATT-005/006/001/008, US-CHR-001/002/007/009, US-LV-002/003/006/008. **24 draft TCs** (TC-ATT-145..154 + ISO-014/015 · TC-CHR-326..330 + ISO-049 · TC-LV-262..264 + ISO-049 · TC-PAY-014/015). Verified already-done: BUG-113 Employee↔Location link RESOLVED (#261); `ShiftScheduleResolver` already shift-aware in payroll (#282). ⚠ **TEST-FINDINGS.md left UNCOMMITTED** — my 5 findings intermingle with the concurrent F&F session's ISSUE-303 flip; reconcile the two together on a clean base. **▶ NEXT (this epic): implement Phase 1** (`Location.DefaultShiftId` + resolver location tier) — gates Phases 2–4. | _(this file)_ |
| **2026-07-14 (p) — ▶ ACTIVE** | **F&F policy VALIDATION coverage added (user follow-up).** User asked whether the F&F *validations* were in the TCs — they were NOT (TC-PAY-013-01..07 traced only the settlement *computation*; the `FnFPolicyService`/`CreateFnFPolicyValidator` config surface had zero automated test). Added **`FnFPolicyServiceTests`** (3 arms): `CreateFnFPolicyValidator` effective-date-required; the **same-effective-date-replacement** money-adjacent rule (one active version per date → the resolver never tie-breaks non-deterministically between two same-date rows); latest-`EffectiveFrom`≤asOf resolution + safe code-default. **`TC-PAY-013-08`** authored (status automated) + registered across TRACEABILITY/TEST-STATUS/TEST-MATRIX (Payroll 199→200 TCs; US-PAY-013 7→8 TCs / 16→19 backing xUnit). **ISSUE-303 validation-half RESOLVED** (residual = pure HTTP-layer `FnFPolicyController` test + settlement 2-tenant cross-read arm). ⚠ ISSUE-303 status-flip in TEST-FINDINGS.md is intermingled with a **concurrent session's** uncommitted calendar-config findings → left uncommitted; re-apply on a clean base. **▶ NEXT: P7 LOW tail remainder.** | _(this file)_ |
| **2026-07-14 (o)** | **Traceability close-out for the F&F feature (#303) — US + TCs authored (doc-only).** Surfaced by the user: the F&F settlement (#303) shipped code + 16 xUnit tests but had NO IEEE-830 US / IEEE-829 TCs (Critical Rule #4 gap). US-ONB-005 only covers the F&F *trigger* (FR-6/BR-4 explicitly defer the *calculation* to Payroll); no Payroll US existed for it, and the tenant-configurable policy was net-new. **Authored `US-PAY-013` (Full & Final Settlement, business-analyst)** — 7 ACs (policy config, effective-dated no-retroactive, auto-compute-on-completion, idempotency, money-safety, no-double-pay, tenant-isolation), FR-1..7, BR-1..8, Phase-2 deferrals; registered in BA/INDEX + BA/STATUS (shipped Phase 1 #303), cross-linked from US-ONB-005 FR-6/BR-4. **Authored `TC-PAY-013-01..07` (qa-engineer)** — one per AC, each traced to the real green xUnit tests (status `automated`), + TRACEABILITY-MATRIX + TEST-STATUS rows; Phase-2 noted as uncovered. Closes the P0-2 traceability item for F&F. **▶ NEXT: P7 LOW tail remainder.** | _(this file)_ |
| **2026-07-14 (n)** | **P2-1d — AttendanceSummaryExportJob export-ready notify shipped + merged (#304).** The 3rd/last export-ready notify gap (US-ATT-007 FR-7 "download link via notification"), a faithful mirror of the Phase 8 LeaveReportExportJob pattern: `RunAsync` threads `requestedByUserId` (from `_currentUser.UserId` at enqueue), the job dispatches a new `attendance_summary_export_ready` catalog event (in-app+email, guarded, skip-on-empty) to the requester. ICurrentUser injected optional (non-breaking). 3 dispatch unit tests mirror the leave test; full BE **3878** green; enforcer CONNECTED (slot-3 threading verified). Auto-healed → **ISSUE-302** (enqueue-site positional-slot test arm, P7 — wiring confirmed by inspection, needs a >1k-employee harness). **▶ export-ready notify family now COMPLETE. NEXT: P7 LOW tail remainder.** | _(this file)_ |
| **2026-07-14 (m)** | **★★ ISSUE-294 F&F SETTLEMENT Phase 1 shipped + merged (#303) — the session's largest item.** User direction (surfaced 4 money-critical decisions first): F&F is a **per-tenant, effective-dated policy** (`TenantFnFPolicy`; changes next-cycle, never retroactive), NOT a hardcoded choice. Replaced `LogOnlyPayrollFnFIntegration` with a real engine: new `FinalSettlement`(+`Line`) entities (idempotent on `OffboardingInstanceId`; 23505 race recovers to the winning ref), computed by REUSING the run's engines (leaver `ProRataPaidDays`+`PayrollSlipCalculator`, `StatutoryDeductionResolver` w/ Location→Tenant→sole-rule→null + skip+flag, forfeitable `LeaveCarryForwardCalculator`); net floored at 0; structure statutory/deduction lines dropped (no double-count); `PayrollRunProcessor` boundary guard (belt-and-suspenders — `CompleteAsync` terminates in-tx). Persistence = dedicated entity (architecture call, not a tenant knob). 3 tenant tables + dormant RLS; migration via `dotnet ef`; API-configurable policy (no FE — Phase 2). Backend-dev built it; I read/verified the money paths, fixed the 23505-race backstop bug (authenticator flag), and added 7 material arms (effective-dated no-retroactive [the user's core requirement], multi-component gross w/ structure-statutory+deduction drop, both skip paths, code-default, run-guard both directions, real-Postgres money figure, full DI-swap seam). Full BE **3868** green; 16 F&F tests + DI-swap; enforcer CONNECTED + authenticator AUTHENTIC. **Phase 2 (deferred, needs-BA formula model):** gratuity/notice/severance/loan-recovery + settlement PDF + FE (`docs/DEV/FNF-SETTLEMENT-PLAN.md`). Auto-healed → **ISSUE-301** (policy-flag coupling, LOW). **▶ NEXT: P2-1d (AttendanceSummaryExportJob notify) + remaining P7 tail.** | _(this file)_ |
| **2026-07-14 (l)** | **★ P7 — ISSUE-296 systematic a11y dialog sweep shipped + merged (#302).** No shared modal existed → 16 hand-rolled `role="dialog"` overlays shared the BUG-109 focus-trap defect. Introduced ONE shared `[appTrappedDialog]` directive (CDK `FocusTrap` + initial-focus capture + Escape→`dismiss` + destroy-on-teardown; barrel-exported; 4-test behavioural spec) and migrated all 16 onto it (additive; animations/backdrops untouched; invite-users' redundant `(keydown.escape)` removed). Scope = focus-trap+Escape (mirrors PR #295); `inert`-on-root deferred (opt-in follow-up). Directive written+proven by me first, then 16 mechanical migrations delegated to frontend-dev; I completed the 5 spec arms it didn't reach + promoted 3 to behavioural Escape-closes on the authenticator's rec. FE Karma **3806** green; enforcer CONNECTED (all 16 wired to real close methods, no double-handler/leak) + authenticator AUTHENTIC 20/20. **▶ NEXT: ISSUE-294 F&F settlement — surfacing the 4 money-critical scope decisions to the user before building.** | _(this file)_ |
| **2026-07-14 (k)** | **P7 tail — TAX preview inputs + CountryCode normalization guard shipped + merged (#301).** Three multi-country statutory-tax follow-ups on one branch: **(A)** TAX-2 component-aware preview — the FR-5 preview took an optional `ComponentAmounts` map so a `PercentOfComponent` exemption previews == the run (was hardcoded `ComponentAmountsById:null` → 0). **(B)** TAX-3 cumulative preview — optional `PriorTaxableIncomeYtd`/`PriorTaxWithheldYtd` scalars threaded into `StatutoryWageInput` so a cumulative rule previews the YTD true-up delta, not the first-month figure (default 0 → monthly unchanged). **(C)** TAX-3 CountryCode guard — `StatutoryDeductionResolver` now matches `upper(btrim(country_code))` (case AND whitespace) to mirror the cumulative pre-scan/report EXACTLY; a raw/import `"lk"`/`"LK "` write no longer diverges (pre-scan threads prior-YTD while the resolver matched nothing). All inputs optional/backward-compat; no migration. **Verify:** full BE 3855 green + tax classes 42/42 on final code; enforcer CONNECTED (found the whitespace gap → fixed in-PR) + authenticator AUTHENTIC 4/4. Tests incl. a **live preview==run cross-check reading real persisted slips** + non-cumulative no-op + real-Postgres `upper(btrim())` `[Theory]` over case+whitespace. **Auto-healed:** ISSUE-299 (LOW — create-time overlap check still raw-compares stored CountryCode), ISSUE-300 (MED test-health — cumulative RUN path InMemory-only). **▶ NEXT P7: ISSUE-296 a11y dialog sweep (FE).** | _(this file)_ |
| **2026-07-14 (j)** | **▶ P7 LOW tail started — ISSUE-297 goal-comment notify routing shipped + merged (#300).** `GoalProgressService.AddCommentAsync` routed `goal-comment-added` to the goal owner unconditionally, so after ISSUE-141 (#298) enabled two-way replies an owner's own reply notified the owner and never the manager. Fixed: route to the COUNTERPARTY — owner reply → manager (`ResolveManagerIdAsync`, the FR-5 seam), manager/HR comment → owner (unchanged), owner-with-no-manager → skip dispatch (never notify the author, never null-recipient HR broadcast). Single dispatch-site change; subject arg stays owner for payload. 3 unit arms (both never-notify-author negatives + null-branch). Full BE suite **3850** green; integration-enforcer CONNECTED + test-authenticator AUTHENTIC (null-branch arm added on rec). **Auto-healed → ISSUE-298 (LOW):** the `goal_comment_added` template copy still says "Your manager added a comment" — now wrong for the manager recipient; author-aware copy needs an interface+catalog change → deferred, not folded in. **▶ NEXT P7: TAX preview inputs + CountryCode guard.** | _(this file)_ |
| **2026-07-14 (i)** | **★ MED cluster 4/4 — recruitment/Performance gaps DONE (#298 + #299) → ALL FOUR non-payroll MED clusters COMPLETE.** Split into two PRs. **#298 (Performance, 4a):** ISSUE-133 (PipController.List missing `Performance.Read.Self` → employee 403 on own PIP list; added, service already self-scopes), ISSUE-140 (100%+explicit-InProgress force-completed → narrowed auto-complete to NotStarted only), ISSUE-141 (user decision: enable — goal OWNER may now comment on own goal; non-owner Read.Self still 403), ISSUE-145 (over-budget submit required no justification → 400 `over_budget_requires_justification` before charging; justified over-budget still proceeds). **#299 (Recruitment, 4b):** ISSUE-232 (surface `ConvertedToEmployeeId`/`IsConverted`/`ConvertedAt` on the applicant read DTOs — additive, no migration), ISSUE-137 (user decision: build — new `Reports.View.Department` perm + fail-closed anti-widening dashboard scope: dept-scoped caller auto-restricted to own `Employee.Department`; no-employee→empty never whole-tenant; departmentId clamped + VacancyId intersected). Both PRs: full suite 3836/3847 green; enforcer CONNECTED (no cross-user/tenant hole; `null` reachable only on full-access; empty-set≠no-filter) + authenticator AUTHENTIC (modified Performance tests corrected-not-weakened; anti-widening + fail-closed arms land the regressions). **Auto-healed (LOW, P7):** ISSUE-141 two-way-thread notification routing (owner reply notifies owner not manager). **★★ P2-2 MED functional cluster fully cleared. ▶ NEXT: P7 LOW tail** (~12 auto-healed follow-ups from the tax/ClamAV/MED work). | _(this file)_ |
| **2026-07-14 (h)** | **MED cluster 3/4 — Redis cluster shipped + merged (#297).** **BUG-115:** the shared multiplexer had `AbortOnConnectFail=false` but the 5s default `SyncTimeout` → a FROZEN Redis (docker-pause) still stalled ~11s/request; `SharedRedisRegistration` now bounds `SyncTimeout`+`AsyncTimeout` (config `Redis:OperationTimeoutMs`, default 1000ms) → fast-fail to DB. **BUG-116 (authz-correctness):** the `user:{userId}:tenants` cache was invalidated at only some sites (ISSUE-056) → stale memberships/roles ≤5min; new fail-soft `IMyTenantsCache` (one key source), AuthService read/write + ALL SIX mutation sites now invalidate after save (SSO-JIT, role-assign, role-replace, disable, new-owner, tenant-deletion per-user loop); ISSUE-056 sites consolidated (no double-evict). 8 lock-in tests (one per site incl. the deletion loop) + 2 BUG-115 DI tests; full suite **3822** green; enforcer CONNECTED + authenticator AUTHENTIC (the 4 previously-unguarded sites closed in-PR). **▶ NEXT MED cluster 4/4: recruitment/perf gaps (ISSUE-133/137/140/141/145/232).** | _(this file)_ |
| **2026-07-14 (g)** | **MED cluster 2/4 — UTC/date-boundary cluster closed (#296 + 2 stale-closures).** Triage found **BUG-245** (`AttendanceDashboardService`) + **BUG-246** (`AttendancePayrollService`) were ALREADY fixed by commit `7343c9b5` (the `TenantClock` tenant-timezone fix, Jul 8) — confirmed in history + `TenantClock` in both services → marked **RESOLVED (stale-fixed)**, not re-done. Only **BUG-085** was genuinely open: a date-only `?startDate`/`endDate` binds to `Kind=Unspecified` → Npgsql refuses `timestamptz` → 500. Fixed **#296**: `AuditLogService.BuildFilteredQuery` normalizes each bound to UTC (`SpecifyKind(Utc)`/`ToUniversalTime`) before the predicate (fixes list + export; no-op for ISO-Z). Testcontainers regression arm in `AuditLogSearchPostgresTests` (throw only reproduces vs real `timestamptz`); 5 Postgres + 26 unit AuditLog green; full suite 3817 pass + 1 unrelated flake (BenefitPlanPostgresTests container-startup under 1h-contention; passes 10/10 isolated). **▶ NEXT MED cluster 3/4: Redis (BUG-115/116).** | _(this file)_ |
| **2026-07-14 (f)** | **MED cluster 1/4 — a11y WCAG cluster shipped + merged (#295).** BUG-108 (photo-upload: file `<input>` moved out of the `role=button` drop-zone + `tabindex=-1` → clears aria-hidden-focus + nested-interactive) · BUG-109 (new-payroll-run dialog: `A11yModule`+`cdkTrapFocus [cdkTrapFocusAutoCapture]`+`(keydown.escape)` → focus-trap/initial-focus/Escape) · BUG-110 (statutory fiscal-year tablist: `+ Add year` moved OUT of `role=tablist` → clears aria-required-children) · BUG-111 (regularization: `role=status`/`aria-live=polite` counter + same drawer focus-trap) · BUG-112 (payroll-integration: `tabindex=0`+`role=region`+aria-label on both `overflow-x-auto` wrappers → clears scrollable-region-focusable). All mirror the repo's sole correct `cdkTrapFocus` example (employee-profile). 5 regression Karma specs (render real components, fail on revert; Escape-close asserted behaviourally); FE build clean, touched specs **77 green**; test-authenticator AUTHENTIC 5/5 (both LOW gaps closed in-PR). **Auto-healed → ISSUE-296:** ~16 OTHER hand-rolled dialogs share the BUG-109 focus-trap defect (no shared overlay component) → filed as a systematic a11y-dialog sweep (introduce a shared trapped-dialog directive + migrate), NOT fixed here (would be an 18-dialog refactor). **▶ NEXT MED cluster 2/4: UTC-boundary (BUG-085/245/246).** | _(this file)_ |
| **2026-07-14 (e)** | **P3-1 ClamAV deploy-gate armed (#294).** The `ClamAvVirusScanner` (fail-closed INSTREAM-over-TCP, BCL sockets) + its DI config-gate already existed + were unit-tested (ISSUE-101) — only config was missing. Armed the **env-var deploy-gate** (mirrors the encryption-key gate): blank `VirusScanning:ClamAv:Host` in `appsettings.json` → AllowWithLog stub in dev/test/CI; prod sets `VirusScanning__ClamAv__Host` via env → fail-closed real scanning. Added a `clamav` (clamav/clamav:stable) compose service behind an opt-in **`scanning` profile** (never starts on a normal local `docker compose up`; ~1.5-2GB RAM), documented the blank key in `docker.env.example`, + a runbook `HRM.Infrastructure/Services/VIRUS-SCANNING.md`. **User decisions:** env-var gate; dev/local OFF; staging/prod run a separate clamd container. Config/compose/docs only — no src/behavior change; 12 ClamAV DI+protocol tests green. **▶ NEXT: remaining P2-2 MED clusters (a11y / UTC-boundary / Redis / recruitment-perf) + P7 tail.** | _(this file)_ |
| **2026-07-14 (d)** | **★★ TAX-4 YEAR-END TAX STATEMENT shipped + merged (#293) — MULTI-COUNTRY TAX FRAMEWORK COMPLETE (TAX-1/2/3/4 = #290/291/292/293).** Replaces the `deferred:true` `YearEndTaxStatement` stub with a real per-employee, per-country-FY summary of the TAX-3 `PayrollSlip.TaxableIncome`+`IncomeTaxWithheld` columns, via the EXISTING `PayrollReportExport` async pipeline — NO new entity/job/scheduler/controller/migration. Descriptor→non-deferred; new `BuildYearEndTaxStatementAsync` (one row/employee → Employee·Country·FiscalYear·TaxableIncome·IncomeTaxWithheld + TotalRow); renders CSV/XLSX/PDF free via the generic renderer; large tenants auto-route to Hangfire + `ReportExportReady`. **Reconciliation:** resolves each employee's country + FY via the IDENTICAL TAX-1 precedence the run uses, sums only FINALIZED slips in `[EffectiveFrom,EffectiveTo]` → the statement matches what payroll actually withheld (enforcer proved it mirrors `PayrollRunProcessor` line-by-line). No N+1; no-country/no-rule/no-slips employees SKIPPED (never a fabricated row). Tests: FY-sum (gross≠taxable so wrong-column fails), multi-country isolation + multi-row TotalRow, FY-window exclusion (prior+next FY), no-country skip, async-export-of-type. **Full BE suite 3817 green.** enforcer CONNECTED + authenticator arms strengthened in-PR (HIGH seed-column-distinction, TotalRow, upper-FY boundary). **Follow-ups (LOW):** Dec-anchored year-end (FiscalYear col self-labels); per-employee-PDF+bulk-ZIP @5k-scale = separate infra; run-through reconciliation arm. **▶ TAX FRAMEWORK DONE — next = the remaining P2-2 MED clusters (a11y, UTC-boundary, Redis, recruitment/perf) + P7 tail.** | _(this file)_ |
| **2026-07-14 (c)** | **★ TAX-3 YTD-CUMULATIVE income tax (per-country) shipped + merged (#292).** Cumulative-vs-monthly PAYE is a per-jurisdiction property → new **`StatutoryRule.IsCumulative`** flag (default FALSE = today's monthly behaviour, existing tenants UNCHANGED; user decision). When TRUE, bands are ANNUAL + each month withholds `tax(cumulative-FY-taxable) − withheld-YTD`. **`PayrollSlip.TaxableIncome` + `IncomeTaxWithheld`** persisted on EVERY slip (feeds YTD + TAX-4). Pure `StatutoryCalculator.ComputeIncomeTaxYtd`; resolver branches on the resolved rule's flag (shared preview/run via extended `StatutoryWageInput`); processor batch-loads per-country effective rules + prior slips (no N+1; ~13-month **ordinal** window surviving the Apr–Mar FY boundary) and sums prior taxable/withheld within the FY window (`period >= EffectiveFrom`). Full flag wiring + validation; CloneFiscalYear carries it. **⚠ Shared-path refinement (both auditors reviewed):** strip+flag discriminator moved `Lines.Count==0`→empty `FiscalYear` so a resolved-country zero-tax month (low earner/below annual threshold) flows through + persists TaxableIncome instead of being wrongly flagged; a genuinely-unresolved country still → empty FiscalYear → strip+flag (TAX-1 guarantee intact, still covered). Tests: `ComputeIncomeTaxYtd` unit; `YtdCumulativeTaxIntegrationTests` (true-up 0/0/0/60k/60k, non-cum default, re-run + exactly-one-slip, mid-year joiner, **FY-boundary reset**, **cumulative-base-post-exemption**); `YtdCumulativeTaxPostgresTests` (columns round-trip + **ordinal window translates across year boundary**). **Full BE suite 3809 green.** enforcer CONNECTED (money path traced, two rule-resolutions provably converge, discriminator cleared) + authenticator AUTHENTIC (material gaps closed in-PR). **Follow-ups (LOW):** component-aware cumulative preview; CountryCode normalization guard for raw writes. **NEXT:** TAX-4 year-end tax statement via `PayrollReportExport` (LAST tax branch). | _(this file)_ |
| **2026-07-14 (b)** | **★ TAX-2 country-aware configurable tax EXEMPTIONS shipped + merged (#291).** Configurable exemptions that reduce the income-tax taxable base, modelled as a child of an IncomeTax `StatutoryRule` (inherits country + FY + effective dates). New `StatutoryExemption` entity (`CalculationType` {FlatAmount, PercentOfGross, PercentOfComponent}, Value, ComponentId?, MaxAmount? cap, **IsAnnual** ÷12 for the monthly run per user decision, OrderIndex) + EF config + migration incl. the dormant `tenant_isolation` RLS policy (NEW-TENANT-TABLE rule). Pure `StatutoryCalculator.ComputeExemption`; resolved through the SHARED `StatutoryDeductionResolver` (preview==run) → summed into `ComputeTaxableIncome`, exposed as `ExemptionsApplied`; **reduces the tax base ONLY — not a payslip deduction line.** Run path now populates `ComponentAmountsById` (was null) so PercentOfComponent works; preview keeps null (documented limitation → follow-up). Full DTO/API/controller both ways; FluentValidation `ExemptionInputValidator` (MediatR pipeline) + service `ValidateExemptions` (income-tax-only, ComponentId-required); CloneFiscalYear clones exemptions. Tests: +8 `ComputeExemption` unit; `StatutoryExemptionIntegrationTests` (InMemory full chain — exact-figure reductions, annual÷12, cap, per-country isolation, exceeds-gross floor, stacked-sum, not-a-deduction-line, preview==run) + `StatutoryExemptionPostgresTests` (Testcontainers typed persistence + update-replaces-children). **Full BE suite 3795 green.** enforcer CONNECTED + authenticator AUTHENTIC 30/30 (all flags closed in-PR: stacked-sum arm, exceeds-gross arm, FluentValidation layer). **Auto-healed → follow-up: preview PercentOfComponent divergence** (preview hardcodes `ComponentAmountsById:null` → component-based reliefs show 0 in the what-if but apply at run; LOW, needs a component-amount preview input). **NEXT:** TAX-3 YTD-cumulative. | _(this file)_ |
| **2026-07-14 (a)** | **★ Multi-country statutory tax FOUNDATION shipped + merged (#290).** The base layer of the re-architected per-tenant multi-country tax framework (user changed scope mid-build: one tenant, branches in several countries, each employee taxed under their branch's country). **Core:** `Location.CountryCode` (ISO) + `Tenant.DefaultCountryCode` (round-trip both ways); `StatutoryDeductionResolver` filters by country BEFORE per-type effective selection (fixes the multi-country collision); 5-col unique index `(tenant,rule_type,country_code,fiscal_year,effective_from) WHERE is_deleted=false` (permits intra-FY versioning, forbids true dupes); per-country `CloneFiscalYear`; `PayrollRunProcessor` resolves each employee's country once/run (no N+1). **Never-silently-mis-tax semantics (money-critical):** `Location→TenantDefault→sole-rule-country (single-country backward-compat fallback)→null`; genuine ambiguity (multi-country tenant, no employee/tenant country) OR resolved-but-unconfigured country → strip statutory + flag; concurrent duplicate race → 409 (PG 23505) not 500. **Design decision (surfaced to user):** single-country fallback prevents silent under-taxation of every existing tenant (the original filter would have dropped ALL statutory for country-less employees). **Tests:** `MultiCountryTaxFoundationTests` (InMemory, full chain, incl. fallback + resolved-but-no-rules flag arms) + `StatutoryRuleMultiCountryPostgresTests` (Testcontainers: index allows versioning, rejects dupe w/ 23505, allows diff-country, honours is_deleted filter). **Full BE suite 3776 green.** integration-enforcer CONNECTED + test-authenticator AUTHENTIC (both flags — resolved-but-unconfigured MED, Postgres-index HIGH — closed in-PR). **NEXT:** tax branch 2 (country-aware configurable exemptions → `StatutoryCalculator.ComputeTaxableIncome`), then 3 (YTD-cumulative), 4 (year-end statement via `PayrollReportExport`). | _(this file)_ |
| **2026-07-13 (d)** | **ISSUE-290/291 + P2-2 audit-gaps cluster shipped (#278, #279, #280).** **#278** ISSUE-290 (perf-dashboard gate `Performance.Read.*`→`View.*` — real HIGH break, dashboard was unreachable; non-mocked regression pin). **#279** ISSUE-291 (custom-role `Reports.View.*` startup backfill + `docs/DEV/UPGRADE-NOTES.md`; user chose backfill+note; +cross-tenant test arm). **#280** P2-2 audit-gaps cluster: **BUG-083** PII-read audit implemented (`Payslip`/`Recommendation.ViewSensitive`, no values stored, post-authz + negative arm); **BUG-081/ISSUE-120/ISSUE-200 STALE-FIXED by #272's opt-OUT** (confirmed w/ regression tests); **BUG-085 re-clustered to UTC**. Auto-healed ISSUE-292 (onboarding semantic-naming ENH, LOW), ISSUE-293 (national-ID no field, needs-BA). BE 3700/3700; all auditors CONNECTED+AUTHENTIC. **User decisions:** ISSUE-200=close+LOW-enh, BUG-083 naming=`.ViewSensitive`. | _(this file)_ |
| **2026-07-13 (c)** | **DEC-2 + DEC-1 shipped (#276, #277).** **#276 (DEC-2, doc-only):** encryption key-rotation SOP runbook at `HRM.Infrastructure/Security/README.md` (overlap rotation + re-encrypt-backlog + verify-SQL); closed the deploy-gate; surfaced "no bulk re-encrypt job yet" → P7. **#277 (DEC-1):** dedicated `Reports.View.All`/`Reports.View.Team` taxonomy replacing the borrowed cross-module scope signal; both resolvers switched (Team now requires the explicit perm + a direct report — deliberate tightening), behavior-preserving add-only seed, Recruiter over-grant corrected, FE roles-catalog + perf-gate cleanup. BE 3685/3685 + FE 3763/3763; auditors CONNECTED+AUTHENTIC. **Auto-healed:** ISSUE-290 (perf-gate dead `Performance.Read.*` strings — pre-existing FE↔BE drift, needs-verify), ISSUE-291 (**⚠ release-note:** custom roles relying on old data-derived team scope drop to self-scope; built-ins auto-backfilled). **User decisions this session:** ClamAV=full-wire+require-daemon (caveat pending), key-rotation SOP=done, Reports perm=done. | _(this file)_ |
| **2026-07-13 (b)** | **P2-2 MED cluster started — ISSUE-226 (offer stored-XSS) shipped (#275).** Sanitize-on-write in `OfferService.GenerateAsync` for the 3 recruiter free-text fields, reusing the already-DI-registered `IHtmlSanitizer` (Ganss.Xss) — no new package; mirrors `VacancyService`. Explore corrected the ticket premise (PDF path is a QuestPDF literal-glyph sink, not HTML — never exploitable; real surface = JSON read path the FE renders; portal projection covered at source). Added 2 material auditor-recommended arms myself (integration DI+MediatR pipeline; `javascript:`/style allow-list pin). BE **3671/3671** green; Offer 50/50; integration-enforcer CONNECTED, test-authenticator AUTHENTIC. **User decisions captured** (see gate list): ClamAV = full-wire+require-daemon (with prod-fail-closed / dev-test-CI-green caveat) · key-rotation SOP = write runbook now · Reports.View.Team = build dedicated permission. Out-of-lane filed: `OfferLetterTemplate.Substitute` latent foot-gun (P7). | _(this file)_ |
| **2026-07-13 — ▶ RESUME POINT (session stop)** | **13-PR continued-execution arc merged (#262–274); prioritized queue's whole top band cleared.** After the P1 arc, ran the refreshed 🔝 queue top-down: **#262** ISSUE-245 (Karma verify-close) · **#263** BUG-243 (last P1 HIGH — Perf FE re-model + SaveGoals) · **#264** ISSUE-287 (mock) · **#265/#266/#268** US-NTF-006 Phases 6/7/8 (**notification delivery COMPLETE**) · **#267** ISSUE-288 (employee self-sign-off) · **#269** BUG-281 (write-time audit PII redaction) · **#270** P3-2 (Redis JWT denylist, fail-open) · **#271** P2-2a (ISSUE-195 HR-report manager-scope + cache-isolation; BUG-120=dup) · **#272** BUG-082 (audit opt-in→opt-OUT; ~50 entities now audited; fixed latent ActorEmployeeNo overflow) · **#273** P3-4 (PII-at-rest AES-256-GCM field encryption — ISSUE-134 + ISSUE-150-enc) · **#274** P3-3 (Redis permission cache, fail-open). BE gate green **3670/3670**; every PR passed integration-enforcer + test-authenticator. **User stopped here to review the batch + re-prioritize.** Remaining work + consolidated out-of-lane/gated items re-sorted in the 🔝 queue below. | _(this file)_ |
| **2026-07-12 (e)** | **P2-1 US-NTF-006 scope corrected + Phases 6 & 7 shipped.** Audit found delivery infra + 8 DI module seams already `Real*`-wired (NOT "13 LogOnly seams"); genuine remainder = inline deferred-notify sites. **Phase 6 (#265):** attendance/overtime/regularization (8 sites, new `IAttendanceNotificationService`). **Phase 7 (#266):** EmployeeStatus probation/reassignment + DocumentExpiry/ScheduledReport jobs (4 sites, new `ICoreHrNotificationService`, cross-tenant per-item TenantId). Both: 3575→3590 green, auditors WIRED+AUTHENTIC. **Auto-healed: Phase 8 tail (P2-1c)** = LeaveReportExportJob:70 + BulkEmployeeImportService:316 (2 sites the initial Explore missed). | _(this file)_ |
| **2026-07-12 (d)** | **P1-5c BUG-243 shipped (LAST P1 HIGH) + auto-heal.** FE re-model of 6 Performance services to the real routes (cycleId via `cycles/active`+`switchMap`) + the bulk `SaveGoals` BE endpoint (8 authentic tests). Verified FE 3759 / BE 3545 green (2 reds = pre-existing ISSUE-287). Auto-healed **ISSUE-287** (test-double staleness → new **P1-6, do next**), **ISSUE-288** (HIGH employee self-service sign-off BE gap → P5-5), **ISSUE-289** (LOW). **All 14 P1 HIGH now addressed.** Next: P1-6 (green BE gate) then P2 (US-NTF-006). | _(this file)_ |
| **2026-07-12 (c)** | **ISSUE-245 verify-closed (no code).** Full Angular Karma suite green ×2 runs (**3757/3757**, deterministic order) on `test/local-subdomains` — the ~26 red specs were cleared incidentally by the P1 merges (#254–261). FE gate now trustworthy. Next: BUG-243 (last P1). | _(this file)_ |
| **2026-07-12 (b)** | **P1-3 shipped + triage/auto-heal.** BUG-080 fixed (7 payroll audit emitters, 8 authentic tests). BUG-084 found stale→RESOLVED (already fixed under BUG-241). BUG-082 re-scoped + gated: filed **BUG-281** (write-time PII redaction) as its blocker → moved BUG-082 to P3-5. Auto-healed **ISSUE-282** (job-path audit Postgres arm) + a PayslipJobRls test-fidelity fix. | _(this file)_ |
| **2026-07-12** | **P1-2 shipped + auto-heal.** Fixed BUG-078 (OT rate off gross not basic). Auto-healed 2 out-of-lane discoveries: **BUG-280** (HIGH — identical defect over-deducted statutory EPF/ETF; fixed same PR #255) and **ISSUE-280** (LOW — carry `Code` on `PayrollSlipLine`; parked P7). Re-sorted plan. | _(this file)_ |
| **2026-07-11 (b)** | **Dev-plan execution refresh.** Ran the P0 triage-verify pass — reconciliation confirmed the **14 open HIGH** figure (heading-by-heading ledger parse); flagged BUG-003/086/002/005 as RESOLVED-token-but-body-conflict (re-verify). Added the **📊 Item-wise Status Tracker** (below) as the execution ledger; began executing P0→P7 one branch+PR per item. | _(this file)_ |
| **2026-07-11 (a)** | **Current active plan.** Rolled over from 07-10 (all it carried shipped). Rebuilt from a full findings/ledger/RLS survey → P0 ledger reconcile + missing TC suites … P7 LOW tail (body below). | _(this file)_ |
| 2026-07-10 | CLOSED — shipped US-ADM-011 workflow runtime (011a/b/c), Training & Benefits (US-TRN-001/002/003), Redis command-spans, RLS flip-prep (ISSUE-268/269/277) + flake fix (ISSUE-275). | [archive/COMPLETION-PLAN-2026-07-10.md](archive/COMPLETION-PLAN-2026-07-10.md) |
| 2026-07-06 | CLOSED — shipped P1–P3 (DataProtection, 8 findings, OTel/health/cache) + the RLS build (flag OFF) + US-ADM-011a + reconciliation Part II (Themes A–M). | [archive/COMPLETION-PLAN-2026-07-06.md](archive/COMPLETION-PLAN-2026-07-06.md) |

_Other closed one-off plans (blocked-TC re-exec/remediation, blocker-verification, fix-findings) also live in [`archive/`](archive/); dated QA reports/snapshots live in [`../reports-archive/`](../reports-archive/)._

## ✅ What shipped 2026-07-10 → 2026-07-11 (closed plan)
- **US-ADM-011 workflow runtime epic** — 011a (#238) · 011b parallel+SLA+notifs (#239) · 011c delegation + Attendance/
  Overtime/Offer wiring + read API (#240).
- **Training & Benefits** — US-TRN-001 catalog/enrol (#241) · 002 benefit plans (#242) · 003 eligibility/enrol (#243).
- **Redis command-spans** shared instrumented multiplexer (#245). **agent-config-guards** (#237).
- **RLS flip-prep + validation:** ISSUE-268 notification/session GUC (#244) · ISSUE-269 payslip long-tx split (#246) ·
  local RLS-on validation NO-GO→**GO** + `roles.sql` fix + findings (#247) · **ISSUE-277** per-request-tx → session-scope
  `TenantGucConnectionInterceptor` (#248, the critical flip-blocker). **ISSUE-275** test-flake stabilized (#249).

---

## ⚠ Reality check before diving in

**▶ CURRENT (re-surveyed 2026-07-15 — supersedes the 07-11 figures below):** the ledger holds **218 OPEN findings —
0 CRITICAL · 2 HIGH · 40 MED · 156 LOW · 20 ENH** (of 453 unique findings; 224 RESOLVED). The **2 HIGH are BUG-284 +
BUG-285** (work-week hardcode → wrong leave balances / wrong OT pay) — both live, both money, both in the CAL queue
above. The original 14-HIGH band was fully cleared across #262–299. ⚠ The file's own summary table (TEST-FINDINGS.md
lines 15–20) is **stale — do not use it**; four status encodings coexist in that ledger and 20 ENH entries carry no
`Status:` field at all (open by construction per its preamble).

_(historical, 2026-07-11)_ The ledger holds **244 OPEN findings (14 HIGH · 78 MED · 152 LOW)**. Many LOW/MED date to the June QA arc and some may
be **stale** (fixed-but-not-re-verified, or test-env/persona artifacts). **Do a triage-verify pass on the HIGH/MED band
first** (`/verify-fix` or a quick re-exec) before scheduling fixes — don't assume all 244 are live. Two ledgers are
**stale and must be reconciled** (P0). Line anchors below are into `docs/QA/TEST-FINDINGS.md`.

**Triage-verify result (2026-07-11b):** heading-by-heading reconciliation **confirms 14 genuinely-OPEN HIGH** (BUG-060-Payroll,
071, 077, 078, 080, 082, 084, 097, 100, 113, 123, 124, 125, 243) — the P1 list stands. **Re-verify-before-trusting (RESOLVED
token but body says "STILL PRESENT"):** **BUG-003 (CRIT** cross-tenant settings write — but memory's 2026-07-03 note says
CLOSED via #119 `TenantAccessGuardMiddleware`, so body is likely stale), **BUG-086** (HIGH, Leave 'Accrued' enum 500 — dup of
BUG-037), **BUG-002/BUG-005** (MED, graceDays default + localization). These 4 lead P1.

## 📊 Item-wise Status Tracker (execution ledger — update per branch+PR+merge)

> **⚠️ SUPERSEDED (2026-07-19) — the P0–P7 body below predates the arc-k/arc-l/#383–390 campaigns and is STALE. The LIVE queue is [`docs/QA/DEFERRED-FOLLOWUPS.md`](../DEFERRED-FOLLOWUPS.md).** Kept for changelog/traceability only. Do NOT execute top-down from here. Notable already-shipped since this body was written: the working-calendar epic (CAL-1..8) is COMPLETE (see changelog 2026-07-16); ISSUE-021 SalaryGrade = DONE #389; BUG-056 goal-finalize = DONE #387; ISSUE-285 birthday-index = shipped #390 (remainder DF-50/DF-51).

> Legend: `TODO` · `WIP` (branch cut) · `PR#nnn` (PR open) · `MERGED` · `VERIFIED` (post-merge re-test green) · `BLOCKED` ·
> `PARKED` (decision/ops-gated). One row = one branch. Findings/IDs in parentheses. Keep this table authoritative;
> re-sort as reality changes.

### 🔝 RESUME POINT (2026-07-15 — GROOMED; execute top-down, PR+merge each; auto-heal out-of-lane into this queue)

> ~~**▶▶ START HERE — CAL-1 → CAL-2 → CAL-3 (the working-calendar epic).**~~ **STALE (2026-07-19): the CAL-1..8 working-calendar epic is COMPLETE (see changelog 2026-07-16); BUG-284/285 closed with it.** Do NOT start here — the live queue is `docs/QA/DEFERRED-FOLLOWUPS.md`.
>
> **🔑 MONEY-PATH DECISIONS (settled 2026-07-15 — do not re-litigate):**
> - **Resolver code-default = Mon–Fri `{1,2,3,4,5}`** — **ADOPT** (decision unchanged), but ⚠ **the "proven no-op (~90%)" rationale was FALSIFIED on 2026-07-15 while pinning CAL-1 — do not repeat it.** `DbInitializer.EnsureDefaultShiftAsync` (`:586`) + `TenantProvisioningService.SeedDefaultShift` do seed a Mon–Fri `IsDefault` shift per tenant, **but the empty-set path IS reachable via a supported operation**: `ShiftService.DeleteAsync` (`:197-238`) guards only on **explicit `EmployeeShift` assignments**, and the tenant default's entire user base has **no assignment row** → `assignedCount == 0` → the default shift deletes → the resolver's `s.IsDefault` query returns null → empty set → `CountWorkingDays` (`:94`) counts **every calendar day** → ~22-day denominators become ~30. Filed as **BUG-287 (MED)**. **The Mon–Fri code default is therefore a real behaviour change — and the RIGHT one**: it degrades that failure mode from "7-day week" to "Mon–Fri". It does NOT fix the missing delete guard (BUG-287 stays open). Blast radius = 6 call sites: `PayrollRunProcessor` (`:198/:335/:996`), `RealPayrollFnFIntegration` (`:115`), `LeaveEncashmentService` (`:235`), `AttendancePayrollService` (`:106/:133`), `AttendanceDashboardService` (`:323`) → **the no-default-shift regression arm is mandatory, not optional.**
> - **`ExcludeHolidaysFromWorkingDays` = default FALSE + EFFECTIVE-DATED switch** (user decision). Default TRUE would raise the OT hourly base for **every existing tenant** on their next run — rejected per the F&F "never retroactive" precedent. ⚠ **Re-scopes task 2.5**: needs a versioned/effective-dated policy (reuse the `TenantFnFPolicy` pattern), NOT a plain `AttendanceSettings` bool. Size 2.5 accordingly.
> - **Cross-tenant `DefaultShiftId` ref → 400** (settles the plan's `// or 404` assertion ambiguity).
>
> **Epic re-split 4 → 8 PRs** (the plan's "4 phases = 4 PRs" understates Phase 2; task **2.4 is the largest single chunk and the least detailed** — it gets its own PR):
>
> | PR | Scope | Gate |
> |----|-------|------|
> | **CAL-1** | Phase 1 foundation: `Location.DefaultShiftId` (+EF config +migration) · extend `ShiftScheduleResolver` to the four-tier chain (Employee→Location→Tenant→Mon–Fri) · expose `DefaultShiftId` in Location CRUD+validator | **gates 2–4** |
> | **CAL-2** | **BUG-284 (HIGH)** — leave day-count + half-day gate via the resolver; 3 sites `LeaveRequestService.cs:132/:142/:353`. Keep Mon–Fri fallback → single-branch tenants unchanged (regression arm) | money |
> | **CAL-3** | **BUG-285 (HIGH) + BUG-286 (MED)** — OT weekend basis from the resolved working-day set (`OvertimeMultiplierResolver.cs:36`) + holiday lookup via location-aware `IHolidayProvider` (`OvertimeService.cs:798-801`) | money |
> | CAL-4 | Task 2.4 — location-scoped `AttendanceSettings` override + `AttendancePolicyResolver` + unique `(TenantId,LocationId)` migration; repoint OT/late/geofence read sites (**biggest chunk; underspecified in the plan — spec it before building**) | |
> | CAL-5 | Task 2.5 — payroll working-days denominator via the **effective-dated** holiday-exclusion policy (see 🔑) | money |
> | CAL-6 | Phase 3 — `Employee.Fte` + proration (US-LV-002 AC-K1) · `FteScaledOvertimeBase` (default false) · `Employee.WorkArrangement` + Remote geofence exemption (+FE employee-form touch) | |
> | CAL-7 | Phase 4a — **ISSUE-304** configurable probation (`Tenant.ProbationPeriodDays` + `Location.ProbationPeriodDays?`) | |
> | CAL-8 | Phase 4b — **ISSUE-305** `FiscalYearStartMonth` → leave year via `LeaveYear` | ✅ #318 (7 sites + cron reschedule; `LeaveEntitlementRule.LocationId` still open) |
>
> **⏹ SESSION TARGET (user-chosen 07-15): CAL-1 → CAL-2 → CAL-3, then reassess at a clean boundary.** Sequencing rationale: the 2 HIGH money bugs land immediately after the gating foundation instead of being buried mid-epic. CAL-4..8 next session. Standing constraints: `dotnet ef` CLI migrations only · dormant `tenant_isolation` RLS policy per new tenant-scoped table · a cross-tenant negative arm per new FK · **calendar/money tests on real Postgres** (InMemory masks these). ⚠ A **concurrent session is live** on this repo (it merged #307/#308 mid-grooming and moved HEAD) — verify branch before every commit and scope every `git add` to explicit paths.
>
> **📉 Demoted / deferred by this grooming:** P7 LOW tail (156 LOW + 20 ENH) · FE template-extraction (~13 PRs, 210 components — deferred: collides with CAL-6's FE work + concurrent session) · P0-2 missing TC suites · the 40 MED tail.
>
> **🧹 Ledger hygiene defects found (fix opportunistically, don't let them mislead):** the summary tables in **TEST-FINDINGS.md** (lines 15–20), **TEST-STATUS.md** (Tally ~154–162: claims 86 `[!]`/3 `[x]`; actual **99 `[!]` · 5 `[x]` · 2 `[ ]` · 5 `[b]`**), and **BA/STATUS.md** ("In progress: 1" vs 6 actual `[~]`) are ALL stale — per-module rows are the source of truth. **7 duplicate finding IDs**; **BUG-068** and **ISSUE-140** are each TWO genuinely different findings sharing one ID → invisible to any automated count. Four items carry live work behind a non-OPEN status (ISSUE-303, ISSUE-150, ISSUE-237, BUG-123).
>
> ---
> _(superseded 2026-07-14 note)_ Everything through the P2-2 MED functional cluster is DONE; two small deferred non-P7 items remain (P2-1d ✅ shipped #304, P0-2). Base `test/local-subdomains` @ HEAD ≈ `9f9d65a1`. Full suite **3847** green. Method unchanged (one branch/cluster → Explore/pinned-contract → delegate → verify build+Postgres+Karma + 2 auditors → RESOLVED in TEST-FINDINGS+tracker → PR+merge). **This session merged 25 PRs (#275–299):** tax framework TAX-1/2/3/4 (#290-293), ClamAV deploy-gate (#294), and all 4 non-payroll MED clusters (a11y #295, UTC #296, Redis #297, Performance #298, Recruitment #299) — plus the earlier #275-289 band.

**✅ CLEARED — the entire prior top band (13 PRs #262–274) shipped:** ISSUE-245, BUG-243 (last P1 HIGH), ISSUE-287, US-NTF-006 delivery (Phases 6/7/8), ISSUE-288, BUG-281, P3-2 JWT denylist, P2-2a RBAC-scope (ISSUE-195; BUG-120=dup), BUG-082 audit-all, P3-4 PII-at-rest encryption, P3-3 Redis permission cache. **BE gate green 3670/3670; all merged, all audited.** Per-item detail in the tracker table below.

**⚠ DEPLOY-GATES & NEEDS-DECISIONS (surface before the relevant deploy / to the human):**
- **[OPS, HIGH] Encryption key (P3-4/#273):** prod/staging MUST set `Encryption__Keys__hrm-field-key-1` (base64 32-byte) via env/secret before app start, or it FAIL-FASTS by design. Dev + tests carry a key.
- **[OPS] RLS prod flip** — the flag is committed OFF; the actual staging/prod flip is the user's ops step (README §3b runbook).
- **[OPS, optional] ClamAV upload scanning (P3-1/#294):** OFF by default (AllowWithLog stub). To enable fail-closed scanning in staging/prod: run a clamd (`docker compose --profile scanning up`, or a host daemon; ~1.5-2GB RAM) + set `VirusScanning__ClamAv__Host` via env. Dev/test/CI stay off. Runbook: `src/backend/HRM.Infrastructure/Services/VIRUS-SCANNING.md`.
- **[DECISION ✅ DONE 2026-07-13, #276] Encryption key-rotation SOP** (P3-4) — runbook written at `src/backend/HRM.Infrastructure/Security/README.md` (DEC-2). Follow-up surfaced: no bulk re-encrypt job yet (parked P7).
- **[DECISION ✅ RESOLVED 2026-07-13] `Reports.View.Team` dedicated permission** (ISSUE-195) — user chose **build the dedicated perm** → queued as **DEC-1** (net-new perm + repoint 6 HR builders).
- **[DECISION ✅ DONE] ISSUE-021** (SalaryGrade entity) → **DONE #389** (entity + CRUD `/api/v1/tenant/salary-grades` + FE + `JobTitle.GradeId` FK-validation) · **BUG-056** (goal-finalize seam) → **DONE #387** (`POST /tenant/performance/goals/finalize` → `GoalStatus.Finalized`/409 `goals_finalized`; re-open = DF-46).

**▶ REMAINING WORK (all HIGH/MED bands cleared; ranked):**
1. **✅ P2-2 MED functional cluster — COMPLETE.** All sub-clusters shipped #275–299: ISSUE-226 XSS (#275), DEC-1/DEC-2 (#276/#277), ISSUE-290/291 (#278/#279), audit-gaps (#280), 5 MED payroll clusters (#281-285), features 165/154/178 (#286-289), **tax framework TAX-1/2/3/4 (#290-293)**, and **all 4 non-payroll MED clusters** (a11y #295, UTC #296, Redis #297, Performance #298, Recruitment #299). Per-branch detail in the changelog (2026-07-13(b)–2026-07-14(i)) + the tracker table below.
2. **P2-1d — AttendanceSummaryExportJob notify** (US-ATT-010 FR-7 export-ready; MED) — auto-healed from Phase 8, same Site-B pattern (thread `requestedByUserId` + a report-ready event). Small; completes the export-notify family. (Only remaining non-P7 MED item.)
3. **▶ P7 LOW tail — THE NEXT-SESSION START.** ~12+ auto-healed follow-ups; batch-triage by module. Higher-value first:
   - **ISSUE-296** — systematic a11y-dialog sweep: ~16 hand-rolled `role="dialog"` overlays share the BUG-109 focus-trap defect (no shared component). Fix = a shared trapped-dialog directive/wrapper (`cdkTrapFocus`+Escape+inert) + migrate the copies (a small FE feature, not a one-liner).
   - **TAX preview inputs** — (a) exemption **PercentOfComponent preview** shows 0 (FR-5 test-calc hardcodes `ComponentAmountsById:null`); (b) **cumulative preview** shows first-month not the YTD delta (no prior-YTD preview input). Run is correct in both; only the what-if under-shows. Needs a per-component + prior-YTD input on `TestStatutoryCalculationQuery`.
   - **ISSUE-141 notification routing** — a goal-owner's reply currently notifies the owner, not the manager (two-way thread should notify the other party).
   - **TAX-3 CountryCode normalization guard** — the processor↔resolver convergence assumes stored `CountryCode` is normalized (the service does it on every write); add a CHECK/normalization guard for a raw/import write bypassing the service.
   - **year-end per-employee PDF + bulk-ZIP @5k-scale** (TAX-4 deferred — separate infra beyond the columnar report).
   - **Test-arm follow-ups (LOW):** ISSUE-137 cross-tenant-employee-resolution arm; a run-through TAX-4 reconciliation arm; Postgres arms mirroring the InMemory money-path/dept-scope tests; BUG-083 read-audit Postgres arm; bulk field-encryption re-encrypt job (P3-4 rotation SOP).
   - **Older LOW items** (pre-existing, batch by module): ISSUE-289, ISSUE-270/274/280/282, the P3-3 version-key TTL-refresh + malformed-JSON arms, `Performance.Read.*` docstring sweep, ChangeTracker.Clear tidy, `OfferLetterTemplate.Substitute` foot-gun, `LocalReportExportStorage` path param, FE async-export UI, etc. — see the P7-1 tracker row for the full list.
4. **P0-2 Missing TC suites** (qa-engineer, report-only) — IEEE-829 TCs for the stories shipped this arc (Training/Benefits, US-ADM-011, US-NTF-006 delivery) + ISSUE-273. Low-risk, traceability only.
5. **Verify tasks (not net-new):** `/verify-fix` re-run of the resolved-this-session findings' TCs to flip `TEST-STATUS.md` (the fixes are merged + auditor-verified but the story-level TC ledger wasn't re-run); BUG-003-family live `--iso` re-run.

**Still-open partials / verify tasks (not net-new):**
- **ISSUE-150 (partial)** — the compensation-at-rest ENCRYPTION half is DONE (#273); the `currentCompensation` SNAPSHOT/join-from-Payroll seam is still unbuilt (separate feature, FR-5 comp comparison + AC-5 gate can't be exercised until then).
- **BUG-003 family** — statically LIKELY-FIXED; formal closure still wants a live `/verify-fix --iso` re-run (park as a verify task).

> **Gated/parked (not in the active queue):** ~~ISSUE-285 (dashboard SLA — birthday-index migration decision + k6 rig)~~ **birthday-index shipped #390; remainder DF-50 (widget parallelism via child-scope-per-widget runner + warmup) shipped #415 + DF-51 (k6 p95/50k) ✅ VALIDATED — DONE**, P4-1 RLS code tail (ISSUE-269 long-tx), P5 net-new stories (US-ADM-012 / US-PRF-011 / US-PLT-004 / SSO US-AUTH-012/016 + 5 [b] TCs), P6 deferred FE (ISSUE-271/272/267 workflow-viewer + eligible-plans UI).

| Item | Priority | Scope (findings/story) | Status | PR | Notes |
|------|----------|------------------------|--------|----|-------|
| P0-1 Reconcile ledgers | P0 | BA/STATUS.md + TEST-STATUS.md drift (ADM-011, TRN-001/002/003 shipped) | MERGED | #253 | done |
| P0-2 Missing TC suites | P0 | TC-TRN-001/002/003, TC-ADM-011-*, US-NTF-006 delivery + ISSUE-273 | TODO | — | qa-engineer |
| P1-0 Re-verify body-conflicts | P1 | BUG-003(CRIT)/086/002/005 | VERIFIED | — | all 4 statically LIKELY-FIXED (code-grounded); BUG-003 family formal closure needs a live `/verify-fix --iso` re-run (park as verify task); 086/002/005 fixed at code layer, stale "STILL PRESENT" wording |
| P1-1 RBAC payroll lockouts | P1 | BUG-060(Payroll)/071/077 | PR#254 | #254 | seed fix; 42/42 unit green; merging |
| P1-2 OT overpay | P1 | BUG-078 (OT base EARNINGS→BASIC) + **BUG-280** (same defect in statutory EPF/ETF, auto-healed) | PR#255 | #255 | Code-based BASIC resolution; 10 unit + 29 payroll integ green on Postgres; merging |
| P1-3 Payroll audit emitters | P1 | **BUG-080** (7 payroll audit actions) | PR#256 | #256 | 7 emitters + 8 authentic tests; BUG-084 stale→RESOLVED (BUG-241); BUG-082→P3-5; ISSUE-282(P7); merging |
| P3-5 Audit-all | P3 | **BUG-281** (write-time PII redaction) → **BUG-082** (opt-out audit-all) | MERGED | #269, #272 | BOTH DONE. BUG-281 (#269) write-time mask; **BUG-082 (#272)** interceptor flipped opt-in→opt-OUT (47 `IAuditExempt` = explicit-writer + high-volume; ~50 business entities now audited) + fixed a latent `ActorEmployeeNo` varchar(50) overflow; 3638/3638; auditors WIRED+AUTHENTIC |
| P1-4a Attendance N+1 | P1 | **BUG-125** + **BUG-283** (shift-resolution N+1, shared resolver) | PR#257 | #257 | ~15k round-trips→3; no migration; 40/40 green; merging |
| P1-4b Leave report N+1 | P1 | BUG-124 (batch entitlement resolution) | PR#258 | #258 | ~325k round-trips→2; no migration; 89/89 green; merging |
| P1-4c Dashboard scale | P1 | BUG-123 (hot-path projection) | PR#259 | #259 | attendance-today/live-board projection; folds ISSUE-284#2; 56/56 green; remainder→ISSUE-285; merging |
| P4-perf Dashboard SLA remainder | P4 | **ISSUE-285** (split from BUG-123) | ✅ DONE | #390, #415 | birthday-index MIGRATION **shipped #390** (`Employee.BirthMonthDay` column+index+interceptor+backfill); **DF-50** widget parallelism (child-scope-per-widget runner + warmup) **shipped #415**; **DF-51** k6 p95/50k ✅ **VALIDATED** (p95 192.9ms) |
| P1-5a FE session restore | P1 | BUG-097 (silent refresh on bootstrap) | PR#260 | #260 | FE-only; chained APP_INITIALIZER; 17/17 auth spec green; merging |
| P1-5b Custom-fields crash | P1 | BUG-100 | RESOLVED | 46d7ebb2 | stale — already fixed (shape-drift); closed |
| P1-5c Perf FE routes | P1 | BUG-243 (mostly FE re-model) + saveGoals BE gap | MERGED | #263 | 6 FE services re-modeled (cycleId via cycles/active resolver) + bulk SaveGoals BE endpoint (8 tests); FE 3759 green, BE 3545 (only 2 pre-existing ISSUE-287 reds); auto-healed ISSUE-287/288/289 |
| P1-6 Restore green BE gate | P1 | **ISSUE-287** (entitlement mock not stubbed for BUG-124 batch resolver → 2 red HrLeaveAttendanceReport tests) | MERGED | #264 | stubbed the batch resolver on the mock; class 7/7 green; BE gate green again |
| P5-5 Employee self sign-off | P5→P1 | **ISSUE-288** (HIGH — caller-scoped BE self endpoint for employee acknowledge/dispute) | MERGED | #267 | 3 `reviews/cycles/active/me/*` self endpoints (Read.Self, resolve caller employee + active cycle) + FE rewire; BE 3596/3596, FE 3763 green; auditors WIRED+AUTHENTIC; unblocked US-PRF-006 AC-3 |
| P1-5d Employee↔Location | P1 | BUG-113 (full-stack) | PR#261 | #261 | BE+FE; profile-DTO prefill healed inline; count 0→1; 473 BE + 105 FE green; ISSUE-286 parked; merging |
| P2-1 Notification delivery | P2/P3 | US-NTF-006 — **CORRECTED SCOPE (2026-07-12 audit):** delivery infra + all 8 DI-level module seams are ALREADY `Real*`/dispatcher-wired (Phases 2a–5b shipped). NOT "13 LogOnly seams." The genuine remainder = 11 **inline deferred-notify sites** in modules that never got a `Real*NotificationService` | WIP | — | split into Phase 6 (attendance-family) + Phase 7 (core-hr + 2 jobs) below |
| P2-1a NTF Phase 6 attendance-family | P2 | Attendance/Overtime/Regularization notify-halves (8 sites: AttendanceService:210/504, OvertimeService:140/234/373/425, RegularizationApprovalService:193/345) → new `IAttendanceNotificationService`/`RealAttendanceNotificationService` + 8 `AttendanceAlerts` catalog events | WIP | — | delegating backend-dev |
| P2-1b NTF Phase 7 core-hr + jobs | P2 | EmployeeStatusService:368 (probation-end HR) + :457 (reassignment alert); DocumentExpiryNotificationJob:74; ScheduledReportJob:96 | MERGED | #266 | new ICoreHrNotificationService + 4 catalog events; cross-tenant per-item TenantId; 3590/3590 green; auditors WIRED+AUTHENTIC |
| P2-1c NTF Phase 8 tail | P2 | **LeaveReportExportJob:70** + **BulkEmployeeImportService:316** | MERGED | #268 | `requestedByUserId` threaded through `ILeaveReportExportJob`; BulkImport→`InitiatedBy` email; 2 catalog events (`leave_report_ready`/`bulk_import_completed`); BE 3609/3609 + new leave-side dispatch test (InternalsVisibleTo HRM.Api→HRM.Tests); auditors WIRED+AUTHENTIC |
| P2-1d NTF export-ready tail | P2 | **AttendanceSummaryExportJob** (US-ATT-010 FR-7 "download link sent via notification" still DEFERRED) — 3rd export-ready notify gap, same Site-B pattern (thread requestedByUserId + dispatch a report-ready event) | TODO | — | auto-healed from Phase 8 backend-dev OUT-OF-LANE; then all export-ready notifies done |
| P2-2a RBAC-scope | P2 | ISSUE-195 (HR-report manager-scope) + BUG-120 (Directory authz — dup of ISSUE-018, already fixed #147) | MERGED | #271 | 6 HR builders scoped via shared resolver + cache-key folds scope + cache-leak test; 3635/3635; auditors WIRED+AUTHENTIC |
| P2-2 sec ISSUE-226 XSS | P2 | ISSUE-226 (offer free-text stored-XSS defense-in-depth) | MERGED | #275 | sanitize-on-write via existing `IHtmlSanitizer` (Ganss.Xss); ticket PDF-premise corrected (QuestPDF `.Text()` = non-HTML sink); 2 auditor arms added (integration DI+MediatR + `javascript:`/style pin); 3671/3671; auditors CONNECTED+AUTHENTIC |
| P2-2 MED clusters (rest) | P2 | audit gaps; payroll semantics; a11y; UTC; Redis; recruitment/perf | TODO | — | fan-out one sub-cluster per branch; ISSUE-226 done; BUG-082 broaden-audit now unblocked |
| TAX-1 Multi-country foundation | P2 | employee→country resolution + country-aware resolver + per-country uniqueness/clone + strip+flag semantics (re-arch of 166/167/177 base) | MERGED | #290 | `Location.CountryCode`+`Tenant.DefaultCountryCode`; resolver country-filter (fixes multi-country collision); 5-col unique index (versioning-safe); single-country backward-compat fallback (prevents silent under-tax); resolved-but-no-rules + ambiguous → strip+flag; 409-race backstop. InMemory + Testcontainers index tests; **3776/3776** green; enforcer CONNECTED + authenticator AUTHENTIC (both flags closed in-PR) |
| TAX-2 Country-aware exemptions | P2 | `StatutoryExemption` child entity → `StatutoryCalculator.ComputeTaxableIncome` | MERGED | #291 | FlatAmount/PercentOfGross/PercentOfComponent + cap + IsAnnual(÷12); resolved via shared resolver (preview==run); reduces tax base only (not a deduction line); run-path ComponentAmountsById populated; migration + dormant RLS policy; FluentValidation + service validation; CloneFiscalYear clones. 3795 green; enforcer CONNECTED + authenticator AUTHENTIC 30/30. Follow-up: preview PercentOfComponent shows 0 (needs component-amount preview input) → P7 |
| TAX-3 YTD-cumulative | P2 | per-country `IsCumulative` flag; annual bands + tax(YTD)−withheldYTD; persist TaxableIncome+IncomeTaxWithheld on PayrollSlip | MERGED | #292 | default-false (existing tenants unchanged); `ComputeIncomeTaxYtd`; batch prior-slip ordinal window (FY-boundary-safe); discriminator refined (Lines.Count→FiscalYear, TAX-1 guarantee intact); FY-boundary-reset + cumulative-post-exemption + ordinal-Postgres arms. 3809 green; enforcer CONNECTED + authenticator AUTHENTIC. Follow-ups (LOW): cumulative preview, CountryCode normalization guard |
| TAX-4 Year-end statement | P2 | US-PAY-009 via `PayrollReportExport` async pipeline (replaces the `deferred:true` stub) | MERGED | #293 | real per-employee/country-FY summary of TaxableIncome+IncomeTaxWithheld; reuses async pipeline (no new entity/job/migration); reconciles with the run (same TAX-1 country+FY resolution, finalized slips in FY window); renders CSV/XLSX/PDF; SKIPs no-country employees. 3817 green; enforcer CONNECTED (reconciliation proven) + authenticator arms strengthened. **★ TAX FRAMEWORK COMPLETE (TAX-1/2/3/4).** Follow-ups (LOW): Dec-anchor, per-employee-PDF-ZIP, run-through reconciliation arm |
| DEC-1 Reports.View.* perm | P2 | ISSUE-195 follow-up — dedicated `Reports.View.All`/`Reports.View.Team` (user DECIDED: build both, wire Team) | MERGED | #277 | self-describing taxonomy; both resolvers gate All→`Reports.View.All` + Team→`Reports.View.Team`+direct-report (was data-derived); behavior-preserving seed (add-only reconcile backfills built-ins), Recruiter over-grant corrected; FE catalog + perf-gate cleanup; BE 3685/3685 + FE 3763/3763; auditors CONNECTED+AUTHENTIC. Auto-healed **ISSUE-290** (perf-gate dead `Performance.Read.*` strings) + **ISSUE-291** (custom-role team-scope drop — release-note decision) |
| DEC-2 Key-rotation SOP | P3 | P3-4 follow-up — encryption key rotation/retention runbook (user DECIDED: write now) | MERGED | #276 | doc-only; runbook at `src/backend/HRM.Infrastructure/Security/README.md` (overlap rotation + re-encrypt-backlog + verify-SQL). Surfaced a follow-up: no bulk re-encrypt job exists yet (needed to fully rotate OFF a key) → P7 |
| P2-3 Red FE base | P2 | ISSUE-245 (~26 pre-existing Angular spec fails) | VERIFIED | — | RESOLVED 2026-07-12 no-code — cleared by #254–261; full Karma suite 3757/3757 green ×2 runs, deterministic order; FE gate trustworthy |
| P3-1 ClamAV | P3 | AllowWithLogVirusScanner → ClamAvVirusScanner (deploy-gate) | MERGED | #294 | scanner code + fail-closed gate ALREADY existed (ISSUE-101, unit-tested); armed the env-var deploy-gate (blank `VirusScanning:ClamAv:Host` → stub in dev/test/CI; prod sets it) + `clamav` compose service behind opt-in `scanning` profile + runbook `VIRUS-SCANNING.md`. User DECIDED: env-var gate, dev/local off, separate clamd container in staging/prod. No src/behavior change |
| P3-2 JWT denylist | P3 | NoOpSessionRevoker → Redis denylist | MERGED | #270 | Redis "revoked-before" cutoff + OnTokenValidated fail-open + iat claim; DI-gated Redis/NoOp; 3630/3630; auditors WIRED+AUTHENTIC. Impersonation-end already covered by ImpersonationEnforcementMiddleware (not the per-user cutoff — would over-revoke) |
| P3-3 Permission cache→Redis | P3 | InMemoryPermissionCache (NFR-2) | MERGED | #274 | `RedisPermissionCache` over the shared multiplexer (P3-2 pattern); tenant-version-marker invalidation (INCR, no SCAN); 15-min TTL; FAIL-OPEN (Redis blip → cache miss → DB resolve, never denies); DI-gated Redis/InMemory; 3670/3670; auditors WIRED+AUTHENTIC. All perm-mutation paths already per-user-invalidate. Minor uncovered (LOW): version-key 30d TTL-refresh + malformed-JSON→miss (shares the tested catch). |
| P3-4 PII at rest | P3 | ISSUE-134 (PIP) + ISSUE-150 (Recommendation comp) | MERGED | #273 | **app-side AES-256-GCM** (user decision, not pgcrypto) via EF value converters; 8 fields (Pip 3 + Recommendation comp 5); config key-ring rotation-ready; `numeric→text` migration + idempotent DbInitializer back-fill; 23/23 Postgres + full suite green; auditors WIRED+AUTHENTIC. **⚠ DEPLOY-GATE (ops):** prod/staging MUST set `Encryption__Keys__hrm-field-key-1` (base64 32-byte) via env/secret or the app fail-fasts (dev+tests carry a key). Rotation SOP = a follow-up needs-decision. Budget pool amounts intentionally NOT encrypted. |
| P4-1 RLS code finalize | P4 | US-PLT-002 code parts (prod flip = ops) | TODO | — | ISSUE-269 long-tx tail |
| P5-1 US-ADM-012 | P5 | plan/module governance enforcement | TODO | — | net-new |
| P5-2 US-PRF-011 | P5 | calibration workspace | TODO | — | net-new |
| P5-3 US-PLT-004 | P5 | observability NFRs | TODO | — | net-new |
| P5-4 SSO | P5 | US-AUTH-012/016 + 5 [b] SSO TCs | TODO | — | net-new; some ops-gated |
| P6-1 Workflow viewer FE | P6 | ISSUE-272/267 (US-ADM-011 FR-12 UI) | TODO | — | BE-complete |
| P6-2 Eligible-plans UI | P6 | ISSUE-271 (US-TRN-003 AC-8) | TODO | — | BE-complete |
| **CAL-1 Calendar foundation** | **P1** | `Location.DefaultShiftId` + four-tier `ShiftScheduleResolver` + Location CRUD (US-ATT-011 **AC-1 + AC-2**; TC-ATT-145/146/147/148/149 + TC-ATT-ISO-014) | **MERGED** | #310 | gates CAL-2..8. Mon–Fri code-default ADOPTED — ⚠ **NOT a no-op** (that claim was falsified; see 🔑 + **BUG-287**); mandatory no-default-shift regression arm. Resolver stays `internal static` (plan's `new ShiftScheduleResolver(ctx)` sketch does not compile) |
| **BUG-287** shift-delete guard | **P2** | `ShiftService.DeleteAsync` guards only `EmployeeShift` refs → deleting the **tenant default** OR a shift wired as a **`Location.DefaultShiftId`** silently changes calendars/pro-rata. FK `Restrict` can't help (delete is a SOFT delete = UPDATE) | TODO | — | auto-healed from CAL-1 pinning, **extended by both CAL-1 auditors**; CAL-1's code default only degrades the tenant-default case (7-day → Mon–Fri). Fix = add `IsDefault` + `Locations.AnyAsync(l => l.DefaultShiftId == id)` arms to the existing 409 `shift_in_use` guard |
| ISSUE-306 resolver `IsActive` asymmetry | P2 | write-time refuses an inactive `DefaultShiftId`, read-time resolver has no `IsActive` filter → a shift deactivated after wiring keeps driving calendars | TODO | — | **needs-decision** (fall-through vs keep-resolving = opposite money outcomes). Currently **latent** — no path sets `IsActive=false` yet → cheap to decide before a deactivate endpoint lands |
| ISSUE-307 Flexible shift, no `WorkingDays` | P2 | `ShiftRequestValidator` only requires `WorkingDays` for Single/Rotating → a Flexible shift persists with no calendar and can be wired to a Location | TODO | — | **contained** by CAL-1 (`ToCalendar` → empty = unresolved → falls through, never a 7-day week; 2 arms). Residual: still creatable + silently no-ops for the admin. Owned by US-ATT-005 |
| **CAL-2 Leave day-count** | **P1** | **BUG-284 (HIGH)** — `LeaveRequestService.cs:132/:142/:353` via resolver (US-LV-003; TC-LV-262/263) | **MERGED** | #311 | ✅ BUG-284 RESOLVED. All 3 sites resolve the four-tier week; preview==deduction. ⚠ ISO(1..7)↔`DayOfWeek`(0..6) bridge `(DayOfWeek)(iso % 7)` + OPPOSITE empty-set conventions (resolver empty=all days, calculator empty=Mon–Fri) → `Count > 0` guard. 11 Postgres arms; revert→4 red / 3 controls green; mutation-tested (tier-3 delete → red, ISO-6 mismap → red ×2). Fixed a late-Dec ledger-year flake |
| **CAL-3 OT weekend + holiday** | **P1** | **BUG-285 (HIGH)** `OvertimeMultiplierResolver` + **BUG-286 (MED)** `OvertimeService.IsPublicHolidayAsync` (US-ATT-006; TC-ATT-153/154) | **MERGED** | #312 | ✅ **BOTH RESOLVED — the HIGH band is now EMPTY (0 open HIGH).** Weekend basis = "not a working day for THIS employee" via the four-tier set (Domain fn stays pure — caller resolves + passes, as it already did for `isPublicHoliday`); holiday lookup routed onto the location-aware `IHolidayProvider` (trailing-optional ctor param; DI always supplies). Both OT paths (auto-detect + pre-approval) quote the same rate. 8 unit + 7 Postgres arms; mutation-tested ×3 (re-hardcode Sat/Sun → 3 red; drop `locationId` → 2 red; drop work-week → 2 red); tenant-wide-holiday + Mon–Fri controls stay green |
| **CAL-4a** Location attendance policy (read layer) | P2 | task 2.4 — `AttendanceSettings.LocationId` + `AttendancePolicyResolver` + repoint every read site (US-ATT-011 **AC-3**; TC-ATT-150/151 + TC-ATT-ISO-015) | **MERGED** | #313 | ⚠ **The plan missed the real risk.** The module rested on an "exactly one row per tenant" invariant (unique index on **TenantId ALONE**) that **7** sites relied on via **unpredicated `FirstOrDefaultAsync()`**. This change breaks it → any surviving unpredicated read returns an ARBITRARY row, applying one branch's geofence/OT policy tenant-wide. All 7 repointed — I found 6 by inspection; my sweep + the auditor **independently** found the 7th (`LeaveReportService.ResolveAbsenteeismThresholdAsync`, a Leave-module scalar `.Select().FirstOrDefaultAsync()` no attendance-shaped grep matches). Row-level resolution per AC-3; regularization resolves for the **subject**, not the approving manager. **+ partial `TenantId` unique WHERE `location_id IS NULL`** (Postgres treats NULLs as distinct → `(tenant,location)` alone would permit two tenant-default rows). Tenant-wide sweeps keep today's behaviour explicitly → **ISSUE-309**. 6 Postgres arms + 1 InMemory regression; mutation-tested (unpredicated read → 4 red; my first absenteeism arm was theater until I inverted the seed order). Auto-healed **ISSUE-308** (lazy-create 23505 race, pre-existing) |
| **CAL-4b** AttendanceSettings CRUD API | P2 | the AC-3 admin half — tenant + per-location settings CRUD (US-ATT-011 **AC-3**; TC-ATT-155/156 + TC-ATT-ISO-016) | **MERGED** | #314 | **`AttendanceSettings` had NO write path at all** — 24 policy fields lazily created at C# defaults, never editable — so AC-3's admin half was unbuildable. 6 endpoints on the existing `AttendanceController`, gated `Attendance.ConfigurePolicy` (already in the catalog + already granted to TenantAdmin/HR → reachable, no ISSUE-210 trap). Row-level, full-replace PUT; soft DELETE; 23505→409; `invalid_location` via the tenant filter (no `IgnoreQueryFilters`). **All 6 queries predicated on `LocationId`** — no 8th unpredicated read. `AttendanceSettings` is NOT `IAuditExempt`, so the BUG-082 opt-OUT interceptor already audits these writes (no hand-rolled rows). 8 Postgres arms; the agent self-mutation-tested (unpredicated read → only the invariant arm reddens, confirming seed-override-first is load-bearing). ⚠ **I caught a TC-ID COLLISION**: the agent tagged its arms `TC-ATT-152`, which already exists as FteScaledOvertimeBase/AC-5 (CAL-6) — retagged to TC-ATT-155/156 + ISO-016 and authored all three TCs (the arc-f traceability lesson). Also fixed **my own** CAL-3 xUnit1026 (2 Theories declared `expectedBasis` and never asserted it — the basis is unit-covered; the DTO exposes only `Multiplier`). Auto-healed **ISSUE-310** (full-replace PUT on a 24-field payload incl. pay rules — BUG-117 class, worse instance) |
| **CAL-5** Payroll denominator | P2 | task 2.5 — effective-dated holiday-exclusion → working-days count (US-ATT-011 **AC-4/FR-5**; TC-PAY-014/015) | **MERGED** | #315 | ⚠ **The plan was wrong twice.** (1) It said `bool …= true` on `AttendanceSettings`; default-ON would have raised the OT base AND the LOP daily rate for **every existing tenant next run** → built as **default-FALSE + effective-dated `TenantPayrollCalendarPolicy`** (TenantFnFPolicy pattern; new table ⇒ dormant RLS policy; + CRUD, per the CAL-4b lesson). (2) "Subtract holidays from the denominator" **is a money bug**: `PayrollRunProcessor` documents that pro-ration must be **single-basis**, so holidays are excluded from the **numerator (`ProRataPaidDays`) AND denominator** alike — denominator-only over-pays joiners/leavers. Honest effect: OT base rises **and LOP deducts MORE**. Location-scoped holidays batched once per distinct location. 3946 green; 9 Postgres arms. **Mutation-tested (the agent reported none):** numerator-drop → single-basis arm RED; code-default flip → control arm RED. ⚠ **the ENTITY initializer default is NOT the guarantee** — the resolver's no-row fallback is (mutating the entity alone leaves every arm green). **Fixed 4 defects in the delegated work** (report truncated before it ran its own suite): `IHolidayProvider` made REQUIRED on 2 services → **72 tests red** (→ trailing-optional + null-guard); LOP arm red (fixture had no attendance data → ISSUE-090 omitted the employee; product right, fixture wrong); location arm red (no salary components + `SingleAsync` on a 2-employee run). **Docs amended:** AC-4/FR-5/BR-6 said default-ON → corrected + BR-9/10/11 added; TC-PAY-014 "(default)"→"(opt-in)"; TC-PAY-015 reframed as the no-regression guarantee |
| **CAL-6** Employee FTE / arrangement | P2 | Phase 3 — `Employee.Fte` + proration · `FteScaledOvertimeBase` (default off) · `WorkArrangement` + Remote geofence exemption (US-CHR-013; TC-CHR-326/327/328/329 + TC-ATT-152) | **MERGED** | #316 | **Closes US-LV-002 AC-K1** — the engine ALWAYS took `fte`; all 3 callers hardcoded `1.0m`. All defaults are the no-regression contract (Fte 1.00 · flag OFF · OnSite), each with a control arm. Geofence exemption is the **radius ONLY** (RequireGeolocation/IP/photo still apply) and **Hybrid is NOT exempt**. `FteScaledOvertimeBase` added to CAL-4b's full-replace DTO or the PUT would have silently reset it (ISSUE-310). ⚠ **The delegated build was cut off**: no migration, **3.3 not implemented at all** (enum+column+DTO but `AttendanceService` never read them — dead code), zero tests. I generated the migration, built the bypass, and wrote all **45 arms**. 3991 green. **Mutation-tested — ⚠ the `FteScaledOvertimeBase` ENTITY INITIALIZER flip SURVIVED all 22 arms** (every OT arm passes the flag explicitly; EF still sends the initializer on INSERT → a lazily-created row would scale every part-timer's OT base). Closed with `LazyCreatedTenantDefault_HasFteScaledOvertimeBaseOff`. Same class as CAL-5's resolver-fallback-vs-entity-default. Also: **one of my own mutations was a NO-OP** (replace matched nothing, reported success) — a surviving mutant is worthless until you prove it was applied. I also mis-tagged the geofence arms as TC-CHR-327 (= FTE validation) → retagged 328 + wrote the real 327/329. **FE deferred (P6)**; API carries both fields. Not automated: TC-CHR-ISO-049 |
| CAL-7 Configurable probation | P3 | **ISSUE-304** — `Tenant.ProbationPeriodDays` + `Location.ProbationPeriodDays?` (US-CHR-009 BR-6) | ✅ **MERGED** | #317 | `Location ?? Tenant ?? 90`, resolved in 3 batched queries (was per-employee). `Location.ProbationPeriodDays` is **nullable so a Location can stay SILENT** — a non-null default would make every Location an override and the tenant setting would never apply. CS8714 shipped hidden by an **incremental build**; `--no-incremental` exposed it → `NoLocation = Guid.Empty` sentinel. |
| CAL-8 Fiscal leave year | P3 | **ISSUE-305** — `FiscalYearStartMonth` → `LeaveYear` (US-LV-002/006/008) | ✅ **RESOLVED** #318 | 2026-07-16 | Was **4** named sites; the sweep found **7** in the engine/jobs. `LeaveYear` (BoundsFor/LabelFor/StartOf) wired through all 7; month resolved once per sweep (N+1 guard); year LABEL unchanged → no ledger renumbering. **The cron was the un-fixable half:** `ProcessLeaveYearEndJob` ran `"0 2 1 1 *"` (1 Jan only) — no in-job constant could make a 1-Jan cron fire on 1 Apr → now daily + per-tenant `ClosingLeaveYearIfDue` window (7-day grace; idempotent per NFR-3). 57 arms, **7/7 mutants killed**; 2 survivors proved equivalent → deleted the dead branch they exposed. ⚠ **ISSUE-176 NOT superseded** (it's Payroll — the old claim was wrong; stays OPEN). Read layer deferred → **ISSUE-311**. `LeaveEntitlementRule.LocationId` (task 4.3) NOT done → still open. |
| FE-EXTRACT template/SCSS | P6 | 210 components · ~300 extractions · ~13 PRs ([`DEV/TEMPLATE-EXTRACTION-PLAN.md`](../../DEV/TEMPLATE-EXTRACTION-PLAN.md)) | **DEFERRED** | #308 (plan only) | user-deferred 07-15: collides with CAL-6 FE work + a live concurrent session; needs a clean tree on base |
| P7-1 LOW tail | **P7 (DEMOTED 07-15)** | 156 LOW + 20 ENH, batch by module | TODO | — | ISSUE-270/274/**280**/**282** (auto-healed: PayrollSlipLine Code; job-path audit Postgres arm), ChangeTracker.Clear tidy, **`OfferLetterTemplate.Substitute` raw `string.Replace`** (auto-healed from #275 — latent XSS foot-gun IF a future HTML/email offer template reuses it; harmless today since the renderer ignores it, values now sanitized on write), **bulk field-encryption re-encrypt job** (auto-healed from #276 — none exists; needed to fully rotate OFF an encryption key per the SOP; low-urgency until first real rotation), **`Performance.Read.*` docstring sweep** (auto-healed from #278 — 6 sibling perf files still name the dead `Performance.Read.*` perms in comments; comment-only, prevents the next copy-paste-into-a-live-gate that caused ISSUE-290), **ISSUE-292** (onboarding audit semantic action naming — auto-healed from #280, LOW ENH), **audited-list-read decision** + **one real-Postgres read-audit persistence arm** (auto-healed from #280 test-authenticator — BUG-083 read-audit is InMemory-only; low-risk plain INSERT), **exact-statutory in negative-net advisory** + **`BulkCreateAsync` negative-net advisory gap** (auto-healed from #281 — the advisory baseline omits statutory/EPF/ETF so it under-warns; bulk-create path skips the advisory entirely; both LOW), **payslip-snapshot PDF-regenerate + list-DTO test arms** (auto-healed from #286 — those snapshot-preference branches are wired but untested), **ISSUE-154 follow-ups** (auto-healed from #287 — FE 409 double-toast via shared error-interceptor; deferred Hangfire job-deletion on cancel; Postgres cancel-frees-period index test; TC-PAY-003-02 QA doc), **ISSUE-178 PR2 follow-ups** (auto-healed from #289 — FE async-export UI; Postgres cross-tenant-download isolation arm; masking-regression fake; `LocalReportExportStorage` path parameterization; dead `IPayrollReportExportJob` binding parity), **TAX-2 preview PercentOfComponent divergence** (auto-healed from #291 — the FR-5 test-calc hardcodes `ComponentAmountsById:null`, so a percent-of-component exemption previews as 0 while the run applies it; the run is correct, only the what-if under-shows component-based reliefs; fix = add a per-component-amount input to `TestStatutoryCalculationQuery` + a preview==run arm for that type; LOW), **TAX-3 cumulative-preview** (auto-healed from #292 — the FR-5 test-calc has no prior-YTD input, so a preview for a cumulative rule shows the FIRST-month figure not the true YTD delta; needs a prior-YTD preview input; LOW), **TAX-3 CountryCode normalization guard** (auto-healed from #292 enforcer — the processor↔resolver two-resolution convergence depends on stored `CountryCode` being normalized, which the SERVICE does on every write; a raw/seed/import write bypassing the service could store an un-normalized code → processor threads prior-YTD but resolver silently uses the monthly branch; add a CHECK/normalization guard OR normalize the resolver comparison; LOW, pre-existing assumption), **TAX-4 follow-ups** (auto-healed from #293 — (a) year-end statement is Dec-anchored: `qp.PayMonth ?? 12` picks the FY containing December; the FiscalYear column self-labels each row so no mislabel, but confirm the FE passes explicit PayMonth/PayYear for a non-Dec-anchored FY; (b) per-employee-PDF + bulk-ZIP @5,000-scale is separate infra the columnar report doesn't build; (c) a run-through reconciliation test arm — run real payroll → assert statement withheld == slips' persisted IncomeTaxWithheld — currently covered by enforcer code-proof + TAX-3 persistence tests; (d) mid-FY IncomeTax rule re-versioning drops earlier-in-FY slips from BOTH run YTD and report symmetrically — decide whether a mid-FY amendment keeps the FY's original start; LOW) |
| — ISSUE-021 / BUG-056 | park | SalaryGrade entity / goal-finalize seam | PARKED | — | decision-gated |

## ▶ Remaining work — priority order

> ⚠ **STALE (2026-07-15 snapshot — predates the #352–#382 campaign).** The P0–P7 breakdown below was groomed BEFORE the arc-k/arc-l "fix all bugs+issues+decisions" campaign, which cleared the MED/LOW bug/issue backlog + every product/BA decision (see the two 2026-07-18/19 changelog entries above). **The LIVE queue is now `docs/QA/DEFERRED-FOLLOWUPS.md`** (per-item, with recommendations); `docs/QA/TEST-FINDINGS.md` holds current finding statuses. Treat the sections below as historical context, not the active plan.

### P0 — Ledger reconciliation + missing test suites (cheap, unblocks accurate planning)
- **Reconcile stale trackers:** `docs/BA/STATUS.md` still marks US-ADM-011, US-TRN-EPIC/001/002/003, US-NTF-006 as
  `[ ]` though they shipped this arc; the `TEST-STATUS.md` tally block (ln 149-157) is stale vs its own per-module rows.
  Flip them to reflect merged state.
- **Author the missing test-case suites (shipped-but-untested-in-the-ledger — ZERO coverage today):**
  `docs/QA/training-benefits/TC-TRN-001/002/003`, `TC-ADM-011-*` (workflow runtime), and US-NTF-006 delivery. Add
  `TEST-STATUS`/`TRACEABILITY` rows. Also **ISSUE-273** (additive test-hardening arms across US-ADM-011 + T&B).

### P1 — HIGH findings (14) — the real defects (triage-verify, then fix by cluster)
- **RBAC persona lockouts (Payroll):** BUG-060 (ln 2552, HR Officer can't configure salary components), BUG-071
  (ln 3315, HR Officer lacks `Payroll.Run`), BUG-077 (ln 3746, HR Officer 403 on all Payroll reports). One role-bundle
  fix likely clears the cluster.
- **Payroll correctness:** BUG-078 (ln 3810) — overtime hourly base uses total EARNINGS not BASIC → OT **overpaid ~2.5×**.
- **Audit completeness:** BUG-080 (ln 3938, 8 payroll audit actions never emitted), BUG-082 (ln 4176, "audit ALL changes"
  false — interceptor covers 3 entity types), BUG-084 (ln 4241, audit keyword search 500s on jsonb `~~`).
- **Scale / SLA:** BUG-123 (ln 5388, dashboard p95 829ms + 60s timeouts @50k), BUG-124 (ln 5427, Leave reports N+1 timeout
  @5k), BUG-125 (ln 5440, Attendance `payroll-data` P95 ~28s @5k vs ≤5s SLA).
- **FE contract / session:** BUG-097 (ln 4778, reload/deep-link logs user out — no silent refresh-cookie restore),
  BUG-100 (ln 4838, Custom Fields page render crash + Add modal throws), BUG-113 (ln 5133, Employee↔Location FK
  unreachable → counts always 0), **BUG-243 (ln 5660, ~9/10 Performance Angular services hit 404 — broad FE↔BE contract
  break)**.

### P2 — MED findings (78) — themed clusters (representative IDs; full list at line anchors)
- **Notification DELIVERY (the biggest theme, ties to P3):** ISSUE-221 (ln 5226, only the template "test email" uses real
  SMTP — all module transactional email is LogOnly), ISSUE-228 (ln 5401, SignalR live but every in-app producer is
  log-only/unreachable), ISSUE-214 (ln 4952, NTF-002/003 pages orphaned from nav). **US-NTF-001/002 delivery.**
- **RBAC / team-scope:** ISSUE-195 (ln 4341, manager `Reports.View.Team` unimplemented → sees full tenant), BUG-120
  (ln 5303, Directory gated ViewOwn only → Manager/HR/Admin 403).
- **Audit gaps:** BUG-081 (4085), BUG-083 (4189, PII-read audit unimplemented), BUG-085 (4254), ISSUE-120 (2854),
  ISSUE-200 (4432, zero onboarding audit), ISSUE-108-PAY (2590).
- **Payroll semantics:** ISSUE-153/154 (3324/3333, idempotency-replay + no cancel/re-run), 156/157 (pro-ration),
  165/166/167/170 (point-in-time payslip, std-deduction/80C, YTD tax, finalized-retro-edit guard), 177/178 (year-end
  statement + report filters), 180 + BUG-079 (encashment), BUG-061/062/074.
- **Scale / N+1:** BUG-095 (4682, export file collision @concurrency), ISSUE-203 (4695, login p95 4.8× SLA — BCrypt),
  ISSUE-201 (4539, negative page → 500), ISSUE-230 (5453).
- **Security:** ISSUE-226 (5344, offer stored-XSS), ISSUE-134 (3060, PIP fields plaintext), ISSUE-165-class PII.
- **a11y:** BUG-108/109/110/112 (5063-5121). **UTC-boundary:** BUG-245/246 (5703/5710). **Redis:** BUG-115 (5174,
  outage ~11s stall), BUG-116 (5187, my-tenants cache no invalidation).
- **Recruitment/Perf gaps:** ISSUE-133/137/140-PRF/141/145, ISSUE-140-REC (3185, REC-010 auto-user/welcome/onboarding
  stubs), BUG-064/065/066 (scorecard/sign-off/offer BR bypasses), ISSUE-232 (3193).
- **⚠ ISSUE-245 (ln 5685) — the Angular unit-test suite is RED on base (~26 pre-existing spec failures).** Fix this
  early: a red FE baseline undermines every FE gate.

### P3 — Architectural / infra debt (shipped-but-incomplete live paths — from the code survey)
- **★ Notification delivery rewire (biggest surface):** `RealNotificationDispatcher` infra landed but **12 `LogOnly*`
  module seams are not switched onto it** (Leave/Payroll/Performance/Recruitment/Payslip-email/TenantWelcome/
  Lifecycle/UserMgmt/F&F/Recommendation), plus deferred notify-halves in Attendance/Overtime/Regularization and ~13
  jobs that only log. This IS ISSUE-221/228. Scope as **US-NTF-001/002 delivery**.
- **★ ClamAV virus scanner** — `AllowWithLogVirusScanner` accepts uploads with only a log on live asset/onboarding/
  employee-document paths. Wire real ClamAV (`TODO(prod)`).
- **★ JWT denylist / session revocation** — `NoOpSessionRevoker`; access tokens can't be revoked (Redis denylist TODO).
- **★ IPermissionCache → Redis** (`InMemoryPermissionCache`, NFR-2) — prod-scale gap.
- **★ Year-End Tax Statement** — `PayrollReportService` live `deferred:true` stub returns a Note (compliance) — US-PAY-009.
- **PII encryption at rest (US-PLT-005, plan-HIGH)** — compensation (Recommendation/Pip/Budget) + PIP reason stored
  plaintext; no pgcrypto.
- **PDF export** deferred across Performance/Recruitment dashboards (CSV/XLSX shipped). **Observability** per-tenant
  enrichment/custom meters (US-PLT-004). Redis eviction seams (TenantSettings/SubscriptionPlan).

### P4 — RLS actual prod flip (GO — ops-gated) + the deferred long-tx tail
- **Ops gates (per environment, user's deploy step)** — README §3b: run `roles.sql`, `hrm_owner` owns schema, repoint
  `DefaultConnection`→`hrm_app` / `PrivilegedConnection`→`hrm_owner` + Hangfire→privileged, coverage-guard green on the
  target Postgres, rollback rehearsed. Greenfield only: `GRANT CREATE ON DATABASE … TO hrm_owner` (ISSUE-278).
- **Deferred code tail (before a HIGH-VOLUME flip):** restructure `DataExportGeneration` + `HrReportExport` to
  GUC-per-short-unit (ISSUE-269 class; low-freq → safe to defer past the initial flip). Finalize **US-PLT-002**.
- Cosmetic: ISSUE-279 (reconciler bypass-warning false-fire), ISSUE-266 (WorkflowService drops ErrorCode), ISSUE-276
  (Redis future-host coupling).

### P5 — Deferred FE (BE-complete this arc)
- **ISSUE-272** [MED] FE workflow-instance detail / step-chain viewer (US-ADM-011 FR-12 UI) + admin instance list;
  tighten **ISSUE-267** (instance-read endpoint is `[Authorize]`-only).
- **ISSUE-271** manager "eligible plans for employee X" UI (US-TRN-003 AC-8; BE API-complete).
- Nav-orphan cleanups: ISSUE-208 (Attendance sub-pages), ISSUE-214 (NTF-002/003).

### P6 — Pending / net-new stories
- **SSO (real gaps):** US-AUTH-012 (per-tenant SSO config), US-AUTH-016 (SSO enforcement / break-glass / admin-consent)
  — also the 5 `[b]` BLOCKED SSO test cases (US-AUTH-011/012/013/014/016).
- US-PRF-011 (calibration workspace), US-ADM-012 (plan/module governance enforcement), US-PLT-004 (observability).

### P7 — LOW tail (152) + small tidies
- Batch-triage the 152 LOW by module (line anchors in the survey). Quick self-contained ones: **ISSUE-270** (Training/
  Benefits notification category), **ISSUE-274** (custom `IEFCacheServiceProvider` for EF-cache Redis spans),
  **DecideCoreAsync `ChangeTracker.Clear()`** tidy on the onApproved-failure path (`WorkflowRuntimeService.cs:309-315`).
- Deferred/feature-blocked (park at decision-gate): ~~ISSUE-021 (no SalaryGrade entity)~~ **DONE #389**, ~~BUG-056 (no goal-finalize seam)~~ **DONE #387**.
  WONTFIX for reference: ISSUE-209/250/265.

## 🔒 Standing rules (carry forward)
NEW-TENANT-TABLE RLS RULE (every new tenant_id table adds its dormant `tenant_isolation` policy in-migration) · **RLS
GUC is set by `TenantGucConnectionInterceptor` on connection open (NOT a request-wide tx)** · cache = read + auto-evict,
tenant-prefixed · RLS config-gated + reversible, committed OFF · BUG-068/252/264 retry-vs-tracked-state (manual tx under
`EnableRetryOnFailure` → wrap in `CreateExecutionStrategy().ExecuteAsync` + detach on rollback) · verify branch before
commit, no `--no-verify` · payroll approval stays on its bespoke `PayrollApprovalService` (converge later = a separate
US-PAY-XXX story) · **the full `dotnet test` gate is now reliable** (xUnit `maxParallelThreads:4`).

## Working method (unchanged)
One `feat/`|`fix/` branch per story/cluster off fresh `test/local-subdomains` → map seams (Explore) → LOCKED spec →
parallel backend-dev + frontend-dev on disjoint paths against a pinned contract → verify FULL suite on Postgres (Docker
up) + read-only auditors (integration-enforcer + test-authenticator) → commit / PR / merge each before the next.
Security/RLS-touching changes: re-run the local RLS-on validation (throwaway Postgres; method in Rls/README §runbook).
Auto-heal out-of-lane flags into this plan + `TEST-FINDINGS.md`.
