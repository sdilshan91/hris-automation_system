# ARCHIVED SNAPSHOT — GAP-PLAN generation of 2026-08-08 (was labelled THE LIVE QUEUE).

> Split out of [`../COMPLETION-PLAN.md`](../COMPLETION-PLAN.md) on **2026-09-01**, when the plan was
> audited and rebuilt. It carried five overlapping sections that each claimed to be 'the queue';
> this is one of them, preserved verbatim as history.
>
> **Not current. Do not execute from this file.** The live execution lane is
> [`../GAP-CLOSURE-QUEUE.md`](../GAP-CLOSURE-QUEUE.md); the current backlog is
> [`../COMPLETION-PLAN.md`](../COMPLETION-PLAN.md).

---

## 🧩 GAP-PLAN (2026-08-08) — THE LIVE QUEUE, from the whole-product requirement trace

> **Source:** [`Architecture/gap-analysis/GAP-REGISTER.md`](../../../Architecture/gap-analysis/GAP-REGISTER.md) + 17 per-pass reports.
> Every row below cites `file:line` evidence in its pass report. **`GAP-###` IDs are stable — never renumber.**
> **This supersedes the P0–P7 block below**, which is a 2026-07-15 snapshot kept as historical context.
>
> **Ordering rule:** tenant isolation first (Critical Rule #1), then money, then compliance, then the two
> structural fixes, then everything the structural fixes make cheap. **Do G1 before G2** — G1 is what stops
> G2 recurring.

### Reality check before scoping anything here
- **Only 2 of 448 Must-Have ACs are MISSING.** Almost nothing is unbuilt. Do not scope this as feature work.
- **Zero orphaned code** — nothing to delete.
- **~70% of all PARTIALs are one defect class** (S-1, the FE↔BE contract). Estimating them individually inflates the plan.
- **36 rows are CONTRADICTED**, most in the *pessimistic* direction. **Reconcile the ledger (G5) early or every estimate built on it is wrong** — the same lesson as 2026-07-30 and 2026-08-04, now measured product-wide.

---

### G0 — P0: isolation, money, compliance (before the next production deploy)

> **Filed to `TEST-FINDINGS.md` 2026-08-08 — these five have finding IDs and are `/fix-finding`-ready:**
> **GAP-001 → [[BUG-297]]** · **GAP-002 (+GAP-017) → [[BUG-298]]** · **GAP-003 → [[BUG-299]]** ·
> **GAP-004 → [[BUG-300]]** · **GAP-005 → [[BUG-301]]**. Everything below G0 has **no finding ID yet** —
> file before starting a `/fix-finding` on it, or the `/verify-fix` close-out has nothing to flip.

| ID | Component | Gap detail (evidence) | Scope required to cover | Size |
|---|---|---|---|---:|
| ~~**GAP-001**~~ ✅ | `TenantResolutionMiddleware` · `AppDbContext` filters · `ConnectionRoutingInterceptor` · `TenantAccessGuardMiddleware` | **An unresolved tenant context disengages ALL FOUR isolation layers at once.** Resolution passes the request through (`:90-93`, `:105-110`); the EF filter is `!IsResolved \|\| …` so it becomes a tautology (`AppDbContext.cs:269-270`); routing sends it to the **BYPASSRLS `hrm_owner`** role (`ConnectionRoutingInterceptor.cs:92-93`); the BUG-003 guard requires `IsResolved` and skips (`:38-42`). **`api` and `app` are both in the shipped reserved-subdomain list** (`:54`). **No US-PLT AC covers this** — the nearest, US-PLT-002 AC-4, is unmet and never negatively tested. *Mitigation: `SystemEndpointHostGuardMiddleware` protects `/api/v1/system/*`; `/api/v1/tenant/*` is unprotected.* | **1.** Run the probe first — authenticate as tenant A, `GET /api/v1/tenant/employees` with `Host: api.<basedomain>`, count rows. **This converts a 60%-confidence risk into a yes/no and must precede the fix.** **2.** Invert the default in `SelectPrivileged()`: privileged **only** when `IsSystemContext` is explicitly true, never merely because nothing resolved. **3.** 400 reserved-subdomain requests to `/api/v1/tenant/*`. **4.** Prove no legitimate path relies on "unresolved ⇒ privileged" — Hangfire, `DbInitializer` seeding, health probes. **5.** Add the missing US-PLT-002 AC + an HTTP-level negative test. **✅ DONE 2026-08-11 — the fourth and deepest link is closed.** `SelectPrivileged()` inverted from `is not { IsResolved: true, IsSystemContext: false }` to **`is { IsSystemContext: true }`**: privilege is now granted *only* to an explicitly-declared system context, never merely because nothing resolved. **The framing that matters: absence is not authority.** "Nothing resolved" and "deliberately cross-tenant" were indistinguishable, and the *more dangerous* reading was the default — so forgetting to scope something handed it the BYPASSRLS role. **Step 4 (the M) found the answer is YES, one legitimate path did depend on it:** startup migrations + seeding run with no ambient and genuinely need privilege (`hrm_app` has DML but **no DDL**), so a naive flip would have stopped the app booting. They now enter an explicit `CrossTenantScope`. **The other two unresolved paths had already been converted by GAP-024 the phase before** — the health probe and the cross-tenant jobs now declare `SetSystemContext()`, which is exactly why 2b was sequenced ahead of 2d rather than beside it. **Checked, not assumed:** `tenants` and `users` carry **no RLS**, so the pre-resolution tenant lookup and login are unaffected; the blast radius is precisely the set of unresolved flows. **New composed test, `UnresolvedContext_RoutedByTheInterceptor_SeesNoRows_GAP001`** — the two halves (routing picks a role · `hrm_app` with no GUC returns nothing) were each already proven and **never composed**, and the composition *is* the claim. It drives the real interceptors against real Postgres; under the old rule the same query returned **every tenant's employees**. Paired with `DeclaredSystemContext_…_StillSpansTenants_GAP001` so "unresolved sees nothing" cannot be satisfied by simply breaking cross-tenant administration. **Two pre-existing tests asserted the OLD behaviour and were inverted, not deleted** — flagged explicitly in-file, since changing a test to match new code is the shape the test-integrity rule exists to catch; here the *specification* changed by design and the replacement assertions are stricter. **Verified live on the rebuilt container, both directions:** the app boots with no permission error, login + tenant queries work, reserved hosts (`api`/`app`/`admin`) still fail closed at **400**, and a platform admin on the admin host still reads **all 3 tenants** through the privileged role. **Mutation-verified with the original expression restored: exactly 4 arms go red, including the real-Postgres one that demonstrates the actual cross-tenant exposure.** Steps 1–4 complete; **step 5's US-PLT-002 AC text is a BA doc edit and remains open.** | **DONE** (code) + AC doc |
| ~~**GAP-002**~~ ✅ | `EntraSsoService` · `Tenant` SSO columns · US-AUTH-012/013/014 | **SSO tenant isolation is appsettings-backed, not DB-backed.** `CheckIsolation` reads `_options.TenantAllowList`; the five DB fields (`AllowedEntraTenantIds`, `AllowedEmailDomains`, `SsoEnabled`, `JitEnabled`, `JitDefaultRole`) have **zero read sites on any login path** — exhaustive grep. `EntraSsoOptions.cs:9-13` admits it is *"the dev-POC home … in the full feature this moves into per-tenant DB config"*. **Consequences:** editing the allow-list in the UI changes nothing; **admin-consent onboarding cannot enable anyone**; `SsoEnabled=false` does **not** block SSO. `STATUS.md:40` declares the BR-5 prod gate satisfied. **It is not.** | **1.** In `CompleteSignInAsync`, replace `CheckIsolation(...)` with an async guard loading the tenant's `SsoSettingsSnapshot` — **the cache already exists** (`AuthService.GetSsoSettingsAsync`). **2.** Evaluate `SsoEnabled` + both allow-lists + JIT gate from it. **3.** Keep appsettings only as an explicit dev-override behind an env check, or delete. **4.** **Correct `STATUS.md:40,43,44` — do not ship SSO to production against the current claim.** **✅ DONE 2026-08-08.** Guard now reads the tenant record through a new `GetSsoSettingsBySubdomainAsync` (reusing the existing snapshot cache, not a second path); `SsoEnabled` gates first; appsettings list is `[Obsolete]` and read by nothing; the two missing audit events are emitted. **Extracted the decision into the pure `SsoIsolationGuard` — it had survived because 80 passing SSO tests reached none of its branches through the full OIDC callback; now 17 direct arms, weighted to refusals.** STATUS.md lines corrected in place, recording that they were false 07-28 → 08-08. | **DONE** |
| ~~**GAP-017**~~ ✅ | `EntraSsoService.GetEmail` | **Unverified email accepted for domain allow-listing** (US-AUTH-013 AC-7, one of only 2 MISSING ACs). `:473-487` returns `email`, else falls back to `preferred_username`; **no `xms_edov`/`email_verified` check anywhere.** | Require `xms_edov == true` (or restrict to tid-match when the claim is absent) before `EmailDomain(email)` can satisfy `domainAllowed`. **Bundle with GAP-002** — same file, same guard. **✅ DONE 2026-08-08** (bundled as advised). A domain match now requires `xms_edov`/`email_verified`; absence means *unknown* and falls back to the tid rule rather than refusing, because tid is bound to the issuing directory and cannot be self-asserted. JIT requires the verified domain rule too. | **DONE** |
| ~~**GAP-003**~~ ✅ | `PayrollSlipCalculator` | **MONEY. LOP under-deducted for every mid-month joiner and leaver.** `:142` computes `proRatedBasic / workingDays`, where `proRatedBasic` was already multiplied by `proRataFactor` at `:126` — **the pro-ration lands twice, and the code contradicts its own comment at `:138`.** Worked: 22 days, BASIC 22,000, joins mid-month, 2 LOP days → deducts **1,000**, correct is **2,000**. **No test combines proration with LOP** — every case has `LopDays: 0` **or** `ProRataPaidDays: null`. | **1.** Divide by `paidDaysBeforeLop`, or use the un-pro-rated basic. **2.** Add the one test that settles it: `WorkingDays: 22, ProRataPaidDays: 11, LopDays: 2` → assert **2,000**. **3.** Decide whether historical slips need correction (**a business decision, like BUG-291 — do not assume**). **✅ DONE 2026-08-08** — used the un-pro-rated basic (`monthlyBasic / workingDays`) rather than dividing by `paidDaysBeforeLop`: algebraically identical but safe for the 0-paid-day joiner an existing arm covers. Two arms added (joiner 11/22 → 2,000; leaver 15/20 → 6,000, so neither passes on a 1/2-factor coincidence), both **mutation-verified**. **The historical-correction decision remains OPEN at §G6.** | **DONE** + decision |
| ~~**GAP-004**~~ ✅ | `AuditLogPurgeService` · `Tenant.AuditLogRetentionDays` | **COMPLIANCE. A daily job hard-deletes audit logs at a 90-day default** against tech-doc §6.9's documented **7-year** retention. `:41` `retentionDays = … ?? 90` then `RemoveRange`. The *"Enterprise = 2555"* promise exists **only as a code comment** (`Tenant.cs:315`) — nothing seeds it. **No archival path.** | **1.** Raise the default, **or** archive-then-delete. **2.** Seed the plan-tier values the comment already promises. **3.** Note the interaction with GAP-005 — the purge needs `DELETE`, so it must run under the privileged role once audit tables are locked down. **✅ DONE 2026-08-08 — and the premise was wrong.** The tech doc does NOT require 7 years for everyone: §19.13 says *"7 years (configurable; **some plans = 90 days/1 year**)"* and the plan matrix says *"90 / 365 / 7y"*. **The real defect was that the tiers were never implemented** — plans seeded without a retention value (all fell to 90) and neither snapshot site copied plan→tenant, so an **Enterprise tenant paying for 7 years was purged at 90 days**. Seeded 90/365/2555 and added the copy beside the existing `MaxEmployees` line in both services. The GAP-005 interaction was already satisfied: `AuditLogPurgeJob` calls `SetSystemContext()`, so it was routing privileged before the revoke landed. **Archival remains unbuilt — now the only open part, and a product question.** | **DONE** |
| ~~**GAP-005**~~ ✅ | `Rls/roles.sql` · `audit_logs` | **Audit immutability is a convention, not a control.** `:43,47` grant `SELECT, INSERT, UPDATE, DELETE` on **all** tables to the runtime `hrm_app` role, audit included. `AuditLogController.cs:19-21` concedes it: *"append-only by code convention … REVOKE DEFERRED"*. **Anything holding the app's DB credentials — including a SQL-injection foothold or a stray `ExecuteDelete` — can rewrite audit history.** | `REVOKE UPDATE, DELETE ON audit_logs, employee_field_audit_logs FROM hrm_app;` in `roles.sql`, **plus** route the purge/anonymize paths to `hrm_owner` (already the privileged route). Re-run the RLS-on local validation. **✅ DONE 2026-08-08.** Revoke added after the broad grant; every legitimate deleter verified privileged FIRST (purge job and tenant-deletion job both `SetSystemContext()`; no cascading FK). **Proved on the live RLS-on database:** `hrm_app` gets `42501` on UPDATE and DELETE, keeps SELECT+INSERT, `hrm_owner` still deletes, and a real API login still wrote its audit row. Regression arm asserts the privilege bits both ways **and** that a real UPDATE throws — mutation-verified. Ops check (d2) added to `PRODUCTION-CHECKLIST.md`. | **DONE** |

---

### G1 — THE TWO STRUCTURAL FIXES ✅ **DONE 2026-08-08** (they are what stop G2 recurring)

| ID | Component | Gap detail | Scope required to cover | Size |
|---|---|---|---|---:|
| ~~**GAP-S1**~~ ✅ | Angular models ↔ backend DTOs (whole FE) | **The root cause of ~70% of every PARTIAL in this audit.** Two hand-written descriptions of one contract with nothing checking they agree. Hit **9 of 13 modules**. Two services literally say `CONTRACT (assumed — reconcile with backend)`. **Fixed pointwise twice and never generalized.** | **✅ TOOLING SHIPPED.** `scripts/gen-openapi.sh` emits the doc **from the built assembly** — no server, no DB, so it runs beside `dotnet build` — to a committed `contracts/openapi/hrm-v1.json` (471 paths / 1021 schemas); `npm run api:types` → `core/api/generated/api-types.ts` via `openapi-typescript`; import surface + migration guide at `core/api/index.ts`. **Both halves gated in `ci-gate.yml`** and both **proved by experiment**: an additive C# DTO change fails `--check` naming the field; stale models fail the FE gate. **⚠ REMAINING (this is G2's work, not a gap in the tooling): the 78 hand-written model files still exist.** Migrating a module = swap its interface for `Schema<'…'>` and let `tsc` point at every disagreeing call site — the failures ARE GAP-009…GAP-016. **Caveat: every generated property is optional**, because Swashbuckle emits no `required` for non-nullable C# reference types, so the types catch wrong/misspelled fields (all observed drift) but do not yet prove presence. | **DONE** — **and it retires most of G2** |
| ~~**GAP-S2**~~ ✅ | `AppDbContext` global query filters | **The EF filter layer has 6 holes; the RLS layer has zero.** The only structural difference is that RLS has a reflection-driven coverage guard asserting **exact set equality** (`RlsIsolationPostgresTests.cs:283-314`). 132 filter calls in `AppDbContext.cs`, **0 in `Configurations/`**. | **✅ SHIPPED.** `HRM.Tests/Unit/TenantQueryFilterCoverageTests.cs` — reflection over `db.Model`, mirrors the RLS guard, InMemory so it runs in dev/CI where `Rls:Enabled=false` makes the EF filter the **only** live isolation layer. Asserts the filter **references TenantId**, not merely that one exists (`!IsDeleted`-only would otherwise pass), plus a vacuity guard and the `User` exclusion the RLS guard also makes. **★ Correction to GAP-006 below: only 3 of the 6 were real.** | **DONE** (guard) |

---

### G2 — HIGH: the contract seam, by module (most of this collapses once GAP-S1 lands)

> **Scope note:** every row here is *"the backend is correct; the frontend cannot reach it."* **Do not re-scope as backend work.** Sizes assume the manual fix; **with GAP-S1 in place most become "regenerate + fix the call sites".**

| ID | Component | Gap detail | Scope required to cover | Size |
|---|---|---|---|---:|
| ~~**GAP-006**~~ ✅ | 6 entities in `AppDbContext` | `AuditLog`, `PayrollReportExport`, `PlanLimitOverride`, `TenantLatencyBucket`, `TenantLifecycleEvent`, `TenantScheduledJob` have **no EF query filter**. RLS covers them when enabled — **but `Rls:Enabled=false` in Development, so dev and CI run on one layer.** | **✅ DONE 2026-08-08 — but the scope note was wrong and following it literally would have shipped a regression.** Only **3** were real holes, each with a doc comment *claiming a filter it did not have*: `AuditLog` (filter needs a `TenantId == null` arm — the column is nullable and shared with system rows), `PayrollReportExport`, `TenantLatencyBucket`. The other **3 are deliberate system/cross-tenant tables** that say so in their own XML docs, do not inherit `BaseEntity`, and — decisively — **0 of their 23 read sites call `IgnoreQueryFilters`**, so a filter would evaluate to `TenantId == Guid.Empty` in system context and return nothing, breaking tenant lifecycle history, US-ADM-004 restore and US-ADM-009 plan overrides. They are now excluded **explicitly with reasons** in the GAP-S2 guard's allow-list. **Bonus: the `PayrollReportExport` filter fixed an unfiled live defect** — `TenantStorageUsage.ComputeBytesAsync` was summing every tenant's export bytes into one tenant's storage figure. | **DONE** |
| ~~**GAP-007**~~ ✅ | 270 `IgnoreQueryFilters()` sites | **None carries the project's own `// nosemgrep:` justification marker**, and `.semgrep/tenant-isolation.yml` (severity ERROR) runs `continue-on-error: true`. Many are legitimate — **nobody can tell which.** The rule's own header says *"tighten to blocking once the codebase is clean."* | One triage pass annotating legitimate uses, **then flip semgrep to blocking.** Size is volume, not difficulty. **✅ DONE 2026-08-11 — and the triage changed the answer rather than confirming it.** The register's "270" is exactly right as a *semgrep finding count* (265 `IgnoreQueryFilters` + 5 raw-SQL); my initial 805 was a raw grep including tests and `bin/`, and the register's number is the better one. **The premise no longer holds, though: those 265 are BACKSTOPPED.** Every tenant-scoped table has an RLS policy (`EveryTenantScopedEntity_HasRlsPolicy_CoverageGuard`), `IgnoreQueryFilters_OnAppRole_IsStillTenantIsolatedByRls` proves the database still caps the result, and GAP-001 (#497) closed the one route that reached the BYPASSRLS role by accident. Two further facts from reading them: `User`/`Tenant` carry **no tenant filter at all** (they are global by design — `users` is the single tenant-bearing table with no RLS, and membership lives in the RLS-protected `user_tenants`), so `IgnoreQueryFilters` there is a **no-op for tenant scoping**; and half the remaining sites already add an **explicit** `TenantId ==` predicate. **So 265 `// nosemgrep:` lines would have been write-only noise — the hand-maintained-list pattern the audit itself flagged as systemic finding S-2 — and would have bought nothing the database was not already enforcing.** Instead: rule severities now match what a linter can *prove* — `hrm-raw-sql-no-tenant-predicate` promoted to **ERROR** (5 findings, **all 5 read and annotated**: three are `SELECT 1 … FOR UPDATE` row locks by primary key that return no columns, one is the `set_config` GUC setter — which *is* the tenant mechanism and cannot be tenant-predicated — and one genuinely does carry `AND tenant_id = {2}` on a concatenated line the regex window missed); `hrm-ignore-query-filters` demoted to **WARNING** with the RLS-backstop rationale in the rule text. **semgrep is now BLOCKING** on our ERROR rules (`--severity ERROR --error`, `continue-on-error` and `|| true` both gone); registry packs stay advisory because making third-party packs blocking is a separate decision. **Gate verified both ways: exit 0 on the clean tree, exit 1 on a planted violation.** **★ The correctness sweep found a real, live regression that #497 had just introduced and that the 5385-test suite could not see.** `ApiCallCounterFlushService` is a `BackgroundService` writing usage rows for *every* tenant with no ambient to inherit; after the GAP-001 flip Postgres refused every 10-second tick with **`42501: new row violates row-level security policy for table "tenant_api_usage"`**, the service's `catch` re-buffered and logged at WARNING, and **tenant usage metering silently stopped — 886 consecutive warnings on the running container while every gate stayed green.** Found by *reading the log*, not by a failing test. Fixed with `CrossTenantScope`; verified live (0 failures, 0 `42501`, 307 calls persisted). **Why nothing caught it:** every RLS write arm exercised a *single* tenant's write; a cross-tenant writer was a shape the suite had no coverage for. Now bound by two real-Postgres arms (unscoped ⇒ 42501, scoped ⇒ both rows land) **and** `HostedServiceTenantScopeTests`, a population guard mirroring GAP-024's — semgrep cannot cover it, because whether a write spans tenants depends on the runtime ambient, not on syntax. Mutation-verified: removing the scope turns the guard red. **Remaining hosted-service exemption is documented with its reason** (`DashboardWarmupHostedService` discards its results). | **DONE** |
| ~~**GAP-008**~~ ✅ | `PayrollReportExportService` | `GenerateAsync:194` reads by id with **neither tenant nor owner check**; `ListAsync:274` and the concurrency count `:101` are unfiltered; **two comments assert a filter that does not exist** (`:22-24,307`). *Dormant RLS policy exists (migration `:53-68`), so not currently exploitable with RLS on.* | Add the filter (GAP-006 covers it) + owner check on `GenerateAsync`, **delete the two false comments**, add a cross-tenant regression alongside the HR export tests. **✅ CLOSED 2026-08-10 — mostly by GAP-006, and the remaining item would have been HARMFUL.** The filter landed with GAP-006, so `ListAsync` and the concurrency count are tenant-scoped and the two "false" comments are now accurate. **The owner check on `GenerateAsync` must NOT be added:** its only caller is `PayrollReportExportJob` (Hangfire), which has no acting user to compare against and runs outside any tenant context, so both checks would break payroll exports while protecting nothing — the row is stamped at `QueueAsync` and the user-facing DOWNLOAD path already enforces resolved-tenant + `RequestedByUserId == _currentUser.UserId` (verified). Rationale documented at the method so the next auditor does not re-file it. | **DONE** |
| ~~**GAP-009**~~ ✅ | Admin user-management screen | **0-for-6 ACs.** Seven of twelve service calls target routes that do not exist — `assignable-roles` and `invite/csv` return **zero backend hits**; four more send the id in the body where the route takes it in the path. | **Correct eight URL strings and two field names. No backend change** — the backend is correct and well tested. Then a spec asserting the real route strings. **✅ DONE 2026-08-08.** Verified against the generated contract: the base URLs were already right; **7 of 12 sub-paths were wrong** exactly as filed — detail (`/{id}` → `/{id}/detail`), `assignable-roles` (no such route → `GET /tenant/roles`), `invite/csv` → `invite/bulk`, `PUT /roles` (id in body) → `PUT /{id}/roles` (id in path, `roleIds`-only body), and the three lifecycle actions moving the id from body to path. **`POST /invite` also takes ONE `{email, roleIds}`, not `{emails[]}`** — fanned out per address, preserving the per-address result the UI renders. **The service docstring documented the wrong routes**, which is largely why this looked intentional; rewritten from the contract. Specs that asserted the broken URLs corrected. | **DONE** |
| ~~**GAP-010**~~ ✅ | Payroll FE (4 write paths) | Bulk assign (`rows`→`Employees`), tax slabs (`slabs`→`TaxSlabs`), adjustments (`type`→`AdjustmentType`), distribution (`generatedCount`→`generated`). **Income-tax slabs cannot be saved from the UI. Every adjustment 400s. The Send button can never enable.** All four marked `[x]` with PR numbers. | Rename the FE model fields to the wire names; **replace the hand-mocked Karma fixtures with the real DTO shape.** Note `payroll.service.spec.ts:179,201` mocks two endpoints that **do not exist on the controller at all** — delete those. **✅ DONE 2026-08-10.** All four wire names corrected and the Karma fixtures moved to the real DTO shape. **One correction: the distribution field is `queuedCount`, not `generated`** as the register states — checked against the contract. **The two phantom endpoints were NOT deleted:** both are called by live UI (the FR-4 "Test formula" button and drag-to-reorder), so deleting the spec arms would remove the only coverage of two real features while hiding the defect. Annotated in place and filed as [[ISSUE-372]] with the fix recommendation (add `/reorder` — both sibling entities already have one). | **DONE** |
| ~~**GAP-011**~~ ✅ | Public careers (`PublicVacancy*Dto`) | DTOs expose `Slug`, **no `Id`**; all three FE surfaces route on `v.id`. Navigation 404s, detail 404s, apply fails `{vacancyId:guid}`. **Compounded by `PublicCareersEnabled` having no writer outside test fixtures** — the whole public surface is off for every real tenant. | **Both halves, or neither works:** add `Id` to both public DTOs + populate at the projection sites; **and** add `PublicCareersEnabled` to the tenant-settings surface (copy the existing `UpdateHiringSettingsRequest` pattern). **✅ BOTH HALVES DONE 2026-08-10** — and the row is right that half of it ships nothing. **Half 1:** `Id` added to both public DTOs and populated at both projection sites. The sharpest form of the bug: the detail route is keyed by `{slug}` but **apply is keyed by `{vacancyId}`**, which no public payload carried — so a visitor could browse and then never apply. Also fixed `getPublicVacancy(id)`, which interpolated an id into the slug route (the "detail 404s" symptom), and aligned 4 more FE fields (`currency`→`salaryCurrency`, `postedAt`→`publishedAt`, added the required `slug`, dropped the phantom `jobTitleName`). **Half 2:** `PublicCareersEnabled` now rides the hiring-settings surface — **nullable on purpose** (`null` = leave unchanged), because a non-nullable bool defaulting to false would let an older client silently switch the careers page OFF, which is the same class of silent failure as the gap itself. 3 new arms incl. one pinning the nullable semantics and one asserting the apply-id on BOTH public surfaces. | **DONE** |
| **GAP-012** ◑ | Performance FE (8 of 11 stories) | Routes and request bodies were reconciled (BUG-243/244); **the response DTO names never were.** No adapter layer. **US-PRF-002/009/010 hard-crash** rather than degrade. `'PromotionConsideration'` vs C# `Promotion` **400s every promotion-flagged submit**. | Per service, add a `map()` adapter — **safer than renaming interfaces; it isolates the change from templates.** Highest value first: PRF-002, PRF-003. **◑ REQUEST HALF DONE 2026-08-10; response half enumerated as [[ISSUE-373]].** A mechanical diff of all 96 performance FE interfaces against the generated contract found 22 with FE-only fields. **Four were genuine write defects and are fixed** (each posted the request object DIRECTLY, no adapter): 360 submissions dropped every answer (`answers`→`items`), PIP create sent `mentorId` not `mentorEmployeeId`, and PIP checkpoints recorded neither status nor note (`status/notes`→`progressStatus/evidenceNotes`, **including the multipart FormData keys**). **★ One flagged item was NOT a defect and I had to revert my own "fix":** `ISaveMeetingNotesRequest.meetingNotesHtml` is mapped to the backend `Body` field by its service, whose docstring says so. **Since an adapter layer is exactly what this row RECOMMENDS, a name difference is as likely to be correct design as a bug — verify the service before editing.** The 17 response-DTO rows are tabulated in ISSUE-373 with per-interface field lists for triage; 57 of 96 interfaces did not name-match at all, so that table is not a clean bill of health. ~~The enum is a two-token rename (**GAP-012a, S, do immediately**).~~ **GAP-012a ✅ DONE 2026-08-08 — and it was TWO tokens, not one:** the API emits `Promotion` AND `Pip`, the FE had `PromotionConsideration` AND `PIP`. `ReviewFlag` is now `Schema<'ReviewFlag'>`, so the union is contract-derived and `REVIEW_FLAG_BADGE` being a `Record<ReviewFlag, …>` fails the build if the enum ever grows. Four specs that mocked the wrong literal corrected. | **L** (or M with GAP-S1) |
| ~~**GAP-013**~~ ◑ | Onboarding FE | 2 dead routes (`/checklists/applicable` vs `applicable-templates`; `/checklists/employee/{id}` **has no backend at all**), 1 wrong verb, 5 clearance field mismatches. **`canComplete()` is permanently false, so "Complete Offboarding" can never be enabled.** | Fix names/verbs in the FE models **only** — do not change the API (21 `OffboardingServiceTests` depend on it). **Then add the two genuinely missing endpoints**, or drop the features needing them: **AC-3's replace/merge prompt cannot be met without `/checklists/employee/{id}`.** **◑ MOSTLY DONE 2026-08-10.** **Built** `GET /onboarding/checklists/employee/{employeeId}` end-to-end (query + handler + service + controller + 3 arms), so **AC-3 is now reachable** — deliberately 200-with-null rather than 404, because "no checklist yet" is the ordinary pre-assignment state. Fixed the `applicable`→`applicable-templates` rename, the PATCH→PUT verb, and **8 field mismatches** (not the 5 "clearance" ones listed — the clearance request body actually matches; the real set was `taskId`/`offboardingId`/`exitInterviewId`→`id` and `applicableDepartmentIds`→`applicableDepartments`). **`assign` was silently discarding the HR officer's inline-edited task set** (`tasks`→`additionalTasks`) — fixed. **⚠ Open, filed as [[ISSUE-374]]: two MORE absent routes the register never mentioned** (`/checklists/preview`, `/templates/lookups`, both with live UI callers) **and the `modify` request shape**, which is a restructure (`addTasks` + `taskChanges`) rather than a rename and needs an FE flow change. Also reconciled the `CONTRACT (assumed — reconcile with backend)` comment that made this drift look deliberate. | ~~M~~ **mostly DONE** |
| ~~**GAP-014**~~ ✅ | `department`/`job-title`/`location` FE models | `{entity}Id` vs `id` → **every deactivate call sends `undefined` in the URL**; departments and job titles are broken **twice over** (FE `PATCH` against `[HttpPost]` → 405). **Independently corroborated by core-hr and attendance.** | Rename three FE fields to `id` (**or add the defensive mapping already present at `document.service.ts:47-49`**) and switch the two verbs to POST. **✅ DONE 2026-08-08** — chose the rename over defensive mapping, since GAP-S1 makes the generated type the single description. **Scope was smaller AND larger than filed:** only **47** refs, not the 407 a naive grep suggests (the rest are `Employee.departmentId`, a genuine field) — but departments had **five** mismatched fields, not one. Deactivate `PATCH` → `POST` on departments and job titles (locations already POSTed). **Phantom fields removed rather than kept optional** (`tenantId`, `managerName`, `employeeCount` on departments) — a field that is always `undefined` is what produced the broken UI; backend gap tracked as [[ISSUE-364]]. **Found en route: the AC-5 client-side deactivate block never fired** (`undefined > 0`), and its test passed only because the fixture supplied a field the API never sends — repurposed to assert server delegation. | **DONE** |
| ~~**GAP-015**~~ ✅ | `appsettings.json` SMTP | **Default deployment sends no email at all** — `Smtp:Host = ""` wires `LogOnlyEmailSender`, whose own config comment says *"the safe default that sends nothing"*. **Includes password reset and account lockout, which BR-1 designates non-suppressible.** | Add `Smtp:Host` to the go-live checklist as a **hard gate**; consider a startup warning, or **fail-fast in Production**, when the log-only sender is selected. **✅ DONE 2026-08-10 — chose FAIL-FAST, not a warning.** BR-1 designates password-reset and account-lockout mail non-suppressible, so a production deploy with a blank `Smtp:Host` would accept a reset request, log it, and send nothing — the user simply never gets the mail and cannot get back in. That is a security-relevant silent failure, not a degraded feature, and a warning in a startup log is exactly the kind of thing nobody reads (cf. ISSUE-362, where a per-run warning went unnoticed for months). The guard reads `ASPNETCORE_ENVIRONMENT` from `IConfiguration` so it lives beside the registration it protects rather than pushing host-environment awareness into Infrastructure. Dev/CI behaviour is unchanged. Hard gate added to `PRODUCTION-CHECKLIST.md` with the instruction to verify by triggering a real reset — a 200 from the endpoint proves only that the request was accepted. | **DONE** |
| ~~**GAP-016**~~ ✅ | Roles admin FE | `RoleDto.Id` vs FE `roleId` → nav/delete hit `/roles/undefined`; compounded by `Admin.Roles.Manage` **not existing in `PermissionCatalog`**, so Create/Delete never render. **Specs green because they mock both defects.** | Rename the TS field; correct the permission strings; **add a build-time test asserting every FE permission literal exists in the catalog — this class will recur.** **✅ DONE 2026-08-08 — and the class was far bigger than filed.** `IRole` is now `Schema<'RolesRoleDto'>` (narrowed on `id`/`name` only), which made `tsc` name every broken call site and **rejected four spec fixtures that had been mocking the invented `roleId`/`tenantId` shape** — S-1 exactly. Two phantom permission literals fixed: `Admin.Roles.Manage` → `Roles.Manage` (it exists nowhere in the backend, so the whole roles-admin route guard could never pass) and `ExitInterview.Conduct` → `ExitInterview.ViewDetail`. **The requested guard is `FrontendPermissionLiteralTests`** — scans guards, directives, service helpers and the assignable catalog, asserts each against `PermissionCatalog`, with a second arm ensuring its baseline can only shrink. **★ It immediately exposed that 17 of the FE catalog's 44 keys exist in NO backend catalog → [[ISSUE-363]], filed not guessed** (each needs an authz decision; the role editor offers them as assignable checkboxes, so an admin can grant permissions that do nothing). Two latent NPEs surfaced en route (nullable `description`/`permissions`). | **DONE** |
| ~~**GAP-018**~~ ✅ | `Program.cs` rate limiter | All three policies partition on **client IP only**, attached to **anonymous endpoints only**. **Every authenticated tenant endpoint is unthrottled**, and the limiter is in-process so limits multiply by instance count — which also silently weakens the `auth-login` brute-force control at scale. | Global limiter partitioned on `(tenantId, userId)` with a system-context bypass; move to the Redis fixed-window already used for portal links when multi-instance. **✅ DONE 2026-08-11** exactly as prescribed — a `GlobalLimiter` keyed on `t:{tenantId}:u:{userId}`, falling back to IP for anonymous traffic, with three documented exemptions (`RateLimiting:Disabled`, `/health`+`/swagger`, system context). **Ceiling deliberately generous (300/min):** this is a runaway/abuse backstop, not a quota — a dashboard load fires tens of requests, so a tight limit would break normal use. **Why not IP:** an office behind one NAT shares an address (an IP partition throttles innocent colleagues together) while a single abusive token roams across addresses and slips any IP bucket. **The partition key is extracted as a pure `GlobalRateLimitPartition.Resolve` and tested directly**, because the HTTP integration host sets `RateLimiting:Disabled=true` — the limiter is structurally unreachable through that harness, so a control tested only there would be a control on paper. 13 arms, **mutation-verified**: reverting to IP-only turns 4 of them red. Redis remains the multi-instance follow-up (still in-memory, still per-instance). | **DONE** |

---

### G3 — MED: never built (no story, no code) — decide scope before estimating

> **These are Pass B's 15 uncovered capabilities.** They are invisible to every ledger **because there is no story line to mark incomplete.** Several are Phase-2-shaped; **the point of listing them is that nothing currently tracks them at all.**

| ID | Component | Gap detail | Scope required to cover | Size |
|---|---|---|---|---:|
| **GAP-019a** | §11.1 operator surface | **8 of the 15 gaps sit here** — revenue dashboard (MRR/ARR/churn), manual billing ops (credits/invoices/refunds/trial extension), platform staff management, maintenance mode, broadcasts, GDPR request intake. **Note the trap: `docs/BA/platform/` looks like it should hold these and does not** — US-PLT-001..006 are cross-cutting engineering stories. | **Decision first: does Phase 1 keep billing ops in a spreadsheet?** If yes, most of this cluster parks. If no, it is one epic. **Do not estimate before that decision.** | **L** (cluster) **⏸ PARKED 2026-08-11 by decision (product owner).** Not deferred for lack of time — parked because **the cost of not having it is low**, which is the register's own assessment: *"With billing offline, the operational cost of the whole cluster is low."* §3.2 defers **automated** billing; this row is the **manual** ops surface §11.1 retains, so it exists to service a billing system that is not running. Seven distinct surfaces (revenue dashboard · credits/invoices/refunds · trial extension · platform staff · maintenance mode · broadcasts · GDPR intake) of net-new operator UI is the weakest value-per-effort item left in the queue while billing is offline. **Revisit trigger: when automated billing goes live** — at that point the operators servicing it need this, and the cost of its absence stops being low. **When it is revisited, start with a scoping pass against `src/`, not with implementation** — Pass D found zero orphaned code across the product, so parts of this may already exist, and five of eight register prescriptions executed on 2026-08-11 were wrong as written. | **PARKED** |
| **GAP-020** | JWT signing key | **No way to rotate a compromised signing key.** `RevokeAllSessions` is per-user. §3.4 declares cross-tenant leakage zero-tolerance; **the containment lever does not exist.** | System-admin rotate endpoint with an overlap window (the key ring already supports it — `JwtKeyRingOptions.cs:22-40`) + a global refresh-token epoch bump. **Route to `/security-audit` for an exploitability rating before scheduling — it may outrank its inferred "Should".** | **M** |
| **GAP-019b** | Google + Apple sign-in | **In Scope at §3.1, with their own named sample stories at §5.2**, and §3.3 assumes the Google dev account is provisioned. `US-AUTH-001:81` deferred all three social logins; Microsoft was rescued with six stories, **Google and Apple never picked back up.** Zero source hits. | One BA story + **reuse the Entra OIDC pipeline — `EntraSsoService` is generic OIDC, so Google is a provider config, not a second architecture** + the FE button. Apple is gated on an Apple Developer subscription (§3.3). | **M** |
| **GAP-019c** | §33.3 platform reporting | Per-tenant SLA breach, storage growth, API call volume, failed-payment queue. **§1.4 success criterion #4 (99.5% uptime measured per tenant) is currently unmeasurable** — `PlatformMonitoringService.cs:511` states the number is a *platform* property returned identically for every tenant. | Either scope the health probe per tenant, **or amend §6.4 to say "platform uptime"** — a requirement that is unmeasurable is worse than one that is unmet. `ApiCallCounter` already gives partial substrate. | **M** **✅ §6.4 DECIDED + AMENDED 2026-08-11** — [[ADR-2026-08-11-uptime-is-platform-not-per-tenant]]. §1.4 criterion #4 and §6.4 now read **"≥ 99.5% monthly PLATFORM uptime"**; "per tenant" is withdrawn. The register's own framing settled it: *a requirement that is unmeasurable is worse than one that is unmet.* `health_probe` has no `tenant_id` and `PlatformMonitoringService.cs:511` says the figure is a platform property returned identically for every tenant — so the only options were to build per-tenant synthetic probing or stop mislabelling the number. **Worth noting the code was already honest:** before probe history existed the dashboard's uptime field was deliberately **null** rather than computed from nothing (TC-ADM-002-17 forbids fabricating it); this just extends that honesty to the requirement text. **A platform metric can read "up" while one tenant is degraded — accepted and now stated.** If per-tenant health is wanted as a *signal* rather than an SLA, the cheap path is per-tenant error-rate alerting off the `tenant_id` log enrichment shipped in GAP-035, not synthetic probes. **Still open: the rest of §33.3** (per-tenant SLA-breach / storage-growth / API-volume / failed-payment views). | **partly DONE** (metric decided) |
| **GAP-021** | US-PRF-011 calibration | Marked `[x]` DONE. **Data model shipped; the workspace and the phase-completion state machine did not**, and `Performance.Calibrate` was **never created** — endpoints reuse three broader permissions. **The story itself warned: *"Do not reuse a docstring-only string; that was the ISSUE-290 trap."*** | Add `CyclePhase.CompletedOn` (+ migration) **as the story recommends, over a calibration-specific flag** · add the permission to `PermissionCatalog` + role bundles + `PermissionCatalogTests` · build the FE workspace. | **M–L** |
| **GAP-022** | `PayrollRunProcessor.ComputeOvertime` | **`FteScaledOvertimeBase` is dead wiring.** `:977` calls `Compute` with **4 of 7 args**; `fte`/`fteScaledBase` fall to defaults and **`.Fte` appears 0 times in the file.** Enabling the flag changes no payroll output. **`TC-ATT-152` is marked `automated` asserting stored earnings the pipeline cannot produce.** | Thread the **already-loaded** per-location policy map + `emp.Fte` into `ComputeOvertime`; **bind an integration arm asserting a persisted `PayrollSlip.OvertimeAmount` under both flag states**, and demote TC-ATT-152 to `draft` until it exists. | **S**+**S** |
| ~~**GAP-023**~~ ✅ | Employee FTE / work arrangement FE | `STATUS.md:61` claims *"SHIPPED … + FE employee-form"*. **Zero frontend files reference `workArrangement` or `fte`.** Backend is excellent (migration, leave proration, Remote geofence exemption). **HR cannot set either through the product** — bulk import or SQL only. | Two form controls on the employee wizard/profile + the two model fields. **Correct `STATUS.md:61`.** **✅ DONE 2026-08-11 — and the register was RIGHT on this one**, which is worth saying given how often it was not this session. Measured first: the 9 apparent FE hits are all in the **generated** `api-types.ts` (from GAP-S1), so no hand-written Angular code touched either field. **I briefly got this wrong in the other direction** — I reported that `UpdateEmployeeProfileCommand` lacked the fields and that GAP-023 therefore needed backend work; it does not. The fields live on the nested `EmploymentInfoUpdate` in `DTOs/`, which my grep of `Commands/` missed. Verified before building on it: `EmployeeService:723-735` really does apply them, with an employment-history entry, and `UpdateEmployeeProfileValidator` binds `ValidFte()`. So this was FE-only exactly as sized. **Shipped:** wizard (create) · profile (edit) · profile read-only display. **The profile half is the one that matters** — create-only would have meant HR could set an FTE at hire and never change it, and full-time-to-part-time is the real case. Blank on the profile sends `undefined`, not `0`/`''`, because the backend reads null as *leave unchanged* — sending a falsy value would either fail `ValidFte` or silently reset a stored arrangement. **The client validator mirrors `EmployeeFteRules.ValidFte()` exactly, including the 2-decimal SCALE rule** — that half is the realistic drift risk, since a looser client rule is worse than none (clean form, then an opaque server error attached to no field). 11 arms, **mutation-verified** (dropping the scale check turns 2 red; allowing FTE > 1 turns 1 red). Six spec fixtures needed the new required fields — added, not weakened. Verified with a **real `ng build`**, not just `tsc` (BUG-302's lesson), and **FE 4053+11 green**. **`STATUS.md:61` corrected** in place. | **DONE** |
| ~~**GAP-024**~~ ✅ | 3 Hangfire jobs | `HealthProbeRecorderJob`, `EncryptionKeyAgeWatchdogJob` (neither calls `SetSystemContext()`) and `DocumentExpiryNotificationJob`'s flat cross-tenant loop **run with no tenant context**. **No `IServerFilter` exists** despite §9.4-3 documenting one. **And no background-job log line carries `tenant_id`** — exactly where you would need it to diagnose an isolation incident. | A real `IServerFilter` reading a `tenantId` job argument and populating `ITenantContext` + log context (then delete the per-job boilerplate) — **or**, short of that, `SetSystemContext()` on the three and wrap the document loop in `RunForTenantAsync`. **✅ DONE 2026-08-11**, but *not* by the prescribed route — **the documented §9.4-3 filter cannot work in this codebase, and building it would have produced a control that looks real and does nothing.** 42 of the 62 job classes create their **own DI scope** (`IServiceScopeFactory.CreateScope()`) and resolve `AppDbContext`/`ITenantContext` from it; a filter can only reach the scope Hangfire *activated* from, so any tenant it set would land on a different scoped `TenantContext` instance than the job body reads — the job would still see `IsResolved == false`. So the two halves were split by what each mechanism can actually reach. **Log half — a real `IServerFilter` (`JobLogContextFilter`), which works because Serilog's `LogContext` is an `AsyncLocal` that flows *down* into the body and its nested scopes:** every job line now carries `job_name`, `job_id` and, for per-tenant jobs, `tenant_id`. That "an `AsyncLocal` written by a *synchronous* callee is visible to its caller" is the entire load-bearing assumption, so it is **asserted against a real Serilog pipeline, not reasoned about** — it is exactly the kind of thing that would compile, run, log nothing extra, and pass review. `Guid.Empty` is logged as **absent rather than as a scope** (an all-zero tenant id reads like a real scope to whoever is grepping during an incident). **Context half — declared by the job bodies** and locked by a coverage guard, `BackgroundJobTenantContextTests`. **Register corrections (measured, not assumed):** it was not 3 jobs — **15** classes declare no scope, of which **11 are enqueue-only schedulers** (they call `IBackgroundJobClient` only, touch no `DbContext`, and run inside the caller's already-scoped request), leaving **3 real job bodies — but a different 3**: the register **missed `OnboardingOutboxReconcileJob`** (it enumerates every tenant) and was **wrong about `DocumentExpiryNotificationJob`, which already declares system context** (`:36`). The scheduler exemption list is itself guarded in both directions (no `AppDbContext`; no stale names), so it cannot become a hiding place. **Why this had to precede GAP-001:** 2d inverts the unresolved-context default from **privileged to restricted**, so every job that never declares a scope passes today by luck and fails afterwards — `OnboardingOutboxReconcileJob` would have silently reconciled **nothing**. 13 arms, **mutation-verified**: dropping a `SetSystemContext()` turns the guard red, skipping `OnPerformed` disposal turns 2 leak arms red, accepting `Guid.Empty` turns 1 red. | **DONE** |
| **GAP-025** | `Employee` `IAuditExempt` | Employee changes route to `EmployeeFieldAuditLog`, which has **no action name, resource type/id, IP, user agent or trace** — and **is not readable from the US-NTF-005 audit viewer.** Compliance-visible. | Have `EmployeeService.cs:903` **additionally** write a structured `AuditLog` row with `Action="Employee.Update"` — **preferred over dropping the exemption, which would reintroduce the double-write it exists to prevent.** Then review the full `IAuditExempt` list for others whose "own writer" is as thin. | **M** |
| **GAP-026** | `BenefitEnrollmentService` | **`EmployeeStatus` is never consulted** — a **Terminated employee is eligible for and enrollable into any rules-free plan** (US-TRN-003 AC-1/FR-4 say "active employees"). Silent: no error, no log, wrong data, on a benefits-cost path. | Add `Status == Active` to the target-employee guard (~`:220`) and to `EligiblePlansForAsync`; one xUnit arm. **Open question to decide, not assume: should an existing enrollment auto-terminate? Recommendation — keep manual (AC-7 makes it explicit), block only new enrollments.** | **S** + decision |
| **GAP-027** | `LocalFileStorage.GetSignedUrl` | **Document download is dead twice over** — returns `/files/{tenantId}/{path}` with the comment *"no real signing"*, and **no route serves `/files/*`**; independently, the FE reads `downloadUrl` while the BE emits `signedUrl`. Same dead link is emailed by the **data-export** feature. | Point the email/UI at the authenticated `/download` route (**S, closes the user-visible break**), or register a real signed-URL endpoint (**M**). Fix the FE field name. | **S–M** |
| **GAP-028** | Tenant data export | Bundle ships **3 of 5 artifacts** (CSVs, `audit_log.jsonl`, manifest with real SHA-256); **documents ZIP and schema PDF absent**, and the emailed link is the dead `/files/…` route. **GDPR Art. 20 portability is the story's stated purpose.** | Fix the link first (GAP-027). Documents ZIP + schema PDF are the **M–L** remainder; both are openly deferred in their TCs. | **M–L** |
| **GAP-029** | `TenantProvisioningService` | **New tenants get no default workflow**, justified by *"no configurable-workflow entity in the codebase yet"* — **now false** (US-ADM-007 shipped it). Consequence: every new tenant runs the AC-11 legacy fallback, leaving **the entire US-ADM-011 runtime engine — parallel steps, SLA escalation, delegation — dormant** until an admin hand-authors a workflow. | Seed one 1-step manager-approval Leave definition at provisioning. **The highest-leverage small fix in admin-console.** | **S** |
| **GAP-030** | US-ADM-012 · US-PLT-004 | Both are **`[EPIC STUB]` stories** — *"AC skeleton only; full detail to be authored before build"* — with **~50 production files and 2 migrations built against them, and zero IEEE-829 TCs.** Neither appears in `TRACEABILITY-MATRIX.md`. **Code built against an empty placeholder reads as "covered" to every ledger and grep.** | **Author the FR/BR/NFR sections into both stories *from the shipped code*** (cheaper and more accurate than the reverse), then commission TCs for the ACs that matter. | **M**+**M** |

---

### G4 — MED: the activation gaps (S-3) — cheapest large win in the plan

> **Every item here is already built and shipped switched off.** Not capability work — **wiring**.

| ID | Component | Gap detail | Scope | Size |
|---|---|---|---|---:|
| ~~**GAP-034**~~ ✅ | CI | **`@axe-core/playwright` installed with zero `AxeBuilder` usages**; `e2e/cross-browser.spec.ts:20,65` has a **real 360px overflow assertion no workflow runs**. | **Add a Playwright job to `ci-gate.yml`. The tests are already written — highest ratio of coverage-gained to work-done in this entire plan.** **⚠ NOT DONE — the premise is unverified and the sizing is wrong.** Attempting it exposed [[ISSUE-365]]: the Docker `frontend` on :4200 has **no `/api` proxy**, so `GET` returns `index.html` with 200 and `POST /auth/login` returns 405. A full e2e run against it produced **30 failures, every one a 30s timeout inside `loginAsE2EOwner`** — i.e. the run said nothing whatever about the application. The suite's own documented prerequisite is `ng serve` (which proxies via `proxy.conf.json`), not the container. **So "the tests are already written" is still unproven — nobody has seen them pass.** And a CI job needs Postgres + backend + a proxying frontend stood up, not just a `npx playwright test` step. **Re-sized S → M.** Done so far: `baseURL` honours `E2E_BASE_URL` so CI and a spare-port local run can point somewhere valid. **Ran it: 28 of 30 now pass.** Two bit-rotted selectors were blocking the entire suite — an ambiguous `Dashboard` link match (Playwright strict-mode violation on every test, fixed with `exact: true`) and an `'Attendance'` nav label that does not exist. **So the premise is half right: the tests are real and mostly pass, but "just add a CI job" was wrong twice — the suite needed repair, and CI needs Postgres + backend + a PROXYING frontend.** Remaining 2 failures move between runs and both time out post-login; **not** the 10/min auth limiter (zero 429s in the backend log), most likely machine contention. Durable fix: Playwright `storageState` so the suite logs in once instead of 30 times. **✅ DONE 2026-08-11 (#499) — the CI YAML is written and GREEN.** An `e2e` job in `ci-gate.yml`: Postgres service + backend + a **proxying** frontend (`ng serve`, not the container) + Playwright chromium. **Both earlier wrong turns are now encoded in the job instead of waiting to be rediscovered:** an explicit **"Verify the proxy actually proxies"** step fails in one second with a clear message if `/api` returns HTML, rather than as 30 mysterious 30-second login timeouts (ISSUE-365) — and it bit me again while writing it, since the Docker `frontend` container was holding `:4200` locally. `ASPNETCORE_ENVIRONMENT=Development` is load-bearing (only there does `DbInitializer` seed the `e2e` tenant). No JWT secret needed — `JwtService` self-generates when the key is blank. **★ The first CI run FAILED, and holding the merge for it was the point:** it settled a question the local run could not. The **auth-login limiter IS the cause** — 28 logins at 200 and **4 at 429** — so the plan's earlier "not the limiter, zero 429s" was right locally and wrong in CI, where a faster runner packs the logins into a tighter window. `RateLimiting__Disabled=true` for this job only; the limiter keeps its own 13 mutation-verified arms. Also fixed: the report artifact never existed, because `reporter: 'github'` writes no report directory — precisely when you most want one. **The prescribed `storageState` fix was NOT implemented because it cannot work here, and the reasoning is recorded in `playwright.config.ts` so nobody re-derives it:** access tokens are in-memory only by design, and refresh tokens are **single-use with rotation and reuse-detection** (proved live: replaying one cookie gives 200 then **401**, and a detected reuse revokes the whole token family). Thirty contexts sharing one saved state would be one pass and 29 401s, failing in a way that looks like an application bug. **Second CI run: all checks green, E2E in 5m29s.** **And it earned its keep immediately** — the uploaded backend log surfaced [[ISSUE-376]] on the very first run: a client-cancelled request logged as an unhandled 500, which no unit or API test can find because none abandons a request mid-flight. **Still open: `@axe-core/playwright` is installed with zero `AxeBuilder` usages** (the a11y half of this row), and cross-browser (firefox/webkit) is deliberately not in CI yet — adding engines never exercised there is its own decision. | ~~S~~ **M** |
| ~~**GAP-031**~~ ✅ | Coverage | Coverlet installed and **never invoked**; no `.runsettings`; Karma coverage reporter **off**. §6.6's ≥70%/≥85% requirement **can be neither met nor missed.** | `--collect:"XPlat Code Coverage"` in `run-backend-tests.sh` + publish the number. **Enforce the threshold later** — measure first. **✅ DONE 2026-08-10** — `COVERAGE=1` opt-in in the gate script (off by default so local runs stay fast), turned on in CI, with a step that prints line/branch coverage to the job summary. Karma gets `--code-coverage`. **Measure-only by design: no threshold enforced**, because setting a gate before anyone has seen the number is how the number ends up setting the gate. The script self-test still passes. | **DONE** |
| ~~**GAP-031b**~~ ◑ | Static analysis | semgrep + gitleaks both `continue-on-error` + `\|\| true`; **no `TreatWarningsAsErrors`**. `package.json:11` `"lint": "ng lint"` **with no lint builder and no ESLint config — it would error if invoked, and CI never invokes it.** | Make gitleaks blocking (**it cannot catch the next committed secret today**); fix or delete the lint script; flip semgrep after GAP-007. **✅ gitleaks BLOCKING 2026-08-10** — `--exit-code 1`, `continue-on-error` and `|| true` both removed. **Measured before flipping, and the measurement mattered:** a working-tree scan reported **124 leaks**, which looked like a multi-day triage — but **121 were in `.angular/cache` webpack packs and gitignored env files that a CI checkout never contains**. A full-history scan (the thing CI actually runs) found **3**, all documentation placeholders (`idem-key-abc-123`, a UUID quoted in a Serilog excerpt). Three narrow allowlist regexes later: **zero findings across 1170 commits**, verified twice. **The register's "S" sizing was right and my intermediate alarm was wrong — I had measured my local tree instead of what CI sees.** Build-cache and `perf/results` paths added to the allowlist too, which also cut a local scan from 8min to 30s. **⚠ Still open: the `lint` script** (`ng lint` with no ESLint config) and the semgrep flip (gated on GAP-007). | **partly DONE** |
| ~~**GAP-035**~~ ✅ (a) | Observability | **`user_id` and `trace_id` are absent from every log record** — `TenantResolutionMiddleware.cs:167-171` is the only enrichment site and pushes tenant only. OTel is **fully wired and fully dormant** (blank `OtlpEndpoint`); `HrmDomainMetrics.cs:14` **deliberately excludes tenant tags**. Combined with **zero alerting**, an incident is diagnosable only after someone opens a dashboard. | **Cheapest third first: `Serilog.Enrichers.Span` + a `user_id` push ≈ 10 lines, closes §6.11-a.** OTel activation is ops (stand up a collector). Alerting is its own slice. **✅ §6.11-a DONE 2026-08-11.** Gap **measured before building** rather than taken on the register's word — on the running stack's log, of **1258 request-scoped property bags `RequestId` appeared in all and `user_id`/`trace_id` in none**; both output templates already render `{Properties:j}`, so only the properties were missing. **Built as Serilog enrichers, not a middleware:** an enricher runs per EVENT, so it also covers pre-auth lines, EF Core SQL and background jobs — none of which a post-auth middleware can reach. **The `Serilog.Enrichers.Span` route the register proposed would have emitted nothing:** with OTel dormant (the shipped default) **nothing registers an `ActivityListener`**, so ASP.NET Core never creates an activity and `Activity.Current` is null on every request. So a `PropagationData`-sampling listener is registered **only while OTel is dormant** (when OTel is on it brings its own listener *and sampler*; a second always-sample listener would override its sampling decisions) — the cheapest level that still yields a real W3C id, including one **continued from an inbound `traceparent`**. **No all-zero trace id and no empty user id are ever emitted** — absent is the honest signal, the same trap as logging `Guid.Empty` as a tenant. **Impersonation is disambiguated:** `ICurrentUser.UserId` is the impersonation TARGET, so a bare `user_id` would attribute an operator's actions to the employee they were impersonating in exactly the record an incident review reads; the operator is emitted alongside as `impersonated_by`. The enricher reads claims directly (a DI resolve per log event can hit a disposed scope as a request completes) and **its claim precedence is pinned to `CurrentUser`'s by a test** so the two cannot drift about who did something. **Verified end-to-end on the running stack, not only in tests** — rebuilt the container, logged in, called `GET /api/v1/tenant/employees`: `trace_id`/`span_id` now on all 392 request-scoped bags, `user_id` on the authenticated ones (the remainder are anonymous health probes, correctly bare). 13 arms, **mutation-verified** (all-zero trace id · flipped claim precedence · listener registered when OTel is on · dropped actor claim each turn one red). **Known limit, stated rather than papered over:** Hangfire job lines carry `job_name`/`job_id`/`tenant_id` (GAP-024) but **no `trace_id`** — jobs run outside any activity; `job_id` is their correlation key. **Still open: OTel activation (ops) and alerting** — the "then **M**" half. | **DONE** (a) / open (b,c) |
| ~~**GAP-032**~~ ✅ | Tenant lifecycle | `terminated` tenants are **not blocked at the API layer** (`TenantStatusEnforcementMiddleware.cs:68` returns early for anything but Suspended/Terminating) — a pre-termination JWT keeps working until it expires. `past_due` is **defined, unreachable and unenforced.** | Add `TenantStatus.Terminated` to the guard (**S**). For `past_due`: **mark Phase 2 in §9.5 now, build when billing lands** — billing is offline in Phase 1, so this is a deferral the doc presents as live behaviour. **✅ Terminated DONE 2026-08-10** — added to the status switch (it fell straight through to `_next`, so a pre-termination JWT kept working until it expired) and refused with 451 on every API path. **Found while doing it: the export carve-out had to become status-DEPENDENT** — `IsAlwaysAllowed` let `/data-exports` through for any status, but a terminated tenant has no grace window and its data is already deleted, so exports are now refused for Terminated only, while Terminating keeps the window its whole purpose depends on (pinned by its own arm). Auth + lifecycle-notice stay open for every status so the UI can still say what happened. 7 new arms; `PastDue` explicitly pinned as unaffected. **`past_due` doc note still outstanding.** | **DONE** (code) + doc |
| ~~**GAP-036**~~ ✅ (key) | Secrets | **No Key Vault / Secrets Manager integration at all**, and **a live base64 AES-256 field-encryption key is committed** at `appsettings.Development.json:37`, self-labelled "safe to commit" — while `EncryptedFieldRegistry` covers `employees.national_id`, **so anyone running the dev stack with real data encrypts it under a public key.** | Move the dev key to user-secrets/`.env`; **make gitleaks blocking** (GAP-031b). A real vault is a separate ops decision. **✅ KEY MOVED 2026-08-11** — out of `appsettings.Development.json` (committed) into gitignored `docker.env`, **same value** so existing local ciphertext stays readable, with `docker.env.example` documenting the requirement and carrying no value. **The "S" sizing was wrong, and the reason is worth recording:** `AesGcmFieldEncryptor` **fail-fasts** when no usable key is configured, so simply deleting the committed key breaks **bare `dotnet run`, design-time `dotnet ef`, AND two CI jobs**. **Verified, not assumed** — with the key blank, `dotnet ef migrations list` dies with *"Unable to create a 'DbContext' … Field encryption is not configured"*. The `migrations` job had been passing only because EF tools default to the Development environment and the live key was sitting in that committed file. Both CI jobs now generate a **throwaway key per run** (`openssl rand -base64 32`) — CI encrypts nothing anyone will ever decrypt, so a per-run key is strictly better than a shared one and there is no secret to store or rotate. Passed **inline via `env`** rather than exported, because the key id contains hyphens and bash's `export` rejects that as an identifier — both that failure and the `env` workaround were reproduced locally before being written into the workflow. The fail-fast message now points at `docker.env`/user-secrets instead of the old location, and `local-dev/README.md` gains the setup step with the reason. **Verified live:** rebuilt the container, it boots in 12s with the key coming only from `docker.env`, login works, and an endpoint reading an encrypted field returns 200 — so existing data is still decryptable. **Still open: a real vault** (Key Vault / Secrets Manager), which remains the ops decision the register called out. | **DONE** (key) / ops (vault) |

---

### G5 — ✅ **DONE 2026-08-10** — ledger reconciliation (was: do early, cheap)

> ✅ **APPLIED 2026-08-10 (PR pending).** Every id was re-checked against `TEST-FINDINGS.md` rather than
> transcribed. **L8 was done FIRST and that ordering was load-bearing:** an intermediate check of mine reported
> ISSUE-109 as OPEN, which was wrong — the grep had hit the PAYROLL twin of a colliding id. No count in this
> section means anything until the ids are unique.
>
> **Verified outcomes, and four corrections to this register:**
> * **7 true id collisions, not 3** — and the one it missed is **BUG-068**, the id the plan headlines. Renumbered
>   the less-cited instance of each pair (→ BUG-303/304, ISSUE-366..370) with a redirect note on both sides.
> * **L1 is 10 closed / 1 deferred / 1 partial**, not "twelve closed". **L2 undercounted** — all 11 attendance
>   ids are closed, not 8.
> * **L4's "split BUG-057" does not hold:** it is goal-scoped and RESOLVED, and `ManagerReviewConfiguration.cs:57`
>   does call `.IsRowVersion()`, so the premise (manager-review lacks the token) is false at the DB layer.
> * **L6 is 7 instances, not 2 — and one of the register's two was a FALSE POSITIVE.** `TC-ADM-010-13` never
>   names `system_audit_log` (its step says "the system audit log" as prose, which `audit_logs` satisfies); I
>   demoted it on the register's claim before reading the file and had to revert. The genuine set is six TCs
>   marked `pass` with an executable step querying the phantom table (→ [[ISSUE-371]]) plus `TC-ATT-152`.
> * **L10a self-corrected:** `PayrollReportExportService.cs:307`'s "the global query filter scopes this" became
>   TRUE when GAP-006 added the filter.
>
> **Not done here, deliberately:** no `[!]` story marker was flipped. That requires re-running TCs
> (`/verify-fix`), which is test execution, not ledger editing.
>
> Original note: **36 CONTRADICTED verdicts.** Per the 2026-07-30 and 2026-08-04 lessons — now measured product-wide — **every estimate built on these rows is wrong until they are reconciled.** The regression tests already exist, so the re-runs are cheap.

| ID | Target | What is wrong | Scope |
|---|---|---|---|
| ~~**GAP-L1**~~ ✅ | `TEST-STATUS.md` recruitment (`:163-172`) | **Twelve closed findings still narrated as live** — BUG-055/058/059/060/066/068(CRIT)/003(CRIT), ISSUE-108/109/122/132/140. **Reading it today re-opens eight fixed tickets.** | `/verify-fix` per ID, or one batch pass over the module |
| ~~**GAP-L2**~~ ✅ | `TEST-STATUS.md` attendance (`:154-158`) | **Eight stale** — BUG-047, ISSUE-065/066/067/069/071/073, BUG-050, ISSUE-086, ENH-005, ISSUE-068 | `/verify-fix` batch |
| ~~**GAP-L3**~~ ✅ | `TEST-STATUS.md` reports (`:215`) | **Five stale** — ISSUE-193/195/197/198, BUG-086 | `/verify-fix` batch |
| ~~**GAP-L4**~~ ✅ | `TEST-STATUS.md` performance (`:188-189`) | BUG-063, BUG-242 stale; **BUG-057 is half-fixed and the ledger does not distinguish** (goals have the concurrency token, manager-review does not) | `/verify-fix`; **split BUG-057** |
| ~~**GAP-L5**~~ ✅ | `TEST-STATUS.md:233-235` | Calls training-benefits **"Actual zero test coverage"** against 6 test files (3 Testcontainers-Postgres) + 6 Karma specs, and US-NTF-006 **"not-yet-built"** against ~1,000 shipped lines and 34 test references. **Four Must-Have NTF stories have no row at all** despite 60 authored TCs. | Reword to *"no IEEE-829 TC suite"*; open `[!]` rows. **Re-rank P0-2 — authoring TCs here is lower value than the FE gaps.** |
| ~~**GAP-L6**~~ ✅ | `TC-ADM-010-13` · `TC-ATT-152` | **Two TCs marked passing against code paths that do not exist.** | Re-run and flip; **audit sibling TCs for the same fabricated-assertion pattern** |
| ~~**GAP-L7**~~ ✅ | `TEST-FINDINGS.md:302-303` · `:322-331` | BUG-003's header says RESOLVED while its body still reads *"STILL PRESENT"*. **A reader cannot tell which is current**, and the resolving code is cited nowhere. | Collapse to RESOLVED citing `TenantAccessGuardMiddleware.cs:38-53`; move the historical narrative into a dated sub-block |
| ~~**GAP-L8**~~ ✅ | `TEST-FINDINGS.md` ID collisions | **BUG-060, ISSUE-108 and ISSUE-109 are each used TWICE** (Recruitment + Payroll), and the roll-up credits "#169 ISSUE-109" without saying which. Also **two `[Trait("TC",…)]` collisions** in the test project (`TC-PLT-004`, `TC-PLT-006`). | Renumber; **re-audit every RESOLVED line citing a colliding ID — a fix to one module currently reads as closing the other's finding** |
| ~~**GAP-L9**~~ ✅ | `BA/STATUS.md` + `INDEX.md` | STATUS contradicts **itself** on US-PLT-002 (`:105/109` vs `:194/283`); INDEX **swaps the MoSCoW** of US-PLT-002/003 vs the story frontmatter; INDEX still calls US-TRN "STUBS"; `:61` claims an FE that does not exist; `:236` contradicts `:111` on the ApiCalls gauge | Reconcile to the code and to story frontmatter (**the spec of record — the gap-analysis depth rule keys off `priority:`**) |
| ~~**GAP-L10**~~ ✅ | Stale code comments | Comments that **actively lie about their own code**: `PayrollReportExportService.cs:307` asserts a filter that does not exist · `PayrollReportService.cs:737-752` describes the pre-ISSUE-197 approximation · `DependencyInjection.cs:345-347` says "No attendance module yet" · 5 core-hr `TODO(module X)` comments whose modules shipped · `appsettings.json:3` + `DbInitializer.cs:66` say `Rls:Enabled=false` everywhere | Sweep and delete/correct. **This is the fourth arc where a stale comment produced a wrong conclusion — including inside this audit's own briefs.** |

---

### G7 — LOW tail (carried so nothing is silently dropped)

| ID | Component | Gap detail | Scope | Size |
|---|---|---|---|---:|
| **GAP-033** | Security headers · TLS | **No CSP/HSTS/X-Frame-Options/X-Content-Type-Options anywhere**; no `UseHttpsRedirection`/`UseHsts`. TLS 1.2+ is enforced **only** by `local-dev/nginx*.conf`, an opt-in dev overlay — **`ops/` contains no TLS/nginx config at all.** *70% confidence this is a genuine gap rather than out-of-repo infra.* | Add the headers middleware (**S**). **Confirm where production TLS terminates before treating the rest as a gap.** | **S** + confirm |
| **GAP-037** | Outbound webhooks | §9.4-7 and §9.7-7 document per-tenant webhook URLs + signing keys. **Zero hits for `webhook` in `src/backend`** — a whole documented subsystem with a documented tenant-isolation contract is unbuilt. **No evidence anyone is waiting on it.** | **Mark Phase 2 in the doc (S).** Build only if a customer asks (**L**). |  **S** (doc) |
| **GAP-038** | `admin.yourhrm.com` | §9.2 and §9.8 document it as the system-admin entry point. **Nobody can authenticate there** — it resolves to `TenantId == Guid.Empty`, which no `user_tenants` row can match. `SystemEndpointHostGuardMiddleware.cs:28-32` explains this in detail. Platform admins are users of the real `platform` tenant. **Anyone provisioning production from §9.2/§9.8 points DNS and the console at a dead subdomain.** | **Doc fix only — the capability is implemented and arguably better than documented.** | **S** (doc) |
| **GAP-039** | Redis leave-balance cache | Invalidation absent module-wide (US-LV-005 AC-1, US-LV-008 AC-3, US-LV-010 AC-2) — four `TODO(redis-balance-cache)` sites. **A deliberate vault decision: balances compute live from the ledger, so there is NO correctness bug today.** An unmet AC, not a defect. | **Leave it.** Revisit only if balance reads become a measured hot path. | — |
| **GAP-040** | §10 folder layouts | Documented backend/frontend trees are stale in both projects (`HRM.Shared`, `Domain/Events`, `Api/Extensions`, `core/http/`, `features/billing/` absent; ~11 real feature areas undocumented; `tailwind.config.ts` is actually `.js`). | Regenerate both trees from `find` (**S**). **The one item here worth *building* is `HRM.ArchitectureTests` — it would have mechanically caught GAP-006.** | **S** |

---

### G6 — DECISION GATE (do not schedule until decided)

| Item | The decision | Recommendation |
|---|---|---|
| ~~**Goal cascading org/department tier**~~ ✅ DECIDED | §11.9 and US-PRF-001 FR-4 promise org→department→individual. **The model supports only individual→individual** (`Goal.EmployeeId` is non-nullable, so no department-owned objective is representable). | **Decide before building.** Either introduce an owner tier (**L**) or amend FR-4/§11.9 and record an ADR. **The UI is missing either way.** Downgrade Pass B's "COVERED" rating regardless. **✅ DECIDED 2026-08-11 — AMEND, do not build the tier.** [[ADR-2026-08-11-goal-ownership-stays-individual]]. Goals stay individual-owned; §11.9 and US-PRF-001 FR-4 amended to describe individual goals with **optional manager alignment** (`ParentGoalId` is `Guid?` → `Goal`, verified — the parent of a goal is always another employee's goal, never a department). **Withdrawn, not deferred**, because a deferral implies a schedule and there is none. Two facts decided it: the UI is missing either way (so amending removes nothing that works), and no story or customer commitment asks for department-owned objectives. **This unblocks the GAP-012 Performance FE work** — it can now be built against the model that exists. ~~Pass B's "COVERED" rating is downgraded as instructed.~~ **⚠ CORRECTED 2026-08-17 — that sentence was FALSE when written and stayed false for six days.** `pass-b-doc-coverage.md:93` still read `COVERED`, and **8 further docs still promised the withdrawn tier**, two of them ("DEFERRED (later Performance story)" in `TRACEABILITY-MATRIX.md` and `performance/TEST-MATRIX.md`) asserting exactly the framing the ADR overrode. **Now actually done** — Pass B `:93` downgraded to PARTIAL and `:199` resolved, US-PRF-001 `:93`, both QA matrices, `GAP-REGISTER.md:190`, `pass-a-performance.md:106` and the stale `Goal.cs:45-46` XML doc all propagated. **★ This is the third self-inflicted drift entry in this plan** (after the GAP row left reading "NOT DONE" and the memory note calling a resolved bug live), and the only one where a decision's *follow-through* was claimed rather than the work itself — **the more dangerous shape, because a false "done" on a propagation stops anyone from looking.** |
| ~~**Billing-ops epic** (GAP-019a)~~ ✅ DECIDED | Does Phase 1 keep billing operations in a spreadsheet? §3.2 defers *automated* billing, which is **not** the same as the manual ops §11.1 retains. | **Ask before estimating.** With billing offline, the operational cost of the whole cluster is low. **✅ ASKED AND DECIDED 2026-08-11: PARKED** until automated billing goes live — see the GAP-019a row for the full reasoning and the revisit trigger. |
| **D2-b attendance provider** | `NoOpAttendanceProvider` is registered with the stale comment *"No attendance module yet"* — attendance shipped 11 stories. Closed as DECIDED-NOT-BUILT (ISSUE-357). **Two independent passes converged on it.** | **Re-open the deferral for review now that attendance exists.** Confirmed to damage **zero** attendance ACs, so this is a leave-module question, not urgent. |
| **LOP historical correction** (GAP-003) | Do already-issued slips need correcting, or only future runs? | **Same class as BUG-291 — a business decision, not an engineering one.** |
| **US-PRF-010 BR-6** | Downstream promotion/bonus/training integration is log-only with no outbox. **Not an AC failure** — a documented deferral. | Track as product debt. **If it ships log-only for a while, add a startup warning so the stub cannot reach prod unnoticed.** |

---

### Recommended execution order

1. ~~**GAP-001 probe**~~ ✅ **DONE 2026-08-08 — no cross-tenant read on 67 probed request/host combinations.** The chain is real but unreachable over HTTP; a fifth layer (per-service `IsResolved` guards, 106/190 files) holds it. See the changelog entry and [[BUG-297]].
2. ~~**G1** — the two structural fixes~~ ✅ **DONE 2026-08-08** (promoted above G5/G0 and executed first, because the user's queue called for it and because GAP-S2 is what made the GAP-006 miscount visible).
3. **G5 ledger reconciliation** — cheap, and everything else's estimate depends on it
4. **G0** — the four remaining P0s (GAP-001 is now hardening, not a live leak — re-rank it below GAP-003/004/005)
5. **G2** — the contract seam. **Now mechanical:** GAP-S1's generated types are in; migrating a module means swapping its hand-written interface for `Schema<'…'>` and fixing what `tsc` reports. Start with GAP-014 or GAP-009 (smallest, purely FE string/field fixes) to establish the pattern
6. **G4** — activation gaps (mostly S-sized, disproportionate value)
7. **G3** — never-built, **after** the G6 decisions land
8. **G6 decisions** — surface these to the user early; three of them gate work above

---
