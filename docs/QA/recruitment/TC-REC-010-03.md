---
id: TC-REC-010-03
user_story: US-REC-010
module: Recruitment
priority: high
type: integration
status: automated
automated_by: HRM.Tests/Unit/ApplicantConversionServiceTests.cs
partial_note: "2026-07-18 (PR #355, ISSUE-140): the ENABLED-branch account provisioning (FR-5/BR-7) is now IMPLEMENTED + covered by automated arms — toggle Tenant.AutoCreateUserOnHire on → passwordless User + Active UserTenant + Employee role + Employee.UserId link, existing-email reused, cross-tenant isolation asserted. STILL DEFERRED: the FR-9 welcome-email / credential-delivery steps (→ US-NTF-006). So the provisioning half is green; the email steps remain blocked on delivery."
exec_note: "2026-07-03: STILL BLOCKED — but NO LONGER by BUG-068 (that convert-500 is FIXED, PR #115 verified live: applicant 019f030e-… → employee EMP-0036, HTTP 201). Now blocked by ISSUE-140: the auto-create-user-account-on-hire feature (AC-3/FR-5/FR-9) is a Phase-1 DEFERRAL — there is NO tenant 'Auto-create user accounts on hire' setting, so this TC's ENABLED precondition is UNSATISFIABLE. Confirmed live: convert returned userAccountCreated=false; tenant-users search 'maya' = 0 (no User/UserTenant/Employee-role created); Serilog seam logs 'FR-9 welcome email DEFERRED (no email platform)' — no Hangfire enqueue/outbox. Cannot verify steps 2-6 until the setting + provisioning + welcome-email queue are implemented (see ISSUE-140). Employee-created (step 1) DOES pass."
created: 2026-06-15
---

# TC-REC-010-03: Auto-create user account enabled -> User + UserTenant + Employee role created; welcome email queued async (AC-3, FR-5, FR-9, BR-7, NFR-5)

## 1. Test Objective
Verify the optional account-provisioning branch (AC-3, FR-5, FR-9, BR-7, NFR-5): when the tenant setting "Auto-create user accounts on hire" is ENABLED, converting the applicant additionally creates, within the same atomic transaction, a `User` (email = applicant email), a `UserTenant` linking the user to the tenant, and a `user_tenant_role` assigning the DEFAULT "Employee" role; and enqueues a welcome/onboarding email via Hangfire (asynchronous, non-blocking — NFR-5). The welcome email carries login/credential-invite instructions (set-password link or social-login per assumptions). Email DELIVERY depends on the Notification System (S25) — this case asserts the queued outbox/Hangfire job + recipient + the account records; delivery is CONDITIONAL (see Note).

NOTE: Welcome-email delivery (FR-9) depends on the Notification System (S25) + tenant "Welcome/Onboarding" template + Hangfire (S28). If delivery is a LOG-ONLY/stub in this increment, assert the enqueued job/outbox row (tenant_id, recipient, template) — do NOT weaken to "no email". Onboarding-checklist generation (FR-8) is owned by the Onboarding module and is asserted only at the trigger seam in TC-REC-010-05.

## 2. Related Requirements
- User Story: US-REC-010
- Acceptance Criteria: AC-3
- Functional Requirements: FR-5 (User + UserTenant + Employee role if enabled), FR-9 (welcome email)
- Non-Functional Requirements: NFR-5 (email async via Hangfire, non-blocking)
- Business Rules: BR-7 (account creation optional, controlled by tenant setting)
- Dependencies: Authentication module (user/user_tenant/role), Notification System S25, Hangfire S28

## 3. Preconditions
- Tenant "acme"; setting "Auto-create user accounts on hire" = ENABLED.
- Applicant {acme_applicantId} Hired with Accepted offer; email maya.f@example.com NOT already a user in acme.
- Default "Employee" role exists in acme.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Auto-create setting | ENABLED | BR-7 |
| Applicant email | maya.f@example.com | -> user.email |
| Default role | Employee | assigned via user_tenant_role |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Convert {acme_applicantId} to an employee (complete the form, Create) | 200/201; employee created (per TC-REC-010-01). |
| 2 | Verify user account | A `User` with email maya.f@example.com is created (or invite-pending), scoped to acme (FR-5). |
| 3 | Verify tenant link + role | A `UserTenant` links the user to acme and a `user_tenant_role` assigns the DEFAULT "Employee" role (FR-5). |
| 4 | Verify password handling | No plaintext password stored; account is invite/set-password-link pending OR social-login per assumptions (Critical Rule #6). |
| 5 | Verify welcome email enqueued | A Hangfire job / outbox row is enqueued to send the welcome+onboarding email to maya.f@example.com with the tenant template; the API response returned WITHOUT awaiting send (NFR-5). |
| 6 | Verify async non-blocking | Conversion API latency does not include email send time; the job runs out-of-band (CONDITIONAL on S25 delivery; assert enqueue if delivery stubbed). |

## 6. Postconditions
- New employee + User + UserTenant + Employee-role created in acme; welcome email queued (not blocking); all rows tenant-scoped.

## 7. Test Category Tags
- [x] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
