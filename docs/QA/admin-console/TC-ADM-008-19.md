---
id: TC-ADM-008-19
user_story: US-ADM-008
module: Admin Console
priority: medium
type: integration
status: blocked
exec_note: "S1: needs-CODE (feature-not-built/RLS) — dev backlog."
created: 2026-06-17
---

# TC-ADM-008-19: [DEFERRED] Large export (> 10,000 records) via Hangfire job + emailed download link

## 1. Test Objective
Verify the ASYNC LARGE-EXPORT path of AC-4 / FR-5: for a filtered result set exceeding 10,000 records, the export is processed as a Hangfire BACKGROUND job (rather than synchronously) and the resulting download link is EMAILED to the requesting user.

DEFERRED — status: blocked. No email service and no blob/object storage are wired for export delivery, so the async >10k path cannot run end-to-end today. The SYNCHRONOUS small-export path (immediate download, filter-respecting, masked, self-audited "AuditLog.Export") IS real and covered run-green in TC-ADM-008-11. (Test Hints — Large export.)

## 2. Related Requirements
- User Story: US-ADM-008
- Acceptance Criteria: AC-4 (large datasets via email link)
- Functional Requirements: FR-5 (> 10,000 rows -> Hangfire job + emailed link)

## 3. Preconditions
- (Deferred prerequisites) Email service + blob storage for the export artifact are operational.
- Acme `audit_log` has > 10,000 rows matching the active filter.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| matching rows | > 10,000 | triggers async path |
| threshold | 10,000 | sync below, async above |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | (When implemented) Trigger export with > 10,000 matching rows | Export is enqueued as a Hangfire background job; the request returns "processing" rather than a synchronous file. |
| 2 | Job completes | The masked, filter-respecting export file is generated and stored. |
| 3 | Confirm email delivery | A download link is emailed to the requesting user. |
| 4 | Confirm self-audit | An "AuditLog.Export" row is still written (BR-4) for the async export. |
| 5 | Until implemented | Expected behavior: "Not available — async large-export requires email + blob storage; synchronous small-export is real (TC-ADM-008-11)." |

## 6. Postconditions
- Deferred; no email/blob path exercised today.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [x] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
