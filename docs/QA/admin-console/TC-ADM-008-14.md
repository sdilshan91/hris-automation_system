---
id: TC-ADM-008-14
user_story: US-ADM-008
module: Admin Console
priority: high
type: integration
status: blocked
exec_note: "2026-07-01 BLOCKED: AuditLogPurgeJob retention test needs (a) seeding audit_log rows dated 91/90/0 days old and (b) force-running the purge job + verifying row survival + tenant-scoping. No API trigger for the job and row seeding/verification need DB access (psql denied). Job is registered per US-ADM-008. Re-run in a DB-seedable + Hangfire-trigger harness."
created: 2026-06-17
---

# TC-ADM-008-14: Retention purge job deletes rows older than the window, keeps recent rows

## 1. Test Objective
Verify FR-6 / BR-5: the `AuditLogPurgeJob` deletes `audit_log` rows older than the tenant's `AuditLogRetentionDays` window and leaves rows within the window intact. The purge is tenant-aware (only the tenant whose window is exceeded loses its old rows). (Test Hints — Retention: set retention to 90 days; a record dated 91 days ago is deleted, recent records intact.)

## 2. Related Requirements
- User Story: US-ADM-008
- Functional Requirements: FR-6 (retention purge by `AuditLogRetentionDays`)
- Business Rules: BR-5 (retention plan-governed)

## 3. Preconditions
- Acme `Tenant.AuditLogRetentionDays = 90`.
- Acme `audit_log` has: row R_old dated 91 days ago; row R_edge dated exactly 90 days ago; row R_recent dated today.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| AuditLogRetentionDays | 90 | |
| R_old | 91 days old | expected deleted |
| R_edge | 90 days old | boundary — retained (within window) |
| R_recent | today | retained |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Run `AuditLogPurgeJob` for Acme | Job completes successfully. |
| 2 | Query for R_old | Deleted (older than the 90-day window). |
| 3 | Query for R_edge | Retained (exactly at the window boundary). |
| 4 | Query for R_recent | Retained. |
| 5 | Confirm tenant scoping | Beta's audit rows are untouched regardless of Beta's own window. |

## 6. Postconditions
- Only rows strictly older than the window are removed for Acme; recent/edge rows intact.

## 7. Test Category Tags
- [ ] Happy path
- [x] Negative test
- [x] Boundary test
- [x] Security test
- [x] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
