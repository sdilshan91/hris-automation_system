# Deferred Follow-ups Register

**Purpose.** A single, durable home for "**verify or do this later**" items — the deferrals, known
residual risks, needs-infra test gaps, and structural clean-ups that surface *while shipping something
else*. Previously these lived only as inline `Follow-up (LOW)` notes buried in `TEST-FINDINGS.md`
RESOLVED blocks, where they were easy to lose. This register indexes them so they can actually be
picked up.

**Convention (how this stays useful).**
- When a fix/PR **defers** something — a documented deferral, a residual risk, a "would be nice / more
  correct" refactor, an env/infra-gated test gap, or a change that would make a whole **bug class**
  impossible — add a row here **in addition to** the note in the source finding. Don't rely on the
  inline note alone.
- Each row links back to its source finding/PR so the full context is one hop away.
- When one is picked up, move it to **Done** (or delete it) and flip the source finding if relevant.
- Skim this register when starting a work session or planning a batch — some items unblock over time
  (e.g. an item blocked by another bug becomes actionable once that bug lands).

Status legend: **OPEN** · **BLOCKED** (waiting on another item) · **DONE**.

---

## Open

| # | Item | Source | Why deferred / value | Effort |
|---|------|--------|----------------------|--------|
| DF-1 | ✅ **DONE (#416, real-PG on-data + expression conversion arms, +TC-ONB-004-14; 2 auditors CONNECTED/AUTHENTIC)** **Remodel date-only `timestamptz` columns to `date`** — `onboarding_checklist_instance.start_date` + `onboarding_task_instance.due_date`, `asset.issue_date`, `exit_interview.interview_date`, `offboarding_instance.last_working_day` + `offboarding_task_instance.due_date`. They hold date-only values; making them `date` (and the properties `DateOnly`) makes the whole **`.Date`→timestamptz Kind bug class impossible**, not just patched. Needs a migration + DTO/reader touches. | [[BUG-289]] · [[BUG-290]] (PR #344/#345) | Structural — kills the bug class the `SpecifyKind(Utc)` patches only guard site-by-site. Medium risk (existing-data migration + read-path changes). | M |
| DF-3 | ✅ **DONE (#400, real-PG, TC-CHR-205 + TC-LV-002-03)** **Testcontainers arms for the InMemory-only money paths** — the storage-quota `SumAsync`/plan-join (BUG-114) and the entitlement recalc + `GetLedgerBalanceAsync` ordering (BUG-118). Both proven only on InMemory (the repo's standing InMemory-masks-Postgres class). | [[BUG-114]] (#332) · [[BUG-118]] (#333) | Defence-in-depth on money paths; low SQL-translation risk but currently unproven on Postgres. | M |
| DF-4 | ✅ **DONE (#411, +TC-LV-268)** **Leave-entitlement recalc enqueue durability** (recon corrected: this is `LeaveEntitlementService.UpdateRuleAsync`, NOT onboarding) — new daily `LeaveEntitlementReconcileJob` (`30 2 * * *`) re-runs `RecalculateEntitlementsAsync(currentYear, null)` per active/trial tenant via `ITenantJobRunner`, unconditionally; a dropped enqueue self-heals within one cadence (idempotent → no double-count). Fast-path enqueue untouched. Systemic Payroll/Onboarding siblings filed [[DF-58]]. — enqueues the recalc *after* the commit (best-effort, `_recalcScheduler?.Enqueue`, matches `PayrollRunService`). A Hangfire-storage outage between commit and enqueue silently skips the recalc → that leaveType's employees never converge. **Enqueue-in-transaction is infeasible** (Hangfire runs on a different connection/role under the RLS design). Since `RecalculateEntitlementsAsync` is idempotent, exactly-once is unneeded; **decision (user, 2026-07-21): a reconciliation SWEEP** backstops the fast-path enqueue. | [[BUG-118]] (#333) auditor note | Durability on AC-5; rare window, but it's the exact bug-class BUG-118 fixed. | M–L (→ S–M as a sweep) |
| DF-5 | ✅ **DONE (#395, +TC-NTF-002-14)** **BR-6 template variant cap → plan-configurable** — `MaxLanguageVariants` was a `const 2`; now wired to `PlanLimitResolver` via a new key `max_template_language_variants` + nullable columns on `SubscriptionPlan` + `Tenant` (CLI migration `Plan_MaxTemplateLanguageVariants`) + `DbInitializer` default-2 seed. `ResolveMaxLanguageVariantsAsync` in `NotificationTemplateService` resolves override>plan>tenant>2 (mirrors `WorkflowService.MaxWorkflows`); `422 variant_limit_reached` uses the resolved cap. Enforcer WIRED; 2 mutation-resistant arms (override raises→3rd allowed, plan lowers→2nd rejected). | [[BUG-122]] · DF-5 (#337) | Matches the plan-limit pattern used elsewhere; low urgency. | S–M |
| DF-6 | ✅ **DONE (#404, +TC-PRF-007-16)** **Dashboard PDF: embed the tenant logo image** — the branded PDF uses the tenant `PrimaryColor` band; the `LogoUrl` image isn't embedded (needs an HTTP fetch + image bytes in the renderer). | [[ISSUE-126]] (#340) | Completes the FR-8 branding; color branding already ships. | M |
| DF-7 | ✅ **DONE (#413, +TC-REC-011)** **Distributed (Redis) magic-link limiter** — the per-IP applicant-portal-link throttle (`ApplicantPortalTokenService.IssueAsync`) extracted into an `IPortalLinkIpRateLimiter` seam: `RedisPortalLinkIpRateLimiter` (fixed-window INCR+EXPIRE, per-(tenant,IP), fail-open) when Redis is configured, else the original sliding-window `DbCountPortalLinkIpRateLimiter` (kept — correct + shared). Same 429; per-email guard untouched; limits → `Recruitment:PortalLink` config. Fallback-table index filed [[DF-59]]. | [[ISSUE-130]] (#341) | The DB-count throttle already gives cross-instance defence-in-depth. | M |
| DF-8 | ✅ **DONE (#410, +TC-CHR-338)** **Reporting-chain breadcrumb** — new `GET /api/v1/tenant/employees/{id}/reporting-chain` returns the ascending chain (employee → manager → … → root), mirroring the `direct-reports` slice: one projected tenant-scoped query + batched job-title lookup (no N+1/CTE), visited-set + `MaxChainDepth` cycle guard, truncates at a missing/soft-deleted/cross-tenant rung, 404 unknown. 7 InMemory arms incl. cross-tenant isolation. Full-HTTP-route regression test filed [[DF-57]]. | [[ISSUE-218]] (#338) | Larger; only needed if the breadcrumb wants a first-class endpoint. | M (actually S–M — mirror of direct-reports) |
| DF-9 | ✅ **DONE (#412, +TC-CHR-339)** **Custom-fields FE render pass** — running-UI verification found a REAL bug, not just a clean pass: the `options` wire-contract drift. BE ships/binds `options` as a JSON-encoded **string** (`string?`) both directions; the FE model typed `string[]` and never parsed → dropdown/multi_select options rendered as the string's **characters** (read) + admin create POSTed an array the BE rejects (write). Fixed with single-boundary normalization in `CustomFieldService` (parse string→array on read, stringify array→string on write); consumers unchanged. Verified: 15/15 Karma (6 DF-9 arms), 2 auditors WIRED/AUTHENTIC, **live browser** (e2e tenant, seeded dropdown → renders real S/M/L/XL, was garbage pre-fix). | [[ISSUE-206]] (#339) | Env-gated (needs the running stack). | S |
| DF-10 | ✅ **DONE (#396, +TC-ONB-002-14)** **Controller idempotency-key precedence test (header vs body)** — `OnboardingChecklistsController.Assign` resolves `Idempotency-Key` header-then-body; now pinned by `OnboardingChecklistsControllerIdempotencyTests` (header wins; blank/whitespace header → body key; header-only flows through) via a captured MediatR command. A precedence-swap mutant now fails. | [[ISSUE-315]] · DF-10 | Cheap controller arm. | S |
| DF-29 | ✅ **DONE (#373)** **Branding favicon + tenant-switcher logos still raw path** — ISSUE-204 (#370) fixed logo/email-logo via an anonymous serve endpoint, but `FaviconUrl` (settings DTO) and the tenant-switcher membership list (`GetMyTenants` → `TenantMembershipDto.LogoUrl`) still emit the raw `/{tenantId}/…` storage path → 404s. Favicon needs a `branding/favicon` serve endpoint; the switcher is CROSS-tenant so it needs a tenant-qualified public route (e.g. `/api/v1/tenant/{subdomain}/branding/logo`) — a larger design. | [[ISSUE-204]] (PR #370) · `@integration-enforcer` | Same 404 class, secondary surfaces; own-tenant GUID only (not a cross-tenant leak). | S–M |
| DF-30 | ✅ **DONE (#381, +7 TCs)** **LocalFileStorage `..` path guard** — `OpenReadAsync`/`UploadAsync`/`DeleteAsync` don't canonicalize/assert the resolved path stays under basePath before `Path.Combine`. Not exploitable today (relativePath is always server-derived), but defense-in-depth is absent. | ISSUE-204 audit (PR #370) · `@integration-enforcer` | Hardening; no reachable vector currently. | S |
| DF-32 | ✅ **DONE (#381)** **Branding UPLOAD response returns the raw storage path** — `TenantSettingsController.UploadBrandingAsync` returns `BrandingUploadResultDto.Url` as the raw `/{tenantId}/branding/…` path, not a servable URL (the one branding surface DF-29 didn't touch). If the FE renders the POST result directly it 404s until a settings refetch. Route it through the `ToServableBranding`-equivalent, or have the FE refetch settings after upload. | DF-29 audit (PR #373) · `@integration-enforcer` | Own-tenant admin POST; same 404 class as DF-29, last surface. FE may already refetch. | S |
| DF-33 | ✅ **DONE (#385, +TC-ATT-161)** **Chronic-lateness escalation notification (US-ATT-008 FR-7, ISSUE-087)** — the per-late notification is wired (`attendance_late`), but there is no dispatch when `lateCount` crosses `LatePolicy.ChronicThreshold`. This is a NEW story: net-new catalog event + placeholders (with the "declare above `_byKey`" ordering gotcha), a new `NotifyChronicLatenessAsync` interface method, a trigger site in the clock-in path (compute MTD count vs threshold), AND a product decision on RECIPIENTS (employee vs manager vs HR pool) + escalation copy. Not a low-tier wiring add. | [[ISSUE-087]] (PR #374 recon) | Product-flavored (recipients/threshold/copy) + net-new event; scope-fenced out of the NTF-006 seam batch. | L (story) |
| DF-34 | ✅ **DONE (#382, +fixed ISSUE-322 data-loss)** **FE org-profile fields for `PayslipFooterDisclaimer` + `PayrollFromEmail`** — both are tenant settings exposed on `OrgProfileDto`/`UpdateOrgProfileRequest` and round-trip through the API, but the Angular org-profile settings form has NO field to view/edit either (ISSUE-159 shipped BE-only; ISSUE-229 same). A tenant admin can't configure the payslip footer or the payslip sender "From" via UI. Add both fields to the settings form. | [[ISSUE-159]] (#371) · [[ISSUE-229]] (#374) · `@backend-dev` | API-complete but not user-reachable; one small FE follow-up covers both. | S (FE) |
| DF-39 | ✅ **DONE (#386)** **Backend for employee Education / Work-History / Dependents (ISSUE-321)** — net-new entities + migrations + endpoints + `UpdateEmployeeProfileRequest` sections (or dedicated sub-entity CRUD), then re-enable the 3 FE forms (currently read-only after #380). | [[ISSUE-321]] (found fixing DF-36 / #380) | Net-new feature; 3 profile sections non-functional. | M–L |
| DF-38 | ✅ **DONE (#386)** **Profile-edit field-level gaps (ISSUE-320)** — convert Employment dept/title/type/status to id/enum `<select>`s (stop free-text→Guid/enum drops + 400 risk); add address-detail fields (city/state/postal/country) to `ContactInfoUpdate`+`Employee` OR drop them from the FE; decide if dateOfJoining is editable; parse the `customFields` JSON on the profile read view. | [[ISSUE-320]] (#380) · `@frontend-dev` | Visible fields silently no-op after the DF-36 route fix; needs FE selectors + a small BE DTO decision. | M |
| DF-40 | ✅ **DONE (#384)** **Account-lockout email never delivered — `LockoutNotificationService` is a log-only stub not migrated onto NTF-006 (US-AUTH-001)** — the lockout trigger correctly enqueues `Enqueue<ILockoutNotificationService>`, but the registered impl (`DependencyInjection.cs:138`) only writes a `[LOCKOUT-EMAIL-STUB]` log line and still carries `TODO(US-NTF-006): hand content to the real IEmailSender`. #374 improved the *content* (tenant-name, ISSUE-063) but not delivery. Route it through `INotificationDispatcher`/`IEmailSender` like the other `Real*` services. Related dead wrapper: [[DF-35]]. | US-NTF-006 gap-row reconciliation (2026-07-19) · `@integration-enforcer`-class Explore finding | Security-adjacent: a locked-out user is never notified; only the log path is reachable. | S–M |
| DF-41 | ✅ **DONE (#384)** **Applicant status-tracking magic-link email is a log-only seam (US-REC-008 FR-7)** — `ApplicantPortalTokenService.RequestLinkAsync` mints + persists the portal token, but the delivering email is a documented log-only seam (`ApplicantPortalTokenService.cs:172` "link would be emailed (FR-7, log-only seam)") — no `IEmailSender`/`INotificationDispatcher` call, so the applicant never receives the tracking link. Wire the real sender. | US-NTF-006 gap-row reconciliation (2026-07-19) · Explore trigger audit | Story-purpose gap: FR-7 magic-link delivery non-functional. | S–M |
| DF-42 | ✅ **DONE (#384)** **Offer email has no candidate magic-link embedded (US-REC-007)** — the offer-sent email + inline PDF are real-delivered (`RealRecruitmentNotificationService.SendOfferSentWithPdfAsync`), but no `IApplicantPortalTokenService` token is issued/embedded at offer-send, so the candidate gets no self-service accept/track link (payload builder carries no portal URL). Issue a portal token at offer-send and embed it. | US-NTF-006 gap-row reconciliation (2026-07-19) · Explore trigger audit | Story-AC gap; email+PDF work, the magic-link leg is absent. | S–M |
| DF-37 | ✅ **DONE (ISSUE-280 part, #423): report BASIC bucket keyed on Code + real-PG round-trip; +TC-PAY-011-15/16; 2 auditors CONNECTED/AUTHENTIC. ISSUE-295 residuals split to [[DF-62]] (product-gated).** **Payroll-model pass: key salary components by Code, not Name (ISSUE-280) + BUG-079 residual encashment clauses (ISSUE-295)** — (280) the BASIC component is identified by Name and `PayrollSlipLine` drops the component Code, so a rename breaks encashment/statutory basis lookups; carry Code onto the slip line and key on it. (295) residual encashment-BASIC-basis / carry-forward-parity clauses from BUG-079. Both are a durable refactor of how components are keyed + valued — do them together as one focused payroll pass with money-path Postgres verification, not piecemeal. | [[ISSUE-280]] · [[ISSUE-295]] (user decision 2026-07-19 — defer) | Cross-cutting money-model change; a half-done keying change risks pay math. Best as one scoped pass. | M–L |
| DF-36 | ✅ **DONE (#380)** **Fix the FE profile-section-edit route mismatch (ISSUE-319)** — `EmployeeService.updateProfileSection` PATCHes `{id}/sections/{section}` (nonexistent) instead of the real `PATCH {id}/profile` with a wrapped `UpdateEmployeeProfileRequest`. Blocks ALL inline profile-section edits (incl. ISSUE-293 National ID edit). Align the FE call (map each section→request property, carry xmin→RowVersion) OR add the `sections/{section}` BE route; verify every section end-to-end. | [[ISSUE-319]] (found in #377 / ISSUE-293 recon) · `@integration-enforcer` (HIGH) | Deferred from ISSUE-293 to keep that PR surgical; it's a broad profile-edit-contract fix needing its own verification. | M |
| DF-35 | ✅ **DONE (#381, deleted)** **Dead `SendLockoutNotificationJob` wrapper** — registered in DI (`Program.cs`) but never enqueued; the live lockout path enqueues `Enqueue<ILockoutNotificationService>` directly, so the job class is orphaned. #374 kept its signature consistent (added `tenantName`) but did not remove it (verify-before-remove + scope). Delete the class + its registration, OR confirm an intended future enqueue path. | DF-29/NTF-006 audit (PR #374) · `@integration-enforcer` | Pre-existing orphan; harmless but dead. | S |
| DF-28 | **ISSUE-204 BE dead logo URL** — ✅ DONE (#370, logo/email-logo). Residuals → DF-29. — the BE returns a relative `/{tenantId}/branding/logo.png` that no route serves, so the brand `<img>` 404s on every page. FE now hides the broken image (#368), but the 404 console noise persists until the BE returns null when no logo is uploaded (or an absolute served-asset URL). | [[ISSUE-204]] (PR #368) · `@frontend-dev` | Console hygiene / brand mark; FE visual already mitigated. | S (BE) |
| DF-27 | ✅ **DONE (#369)** **A4 auth FE follow-ups** — the BE now emits (a) a new SSO error code `tenant_required` (ISSUE-220), (b) `recoveryCodesRemaining`/`shouldRegenerateRecoveryCodes` on the login response (ISSUE-241), and (c) `POST /api/v1/auth/change-password` (ISSUE-248) — none consumed by the Angular FE yet. Map `tenant_required` to a distinct message; surface the regenerate prompt; add a change-password form. | [[ISSUE-220]] [[ISSUE-241]] [[ISSUE-248]] (PR #367) | Additive/backward-compatible BE; FE UX. | M (FE) |
| DF-25 | ✅ **DONE (#381; +critical AddMemoryCache DI fix)** **Session idle-timeout per-request read → cache** — ISSUE-061 (#365) clamps the activity debounce using the tenant's `IdleTimeoutMinutes`, read via an indexed PK SELECT on EVERY authenticated request (the debounce was meant to minimize DB work). Memoize per-tenant. | [[ISSUE-061]] (PR #365) · `@integration-enforcer` | Hot-path perf; correctness is sound. | S |
| DF-26 | ✅ **DONE (#369)** **Vacancy-create FE must send headcount** — ISSUE-096 (#365) made `headcount` required (400 on omitted). The Angular vacancy form must always send it (no more silent default 1). | [[ISSUE-096]] (PR #365) | Additive BE contract; FE form check. | S (FE) |
| DF-24 | ✅ **DONE (#369)** **Bulk-assign-manager FE display** — ISSUE-027(b) (#362) split the status text out of `employeeName` into a new `message` field on `BulkAssignManagerItemResult`. The Angular bulk-assign result UI must read `message` for the status and `employeeName` for the (now correct) name. | [[ISSUE-027]] (PR #362) | Additive BE contract change; FE display follow-up. | S (FE) |
| DF-18 | ✅ **DONE (#387)** **ISSUE-099 within-tenant goal-read authz** — `GoalService.GetByIdAsync` is tenant-scoped (cross-tenant IDOR fixed, #358) but a `Performance.SetGoal.Team` manager can read ANY in-tenant goal by id, not just their reports'. Decision (user 2026-07-19): limit to reports — added `AuthorizeForEmployeeAsync(goal.EmployeeId)` + a self-read branch (owner reads own goal). | [[ISSUE-099]] (PR #358→#387) | Finer authz; the reported cross-tenant IDOR is fully closed. | S |
| DF-19 | ✅ **DONE (#427, 2 auditors CONNECTED/AUTHENTIC). ISSUE-045 pool-aware carry-forward restoration.** Persisted the BR-4 FIFO split: `LeavePool` enum + `LeaveLedger.Pool`/`CarryForwardTrackingId` + `LeaveCarryForwardTracking.ConsumedDays` (migration `20260722005029_LeavePoolAllocation`, backfill converges to prior derived `MIN(carried, used-in-to-year)` → existing buckets byte-identical). Cancel now restores each pool to its bucket (carried expiry PRESERVED; terminally-expired bucket → accrual fallback); `ProcessExpiryAsync` reads the persisted `ConsumedDays` (`remaining = carried − consumed − expired`); `GetUsedInYearAsync` retired. **CRITICAL caught + fixed in review:** the first cut bumped `ConsumedDays` only on approval → encashment/LOP consumption invisible to expiry → over-forfeit. Fix = extracted **one shared `PooledLeaveLedger.AppendDeductionAsync`** (tags pool + bumps `ConsumedDays` + `Undo` for rollback) and routed **all 4** consumption paths through it (approval, HR encashment w/ adjustment-reject rollback, compulsory-leave, LOP-conversion) → backfill↔runtime parity. Tests: `LeavePoolAwareCarryForwardTests` (approve-split→cancel-restore→expiry-forfeit lifecycle; expired→accrual fallback; shared-helper bump→no over-forfeit; `Undo`); 617 Leave + 64 LOP/Encash unit arms green (Testcontainers PG arms defer to Docker run). Residual → [[DF-65]]. | [[ISSUE-045]] (A2 defer 2026-07-18; unblocked 2026-07-19; DONE 2026-07-22) | Feature-sized; documented TODO. `LeaveCarryForwardTracking` has no `Pool` field yet — that + FIFO-aware restoration is the work. | L |
| DF-20 | ✅ **DONE (#403, +TC-LV-203/UpdateOrgProfileValidatorTests)** **ISSUE-044 tenant-configurable leave cancellation window** — hardcoded `const=0`; FR-7 wants a per-tenant setting (column + API + read). | [[ISSUE-044]] (A2 defer 2026-07-18) | New config feature, not a LOW fix. | M |
| DF-21 | ✅ **DONE (#408, +TC-LV-267)** **ISSUE-039 my-balance N+1** — per-leave-type entitlement-engine loop in `GetMyBalancesAsync` swapped for ONE `ComputeProratedEntitlementsBatchAsync` call (the batched resolver already existed from BUG-124; 2 queries total). Missing-pair → 0 preserves the old failure/probation fallback; orphaned `ResolveEntitlementAsync` deleted; no-N+1 guard test added. | [[ISSUE-039]] (A2 defer 2026-07-18) | Perf; >200ms NFR-1. | M (was: real refactor; actually S — method pre-existed) |
| DF-22 | ✅ **DONE (#409, +TC-ATT-162)** **ISSUE-309 per-location policy on tenant-wide sweeps** — auto-clock-out + monthly-summary now batch-resolve the effective `AttendanceSettings` PER LOCATION via the pre-existing `AttendancePolicyResolver.LoadAllAsync`/`For` (override → tenant default → code default); monthly-summary's per-employee tenant-default read (an N+1) is also killed. Absenteeism report **kept tenant-wide** by scope. Shift-shadows-override edge filed [[DF-56]]. | [[ISSUE-309]] (A2 defer 2026-07-18) | Larger refactor across 3 sweeps. | M–L (actually S–M — resolver pre-existed) |
| DF-23 | ✅ **DONE (#425, child-collection model; +TC-ATT-005-16/17/18; 2 auditors CONNECTED/AUTHENTIC; real-PG arm caught+fixed a full-replace `Clear()` defect → RemoveRange). BE-only; admin list-UI is a FE follow-up.** **ISSUE-068 multi-location geofence (FR-3)** — geofence supports a single center; multi-location needs an allowed-locations collection. (The permission-name-drift half is being fixed now in the Attendance A2 batch.) | [[ISSUE-068]] (A2 defer 2026-07-18) | Unbuilt feature; permission half done separately. | M |
| DF-17 | ✅ **DONE (#388)** **Org-tree nested-children payload is unused by the FE** — the GET endpoint nests `children` to the requested depth, but `OrgTreeService` (post ISSUE-207 fix) projects only the flat `.nodes` and the tree builds from `parentId` links + lazy-loads on expand. So each expand does a round-trip even though depth-2 data was already delivered. Either the FE should flatten+use the nested `children`, or the BE should return a flat projection. | [[ISSUE-207]] (PR #357) | Minor efficiency/UX; the tree is fully functional via lazy-load today. | S–M |
| DF-16 | ✅ **DONE (#400, real-PG, +TC-PAY-008-16)** **Postgres + cross-tenant arms for `PayrollApprovalStepConfig`** — the new step-config CRUD (atomic RemoveRange+re-add replace), the `UserHoldsRoleAsync` role-join, and a tenant-A/tenant-B isolation arm are InMemory-only. Isolation is enforcer-verified at the code level (global filter + tenant-scoped joins + dormant RLS policy), so this is coverage-hardening, not a suspected defect. | [[BUG-076]] (PR pending) · `@test-authenticator` | Belt-and-suspenders on a financial separation-of-duties control; happy path + guards are well-covered on InMemory. | M |
| DF-15 | ✅ **DONE (#400, real-PG, +TC-REC-010-15)** **Testcontainers arm for hire-provisioning retry-detach** — `ApplicantConversionService` guards `BeginTransactionAsync` behind `IsRelational()`, so the new provisioned User/UserTenant/UserTenantRole rollback-detach (BUG-264 retry class) is only reachable on real Postgres and is InMemory-unexercised. Add a forced-retry Testcontainers arm asserting a transient retry re-inserts no duplicate account rows. | [[ISSUE-140]] (PR pending) · `@test-authenticator` | Defence-in-depth on the retry edge; happy-path provisioning is well-covered on InMemory. | M |
| DF-13 | ✅ **DONE (#384 — duplicate of DF-41)** **Magic-link email delivery** — the "applicant-portal token minted but link never emailed (FR-7, no live caller)" seam is the *same* seam as DF-41, wired in #384 (`ApplicantPortalTokenService.RequestLinkAsync` → `applicant_portal_link` dispatch; live caller `POST /api/v1/careers/portal/request-link`). The "BLOCKED on US-NTF-006" premise was stale — that delivery infra was already live. Close as a duplicate. | [[ISSUE-132]] · [[DF-41]] (verified 2026-07-19) | — | — (done) |
| DF-14 | ✅ **DONE (#394, +TC-PAY-008-14)** **Payroll approval — "Pending Approvals" read-endpoint** — the notification half already shipped; the missing read-side is now built. Shipped `GET /api/v1/payroll/approval/pending` (`[RequirePermission("Payroll.Approve")]`) → `GetPendingApprovalsAsync` returns a run **iff approving it would not 403** — mirroring ApproveAsync's three gates via a **shared** `HasCallerAlreadyApprovedAsync` predicate (step-role `UserHoldsRoleAsync`, maker-checker + small-team `CountEligibleApproversAsync`, distinct-approver), batched submitter-name resolution (Employee→User, no N+1), tenant-scoped. FE `listPendingApprovals()` repointed off the ignored `?status=` param. 12 xUnit + 3 FE arms; enforcer WIRED, authenticator 100%. | [[ISSUE-172]] · DF-14 (delivery verified live 2026-07-19; read-endpoint 2026-07-20) | Delivery + audit trail already worked; this added the read-side queue endpoint. | S |
| DF-12 | ✅ **DONE (#369)** (filed as **[[ISSUE-317]]**) **FE has no `Unknown` badge for a tolerated corrupt row** — `applicant.models.ts` / `pipeline.models.ts` (`STAGE_BADGE`) / `payroll-run.models.ts` (`RUN_STATUS_BADGE`/`LABELS`) are `Record<Enum,string>` with no `Unknown` key, so a row the BE now tolerates renders a **blank** badge (undefined key lookup) rather than a visible "unknown/corrupt" affordance. Graceful degradation, no crash — but silent. | [[ISSUE-231]] (PR #348) · `@integration-enforcer` | Decision: add an `Unknown` badge (surface the bad row so someone fixes the data) vs accept blank. | S |
| DF-31 | ✅ **DONE (#405, +TC-PAY-004-05)** **ISSUE-162 per-employee payslip retry (FR-8)** — `PayslipsController` exposes only run-level generate/regenerate; `RegenerateAsync` overwrites ALL slips in the run. A per-employee "retry this one failed slip" needs a single-employee render path through `PayslipBatchRenderer` + a new command/handler/route + tests. Deferred from the clear-engineering batch (PR #371) as story-sized, not a small clean add. (Note: the payslip *email* resend already supports per-employee retry via `POST runs/{runId}/payslips/resend-emails` with `employeeIds`/`onlyFailed`; this DF is about PDF **generation** retry.) | [[ISSUE-162]] (PR #371 assessed 2026-07-18) | The whole run can already be regenerated; only the granular single-slip retry is missing. | M (story) |
| DF-43 | ✅ **DONE (#398, +TC-ATT-001-14)** **`TimeProvider` clock seam for `AttendanceService`** — the five `DateTime.UtcNow` sites (clock-in/out timestamps, audit `CreatedAt`, idempotency `CreatedAt`/`ExpiresAt`) now read from an injected `TimeProvider` (trailing-optional ctor param → `TimeProvider.System` default, mirroring `LeaveDashboardService`/`NotificationPreferenceService`; no DI registration, no behaviour change). Private `UtcNow => _timeProvider.GetUtcNow().UtcDateTime`. Full 215-test Attendance suite green + a determinism arm (fixed `FakeTimeProvider` → fixed `ClockIn`). **Unblocks DF-44** (deterministic clock for the chronic-lateness real-PG arm). | DF-33 build · DF-43 · `@backend-dev` OOL | Testability/determinism; feature works. Behaviour-neutral seam. | M |
| DF-44 | ✅ **DONE (#399, real-PG, TC-ATT-161)** **Real-Postgres arm + DI-resolution assert for the DF-33 chronic-lateness crossing** — the crossing count (`CountLateDaysInMonthAsync` month-range query) + the local-date same-day guard (`HasOtherLateLogOnLocalDateAsync`, UTC-window scan + **client-side** `TenantClock.LocalDateOf` compare) are proven only InMemory-through-EF (the standing InMemory-masks-Postgres class). Bounds use explicit `DateTimeKind.Utc` and the local-date compare runs after `ToListAsync` (verified PG-safe), so this is defence-in-depth, not a suspected defect. Also add a composition-root assert that DI resolves a **non-null** `ILateEarlyService` into `AttendanceService` (the optional ctor param would silently no-op the whole feature if the registration were ever dropped — config-masked class, cf. [[imemorycache-not-registered-under-redis]]). Related basis note: the month count buckets on **UTC** day while the guard compares **tenant-local** date — consistent with the existing report/self-score basis; only matters for far-offset tenants. | DF-33 build (PR pending) · `@test-authenticator` · `@integration-enforcer` | Coverage-hardening on a money-adjacent HR-escalation path; happy path + crossing guards well-covered InMemory. | M |
| DF-45 | ✅ **DONE (#399, real-PG, TC-CHR-335)** **Real-Postgres arm for the DF-39 employee sub-tables** — `EmployeeEducation`/`EmployeeWorkHistory`/`EmployeeDependent` full-replace (`RemoveRange` + re-`Add` reusing `input.Id` → **delete-then-reinsert-same-PK**, now live from FE traffic since #386 sends the row `id`), the migration `AddEmployeeProfileExtensions`, the `DateOnly`→`date` round-trip, and cross-tenant reads on the 3 tables are all InMemory-only. Add a Testcontainers arm proving the crossing on real PG (mirrors the shipped `EmergencyContact` pattern, so low risk). | DF-38/39 (#386) · `@integration-enforcer` · `@test-authenticator` | Coverage-hardening; the delete/reinsert-same-PK ordering is exactly the InMemory-masks-Postgres class. | M |
| DF-46 | ✅ **DONE (#393, +TC-PRF-001-15)** **goal-set re-open / unlock flow (BUG-056 follow-up)** — a finalized (locked) goal set had NO re-open path. **Decision (2026-07-20):** HR (`SetGoal.All`) **or** the employee's direct/finalizing manager (`SetGoal.Team`) may re-open, with a **MANDATORY audit reason**, resetting the set `Finalized`→`Acknowledged`. Shipped: `GoalService.ReopenGoalsAsync` (reuses `AuthorizeForEmployeeAsync` + `AddGoalAudit`; 409 `goals_not_finalized` guard; only Finalized goals transition), `ReopenGoalsCommand`/handler, `ReopenGoalsRequest`, `ReopenGoalsValidator` (reason `NotEmpty`+`≤1000`, rejects whitespace), `POST goals/reopen` `[RequirePermission(SetGoal.Team\|All)]`, FE re-open button in the locked banner + `reopenGoals()` service. 7 xUnit + 7 Karma arms green. | BUG-056 · DF-46 · `@integration-enforcer` · `@backend-dev` | Finalize+lock ships and works; only the undo was missing. Security-adjacent (who can unlock a signed-off set). | S–M (after decision) |
| DF-47 | ✅ **DONE (#399, real-PG, TC-PRF-001-14/15)** **Real-Postgres / HTTP round-trip arm for goal finalize + re-open (BUG-056, DF-46)** — `FinalizeGoalsAsync`/`ReopenGoalsAsync`, the four write-guards, and the `"Finalized"`/`"Acknowledged"` enum-string persistence are proven only via InMemory-through-real-EF; no Testcontainers HTTP test exercises `POST /api/v1/tenant/performance/goals/finalize` **or `…/goals/reopen`** end-to-end (incl. the `ValidationBehavior` rejecting an empty re-open `Reason` through the real pipeline) or the string round-trip on Postgres. LOW risk (no manual tx / jsonb / DateTime.Kind in this path — status-enum writes only), but closes the connectivity-proof gap. Also un-tested end-to-end: the `Goal.Finalized`/`Goal.Reopened` audit-row writes. | BUG-056 / DF-46 (#393) · `@test-authenticator` · `@integration-enforcer` | Coverage-hardening; dispatch wiring is trace-proven + the enum-strings are siblings of already-shipped values. | S |
| DF-48 | ✅ **DONE (#399, real-PG, +TC-CHR-005-48)** **Real-Postgres arm for the `salary_grades` table (ISSUE-021)** — the new `SalaryGrade` entity's `decimal(18,2)` Min/Mid/Max precision/rounding, the snake_case config, the case-insensitive `(TenantId, Code)` unique-index translation, and the tenant global query filter are all InMemory-only. InMemory does NOT enforce Postgres `numeric(p,s)` precision or the `Code.ToLower()` uniqueness collation. Add a Testcontainers arm (fold into DF-45's real-PG pass — same wave). | ISSUE-021 (#389) · `@test-authenticator` | Coverage-hardening on a **money** table; the exact InMemory-masks-Postgres class (cf. BUG-289/290). | S–M |
| DF-49 | ✅ **DONE (#399, real-PG, +TC-RPT-005-49)** **Real-Postgres arm for the dashboard birthday-index (ISSUE-285a)** — the `BirthMonthDay` index-backed SQL window (`windowKeys.Contains(e.BirthMonthDay)`), the anniversary arm's `DateOfJoining.Month*100+.Day` Npgsql translation, AND the migration **backfill SQL** (`EXTRACT(MONTH…)*100+EXTRACT(DAY…)` — existing rows get their key ONLY from this, the interceptor never fires for them) are all InMemory-only. Enforcer verified the code by inspection (birthday arm = plain-column IN → translates + index-usable; anniversary translates ~87%), but a post-query `IsWithinNextDays` re-filter means deleting the SQL `Where` keeps InMemory tests green → the optimization itself isn't pinned. Add a Testcontainers arm: apply the migration + assert backfill on a pre-seeded row, assert both arms translate + return the right set, ideally EXPLAIN shows the `(tenant_id, birth_month_day)` index. | ISSUE-285a (#390) · `@integration-enforcer` · `@test-authenticator` | Proves the actual perf goal + the untested backfill; the standing InMemory-masks-Postgres class. | M |
| DF-50 | ✅ **DONE (#415, +TC-RPT-005-50)** **ISSUE-285(b): parallelize the ~8 sequential dashboard widgets + cold-start warmup** — recon found the measured ~25s pain is COLD-START (EF model build + plan compile + cold caches), NOT warm-latency (p95 already ~193ms), so shipped BOTH (user decision 2026-07-21): (1) `DashboardWarmupHostedService` primes model/plans/pool on startup (non-blocking, `IgnoreQueryFilters`, never crashes startup); (2) `IParallelTenantScopeRunner` fans out the widgets with **child-scope-per-widget** (NOT `IDbContextFactory` — reuses the `TenantJobRunner` pattern), bounded (`Dashboard:MaxWidgetParallelism`=4). **The cross-tenant risk is CLOSED by the mandatory `SetTenant` re-seed per child scope** (a fresh scope's `ITenantContext` is unresolved → fail-open filter → leak; the runner re-seeds before any query), guarded by a mutation-killing isolation test. Enforcer WIRED (gate holds); authenticator AUTHENTIC. | ISSUE-285 (COMPLETION-PLAN P4-perf) — spun off from #390 | Perf; but a concurrency refactor with tenant-isolation risk — do it deliberately. | M |
| DF-53 | ✅ **DONE (#419, validated end-to-end on fresh postgres:17-alpine + acme-present)** **Perf seed not self-contained on a fresh/throwaway DB** — `perf/seed/seed-perf-tenant.sql` sources perfadmin's password hash from `tenantadmin@acme.test` AND replicates the 8 built-in roles from the `acme` tenant. A normal dev DB has acme (QA reseed) but a freshly-migrated DB has only `platform`+`e2e` (`DbInitializer`) → the seed hits `fk_user_tenants_users_user_id` + produces a permission-less admin (`roles=0`). **Fix:** COALESCE the role/hash source across acme→e2e→any built-in-role tenant (and derive the hash via `crypt(...)` when the source user is absent) so it works on any DB. Workaround validated + documented in `perf/README.md` (2026-07-20 DF-51 run). | DF-51 run (2026-07-20) | Rig-hardening; the committed seed still works on the normal dev DB. | S |
| DF-54 | ✅ **DONE (#418, +6 arms: 4 pure-helper + 1 component-wiring + 2 BE)** **FE: proactively disable the employee Cancel button inside the tenant leave-cancellation window (DF-20 follow-up)** — the BE now enforces `LeaveCancellationWindowDays` (an Approved leave inside the window → 400); the employee `evaluateCancelEligibility` still hardcodes window=0, so the Cancel button shows enabled and 400s on submit (graceful, but not proactive). Add `cancellationWindowDays` (or a per-row `cancellable` flag) to the employee `GET /leaves/mine` payload, then thread it into `evaluateCancelEligibility(req, today, windowDays)`. | DF-20 (#403) · `@frontend-dev` | UX truthfulness; the BE guard already protects correctness. | S |
| DF-56 | ✅ **DONE (#426, user-decided: knobs wired into money paths). 5 nullable `int?` knobs on `Shift` (Standard/Minimum/AutoBreak/AutoBreakThreshold/OvertimeThreshold) wired `shift.<knob> ?? existing` into all 4 resolution sites incl. clock-out + overtime-record; null-invariant proven (guards byte-unchanged); explicit-knob money teeth (TC-ATT-005-19 unit + TC-ATT-006-19 OT-record: shift 420 → OT 120 vs tenant 480 → 60); regularization heal added; validator floors Standard/Minimum at 1 (MED-1). Residuals → [[DF-63]] [[DF-64]]. 2 auditors CONNECTED/AUTHENTIC.** **Shift shadows per-location work-minute knobs in the monthly summary (DF-22 follow-up)** — DF-22 now honors a Location's `AttendanceSettings` override for `HalfDayEnabled` and, as a *fallback*, `MinimumWorkMinutes`/`StandardWorkMinutes`. But when an employee has an assigned `Shift`, its work-minutes win over `AttendanceSettings` (the `if (minimumMinutes <= 0)` / `if (standardMinutes <= 0)` fallbacks only fire when the shift carries none), and `Shift` has **no** `StandardWorkMinutes`/`AutoBreak`/`OvertimeThreshold` fields — so a location override for those specific knobs can be shadowed by a shift for shifted employees. Pre-existing `Shift`-vs-`AttendanceSettings` precedence design (US-ATT-005 TODO, `AttendanceCalculator.cs:20-25`), NOT introduced by DF-22. **Fix would need a `Shift` schema change** (per-location work-minute knobs on the shift, or an explicit precedence decision). | DF-22 (#409) · `@backend-dev` OOL | Design/decision; DF-22 already honors the override for `HalfDayEnabled` + the fallback minima + the full auto-clock-out AutoBreak/Overtime path. | M (decision-gated) |
| DF-59 | ✅ **DONE (#417, +TC-REC-008-16 source-coupled real-PG arm)** **Composite index for the DF-7 DB-count fallback** — the `DbCountPortalLinkIpRateLimiter` (used only when Redis is unconfigured) filters `applicant_portal_token` on `request_ip` + `created_at`, but the table has no index on those columns (only `(tenant_id, applicant_email)`), so each fallback issuance does a filtered COUNT that may seq-scan at scale. **Fix:** add a `(tenant_id, request_ip, created_at)` composite index via an EF migration. Redis path avoids the COUNT entirely, so this only matters when Redis is off. | DF-7 (#413) · `@backend-dev` OOL | Fallback-only perf; needs a migration (out of the DF-7 seam scope). | S |
| DF-58 | ✅ **DONE (#421, 2 reconcile sweeps + 9 arms + app-boot DI guard; residual → DF-61)** **Same post-commit-enqueue durability gap in `PayrollRunService` + `OnboardingChecklistService` (DF-4 sibling)** — DF-4 fixes only the leave-entitlement recalc. The identical best-effort `_scheduler?.Enqueue(...)` after commit exists in `PayrollRunService` (~:161-163, :290-292) and the onboarding checklist path, with no shared helper — a Hangfire-storage outage in that window silently drops those jobs too. **Fix:** either add the same idempotent reconciliation-sweep backstop to each (if their consumers are idempotent — verify first), or extract a shared enqueue-with-backstop helper. NOT a general outbox unless product wants exactly-once/audit (that path = the deferred Tier-C-ish L). | DF-4 (#411) · recon | Systemic reliability; same rare-window class as DF-4. Verify each consumer's idempotency before picking the backstop shape. | M |
| DF-57 | ✅ **DONE (#422, +TC-CHR-011-14 real-PG full-route arm: ascending + cross-tenant truncation + 404 + 403-authz)** **Full-chain HTTP-route regression test for query endpoints (DF-8 follow-up)** — DF-8's `GET /employees/{id}/reporting-chain` (and its sibling `direct-reports`) are covered by InMemory service-layer arms that call the service directly; no test drives route→controller→MediatR→handler, so removing the controller action or mis-registering the query fails zero tests. **Fix:** one Testcontainers full-route arm (real `Employee.View.All` persona) asserting ascending order + tenant truncation that would fail if the wiring were removed. Repo-wide convention gap (many controller slices are service-layer-tested only), not DF-8-specific — the walk logic + tenant-scope ARE covered by the 7 InMemory arms. | DF-8 (#410) · `@integration-enforcer` OOL | Endpoint-wiring regression coverage; the dispatch is a verified mirror of the wired `direct-reports` action. | S–M |
| DF-55 | ✅ **DONE (#424, +TC-PAY-004-06): `Payslip.Retried` audit action written with the acting `ICurrentUser` in `RetryOneAsync` (distinct from the system-actor render audit).** **⚖ DECISION: actor-attributed audit for a manual per-employee payslip retry (DF-31 follow-up)** — a human `POST .../retry` currently writes only the system-actor `PayslipPdfGenerated` render audit (indistinguishable from an automated batch render); there is no "user X retried employee Y's slip on run Z" record. **Recommendation:** add a distinct `PayslipRetried` audit action written with the acting `ICurrentUser` in `RetryOneAsync` (US-PAY-012 / BR-1 "complete audit trail" — a manual admin payroll action should be attributable). Also LOW-severity here: a real-Postgres arm for the retry path (predicates are translatable). | DF-31 (#405) · `@test-authenticator` OOL | Compliance/audit-completeness on a manual payroll action; small BE add once decided. | S |
| DF-52 | ✅ **DONE (#399, real-PG, TC-PAY-008-14)** **Real-Postgres / HTTP-route arm for the DF-14 pending-approvals queue** — the 12 `GetPendingApprovalsAsync` unit arms (incl. the cross-tenant isolation arm) run InMemory-through-real-EF; no Testcontainers arm hits `GET /api/v1/payroll/approval/pending` end-to-end, so the tenant scoping is proven on the EF global filter but **not** the committed-OFF Postgres RLS policy on `payroll_runs`, and the route's `Payroll.Approve` gate is only FE-mocked. Also optional here: the bounded per-run distinct-approver query could pre-load Approved histories in one `WHERE WorkflowInstanceId IN (…)`. LOW risk (read-only LINQ; `.Contains`/joins translate). Fold into the DF-3/15/16/45/47/48/49 real-PG coverage wave. | DF-14 (#394) · `@test-authenticator` · `@integration-enforcer` | Coverage-hardening on a scoped read path; the standing InMemory-masks-Postgres + RLS-not-exercised class. | S–M |
| DF-51 | ✅ **VALIDATED (2026-07-20, real 50k run)** **ISSUE-285(c): k6 dashboard-at-scale scenario** — the scenario already exists (`perf/scripts/01-hot-reads.js`, `widgets p(95)<800` threshold). **Ran it at emp_count=50000 / 50 VU / 5 min on a throwaway `hris_perf_db`: dashboard `widgets p95 = 192.9ms` (SLA <800ms ✅), employees 307.8ms, context 37.9ms, reports 24.5ms, error rate 0.00%; 69,201 checks 100% pass.** The prior committed run showed widgets p95 829ms (failing) → now passing (likely helped by the #390 birthday-index work). **⚠ One wart:** cold-start widget outliers (max ~25s on the first hits before the EF query-plan + 2nd-level cache warm) — p95 is fine but first-hit latency is high; folds into DF-50 (a warmup or parallelize pass). Original text: add a dashboard-load scenario asserting the SLA (p95<800ms, no 60s timeout @ 50k rows / 50 VUs). Env-gated (needs the running stack + 50k seed) — can't run in a dev session. | ISSUE-285 (COMPLETION-PLAN P4-perf) — deferred from #402 | Perf verification; env-gated, closes the ISSUE-285 SLA proof once DF-49 lands the index. | S |
| DF-60 | ✅ **DONE (#429, test-only).** **FE: Karma spec pinning bare-date (`yyyy-MM-dd`) parsing in `my-onboarding.models.ts`** — added 3 arms to `my-onboarding.models.spec.ts`: today-boundary (due today → not overdue), day-boundary (due yesterday → 1 day, min-1 floor), and the wire-format contract (bare `2026-07-10` → overdue vs the same date as an ISO datetime string → Invalid Date → not overdue), characterizing the DF-1 dependency so a datetime revert is visible in the test file. 16/16 green. — DF-1 (#416) changed the wire format for onboarding/offboarding date fields from full datetime to bare `date`, which *fixes* a latent `new Date(`${task.dueDate}T00:00:00`)` parse (`my-onboarding.models.ts:136,147`) that silently produced Invalid Date on the old datetime string. No spec pins this, so a future revert to `DateTime` serialization would re-break the overdue/countdown calc invisibly. Add a Karma spec asserting the overdue/countdown computation on a `yyyy-MM-dd` `dueDate`. | DF-1 (#416) · `@integration-enforcer` OOL | Regression protection; healed-by-DF-1 (informational), but binds the wire-format contract with a test. | S (FE) |
| DF-61 | **Payroll `RerunAsync` dropped-enqueue leaves stale `ReviewPending` slips (DF-58 residual)** — DF-58's payroll sweep re-enqueues only stranded `Queued` runs; the `RerunAsync` (`PayrollRunService.cs:290-293`) post-commit enqueue, if dropped, leaves a `ReviewPending` run with the OLD slips (the reprocess that would replace them never runs). A status sweep can't heal it safely: a `ReviewPending` run is status-indistinguishable from a correctly-processed one, and `PayrollRun` has no concurrency token, so re-enqueuing a live/valid `ReviewPending` run would race the worker (slip corruption). No lockout (it's a manual, re-triggerable HR action). **Fix (if wanted):** a per-run idempotency/dedup token (e.g. a `ReprocessRequestedAt` marker or a job-dedup key) so the sweep can distinguish "reprocess requested but not run" from "processed" — larger than a naive sweep, decision-gated. | DF-58 (#421) · recon | Reliability tail; no lockout, manual re-trigger available. Needs a dedup token to auto-heal safely. | M (decision-gated) |
| DF-62 | **⚖ DECISION: ISSUE-295 encashment/carry-forward residual clauses (BUG-079; split from DF-37)** — three product/BA-gated clauses, no code TODOs (tracked in `TEST-FINDINGS.md`): **(1)** leave-encashment daily-rate numerator uses the RAW current monthly BASIC un-prorated (`LeaveEncashmentService.CurrentMonthlyBasicAsync`) — is a pro-rated/point-in-time BASIC required? **(2)** a null `CarryForwardLimit` currently lets the whole non-negative balance be encashed (still balance-gated, `LeaveEncashmentService.cs:105-107`) — intended, or hard-block? **(3)** gate-vs-year-end forfeitable parity: the encashment gate ceiling derives from the latest ledger `BalanceAfter` while the year-end job computes forfeiture via `LeaveCarryForwardCalculator`; they agree only when `Σaccruals == engine ProratedEntitlementDays`, divergence erodes carried days (employee detriment, not double-pay) — optional hardening. All three are **pay-policy decisions**, not mechanical fixes; DF-37 deliberately scoped to the ISSUE-280 keying refactor. | [[ISSUE-295]] · BUG-079 · DF-37 recon (#423) | Money-policy; needs a product/BA call per clause before building. | M (decision-gated) |
| DF-63 | ✅ **DONE (#428, 2 auditors CONNECTED/AUTHENTIC). Auto-clock-out is shift-knob-unaware (DF-56 residual).** `AutoClockOutJob` now resolves `IShiftService` from the per-tenant runner scope (tenant/RLS-safe — shared-scope traced) and passes each open log's resolved shift into `AttendanceCalculator.Calculate(..., shift:)` via a memoized per-`(employee, clock-in date)` `ResolveShiftAsync` (mirrors the DF-22 `locationByEmployee` batch); resolution failure → null shift → byte-identical pre-DF-63 math. **No pay impact** (auto-close = ANOMALY, no `OvertimeRecord`; regularization re-applies knobs before pay) — a stored-minutes consistency fix. Tests (TC-ATT-162): default-shift arm (shift break 120 vs tenant 60 → 479 not 539, mutation-proven) + assignment-path arm (one sweep: assigned 90→509 vs default 120→479 vs tenant 60→539, proves per-employee FR-7 resolution). 6/6 AutoClockOut + 344/344 Attendance/Shift/Overtime/Regularization green on real PG. Residual: RLS-on path not exercised by the InMemory arm → [[DF-63-rls]] parked at the RLS decision-gate. | DF-56 (#426) · 2 auditors | Consistency-only; no pay impact (ANOMALY, no OT record). Batch-shift-resolution in a Hangfire job. | M |
| DF-64 | **⚖ Regularized OT may not create an `OvertimeRecord` (pre-existing; surfaced by DF-56)** — `RegularizationApprovalService` recompute updates `log.OvertimeMinutes` (now shift-knob-aware post-DF-56 heal) but does NOT appear to create/update an `OvertimeRecord`; payroll pays from approved `OvertimeRecords`, so an auto-closed→regularized OT day may never produce a payable OT record. PRE-EXISTING (not introduced by DF-56). **Decide:** should a regularized OT day generate an `OvertimeRecord` (subject to the approval flow)? If yes, wire it + add the regularization explicit-knob money arm (test-authenticator's HIGH — the heal's log-recompute is enforcer-verified correct but untested end-to-end). | DF-56 (#426) · `@integration-enforcer` (LOW pre-existing) · `@test-authenticator` (HIGH untested) | Possible unpaid OT on regularized days — verify intent before wiring. | M (decision-gated) |
| DF-65 | **⚖ Year-end auto-encashment doesn't bump `ConsumedDays` (DF-19 residual, same bug class)** — `LeaveCarryForwardService.cs:189-192` writes an `Encashed` row for the *forfeitable excess above the carry limit* (`fromYear`) without routing through `PooledLeaveLedger` / bumping `ConsumedDays`. Since DF-19's `ProcessExpiryAsync` now trusts `ConsumedDays` (not the derived Used+Encashed sum), a **narrow config** — a `ToYear=fromYear` carry bucket still `Active` at the year-end close (low-usage tenant + long `CarryForwardExpiryMonths`) — could let expiry re-forfeit days the year-end job already encashed (same over-forfeiture direction as the DF-19 CRITICAL). Enforcer confidence it's live-reachable: ~35%; **defensible under strict FIFO** (carried consumed first during the year → year-end residue is accrual-pool). **NOT wired in #427 on purpose:** this is the year-end carry/forfeit *sweep*, not a per-transaction consumption — routing it through the carried-first helper could double-count against the sweep's own forfeiture. **Decide:** confirm the FIFO assumption holds, or make the sweep reconcile the superseded bucket's `ConsumedDays`. | DF-19 (#427) · `@integration-enforcer` (LOW) | Narrow config; verify intent before touching the sweep. | S–M (decision-gated) |

## Done

| # | Item | Source | Resolution |
|---|------|--------|------------|
| DF-2 | **Full end-to-end cumulative-RUN true-up on real Postgres** — the processor running the payroll + persisting withheld deltas end-to-end, previously InMemory-only. | [[ISSUE-300]] · **PR #347** (2026-07-17) | `YtdCumulativeRunPostgresTests` drives `InitiatePayrollRunCommand → PayrollRunProcessor.ProcessAsync` on `postgres:17-alpine`. 2 arms green (cumulative Apr–Aug true-up `0,0,0,60k,60k` + non-cumulative flag-gate contrast). Confirmed the run write path is date-Kind-clean (unblocked once BUG-289/290 landed). |
| DF-11 | **`RejectionReason` enum columns still strict → board/detail can still 500** (filed as [[ISSUE-316]]). | [[ISSUE-231]] · **PR #351** (2026-07-17) | Both `rejection_reason` columns now read through `TolerantEnumToStringConverter<RejectionReason>(Unknown)` (nullable-safe); `Unknown=99` added; stage-move validators guard it; 2 Postgres arms + mutation-verified; TC-REC-003-16. Every enum-string column on the recruitment board/detail + payroll read paths is now tolerant. |
