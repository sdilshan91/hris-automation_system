# Auditor Findings Log

**Purpose.** A single, durable log of every finding raised by the read-only review agents —
`@integration-enforcer` (wiring `OUT-OF-LANE` / GAPS) and `@test-authenticator`
(`MISSING (should exist, don't)` coverage arms + weak/theatrical arms) — together with its
**resolution status**.

These findings are auto-healed piecemeal: **fixed inline** in the same PR before merge, or **filed as a
`DF-##` row** in [`DEFERRED-FOLLOWUPS.md`](DEFERRED-FOLLOWUPS.md). That works, but it scatters them, and a
LOW-severity item judged "acceptable for now" can silently fall through. This log makes the full set
visible in one place so **nothing is lost** — including the deliberately-accepted ones.

**Convention (how this stays useful).**
- Log a row for **every** `@integration-enforcer` OUT-OF-LANE/GAP and **every** `@test-authenticator`
  MISSING/weak-arm finding, **at the time it is raised** (during the gate for a cluster/PR).
- **Status vocabulary:**
  - `FIXED-INLINE (#PR)` — resolved in the same PR before merge (the orchestrator adds the material arm / the dev fixes the code).
  - `FILED (DF-##)` — deferred; tracked as a `DF-##` row in `DEFERRED-FOLLOWUPS.md` (one hop to full context).
  - `ACCEPTED` — judged not worth acting on, **with a reason** (this is the class that used to fall through).
  - `OPEN` — raised, not yet triaged/resolved.
  - `RESOLVED (#PR)` — a previously-`FILED`/`OPEN` item later fixed; update the row.
- When a `FILED` item's DF row is later picked up, flip both this row (`RESOLVED`) and the DF row.
- Skim this before trusting a "green" cluster or when planning a coverage/hardening batch.

Finding types: **OOL** = enforcer OUT-OF-LANE/GAP · **MISS** = authenticator "MISSING (should exist, don't)" · **WEAK** = authenticator weak/theatrical arm.

---

## Findings

| Date | PR / cluster | Auditor · type | Finding | Sev | Status |
|------|--------------|----------------|---------|-----|--------|
| 2026-07-19 | #384 · DF-41/42 | authenticator · MISS | No arm proves the offer magic-link renders on the primary PDF path (only the dispatcher-fallback payload was inspected). | MED | **FIXED-INLINE (#384)** — added a catalog presence arm asserting `offer_sent` renders `{{offer.portalUrl}}`. |
| 2026-07-19 | #385 · DF-33 | authenticator · WEAK | The `ChronicThreshold == 0` disable-guard arm seeded `prior:6` → the `==threshold+1` arithmetic masked the guard (a guard-removal mutant survived). | MED | **FIXED-INLINE (#385)** — reseeded to `threshold:0, prior:0` so only the `>0` guard suppresses the fire. |
| 2026-07-19 | #385 · DF-33 | backend/authenticator · OOL | `AttendanceService.ClockInAsync` stamps `DateTime.UtcNow` — no `TimeProvider` clock seam (test-determinism; ~1s/month month-end window). | LOW | **RESOLVED (#398)** — clock seam shipped #398 (DF-43 DONE). |
| 2026-07-19 | #385 · DF-33 | enforcer/authenticator · MISS | No real-Postgres arm for the crossing count/local-date guard + no composition-root assert that DI injects a non-null `ILateEarlyService`. | LOW | **RESOLVED (#399)** — real-PG chronic-lateness arm (AttendanceChronicLatenessPostgresTests). |
| 2026-07-19 | #386 · DF-38/39 | enforcer · OOL | **GAP-A:** `getEmploymentTypes` returned `{id,name,displayName}` typed as `{value,label}` with no `map()` → the Employment Type dropdown was **blank/undefined in prod**, masked by a wrong-shape spec mock. | HIGH | **FIXED-INLINE (#386)** — added the `map()` + corrected the masking mock. |
| 2026-07-19 | #386 · DF-38/39 | enforcer/authenticator · OOL | **GAP-B:** FE sub-section send-mapping dropped the row `id` (churned PKs) + `fieldOfStudy`/`startYear`/`description` (data loss on edit). | MED | **FIXED-INLINE (#386)** |
| 2026-07-19 | #386 · DF-38/39 | authenticator · MISS | WorkHistory/Dependents full-replace arms didn't prove removal-of-omitted (+ vacuous `OnlyContain`); no cross-tenant isolation arm on the 3 new sub-tables. | MED | **FIXED-INLINE (#386)** — orchestrator added the removal + isolation arms. |
| 2026-07-19 | #386 · DF-38/39 | authenticator · MISS | Real-Postgres arm for the 3 new sub-tables (delete-then-reinsert-same-PK, `DateOnly`→`date`). | MED | **RESOLVED (#399)** — real-PG sub-table arm (EmployeeProfileSubEntitiesPostgresTests). |
| 2026-07-19 | #387 · BUG-056 | enforcer · OOL | Finalize moves goals `Acknowledged→Finalized`, but 3 "active = Submitted‖Acknowledged" filters (`GoalProgressService` ×2, `StaleGoalNudgeService`) omitted Finalized → a finalized set **silently vanished from progress tracking + stale nudges**. | MED | **FIXED-INLINE (#387)** — added `Finalized` to the 3 filters + a regression arm. |
| 2026-07-19 | #387 · BUG-056 | backend · OOL | `AggregateStatus` mapped a finalized set to the `"Draft"` team-dashboard bucket. | LOW | **FIXED-INLINE (#387)** — added a `Finalized` branch + test. |
| 2026-07-19 | #387 · BUG-056 | authenticator · MISS | Empty-set→422 boundary; `Update`/`Delete` write-guard arms (only Save+Create were proven). | LOW | **FIXED-INLINE (#387)** — orchestrator added the arms. |
| 2026-07-19 | #387 · BUG-056 | enforcer/backend · OOL | A finalized goal set has **no re-open/unlock path** (a mistaken finalize is unrecoverable). | MED | **FILED (DF-46)** — ⚖ decision-gated. |
| 2026-07-19 | #387 · BUG-056 | authenticator/enforcer · MISS | Real-Postgres/HTTP finalize round-trip arm + `Goal.Finalized` audit-row assertion. | LOW | **RESOLVED (#399)** — real-PG finalize/reopen arm (GoalFinalizeReopenPostgresTests). |
| 2026-07-19 | #388 · DF-17 | authenticator · MISS | No explicit zero-HTTP arm on a delivered depth-2 **leaf**; no fallback arm for a truncated node **inside** the delivered depth. | LOW | **ACCEPTED** — the core "zero-HTTP on in-depth expand" crux is mutation-proof; the fallback is proven at the depth-1 boundary. Noted in the #388 PR. |
| 2026-07-19 | #389 · ISSUE-021 | enforcer · OOL | `JobTitleDto` had no `GradeName` → the job-title list rendered "—" for every grade. | MED | **FIXED-INLINE (#389)** — added `GradeName` + a batched (no-N+1) `SalaryGrade` join in the read paths. |
| 2026-07-19 | #389 · ISSUE-021 | enforcer · OOL | The `salary-grades` route guard omitted `'Tenant Owner'` (present on sibling admin routes). | LOW | **FIXED-INLINE (#389)** — added `'Tenant Owner'` + a nav item. |
| 2026-07-19 | #389 · ISSUE-021 | authenticator · MISS | No cross-tenant grade-link rejection arm (a tenant-B grade must be rejected for a tenant-A JobTitle — BUG-003 class). | MED | **FIXED-INLINE (#389)** — orchestrator added the arm. |
| 2026-07-19 | #389 · ISSUE-021 | authenticator · MISS | Real-Postgres arm for `salary_grades` `decimal(18,2)` precision + `Code` uniqueness collation. | MED | **RESOLVED (#399)** — real-PG salary_grades arm (SalaryGradePostgresTests). |
| 2026-07-19 | #390 · ISSUE-285a | enforcer/authenticator · MISS | The index-backed SQL, the migration **backfill SQL**, and the anniversary `.Month`/`.Day` translation are InMemory-only; a post-query re-filter means deleting the SQL `Where` keeps InMemory tests green (the optimization isn't pinned). | MED | **RESOLVED (#399)** — real-PG birthday-index arm (DashboardBirthdayIndexPostgresTests). |
| 2026-07-19 | #390 · ISSUE-285 | (deferred by design) · — | (b) parallelize the ~8 sequential dashboard widgets via `IDbContextFactory` (concurrency + cross-tenant-in-factory risk). | MED | **FILED (DF-50)** |
| 2026-07-19 | #390 · ISSUE-285 | (deferred by design) · — | (c) k6 dashboard-at-scale SLA scenario (env-gated: 50k seed + running stack). | LOW | **FILED (DF-51)** |
| 2026-07-19 | #390 · ISSUE-285a | enforcer · OOL | Anniversary arm is a seq-scan (no denormalized `JoinMonthDay` column/index) — functional but unindexed. | LOW | **ACCEPTED** — birthday index (the ISSUE-285a target) is done; anniversary index folded into DF-49 as an optional follow-up. |
| 2026-07-20 | DF-46 · goal-reopen | authenticator · MISS | No positive arm for the BR-4 **direct-manager** (`SetGoal.Team`) re-open allow-path — only HR-happy + non-manager-403 existed; a mutant requiring `SetGoal.All` (dropping the manager branch) would survive. | HIGH | **FIXED-INLINE (#393)** — orchestrator added `Reopen_ByDirectManager_WithSetGoalTeam_Succeeds`. |
| 2026-07-20 | DF-46 · goal-reopen | authenticator · MISS | No **cross-tenant isolation** arm (BUG-003 class) — a tenant-B caller must not re-open tenant-A's finalized set; relies solely on the global query filter, untested. | HIGH | **FIXED-INLINE (#393)** — orchestrator added `Reopen_CrossTenant_CannotUnlockAnotherTenantsSet`. |
| 2026-07-20 | DF-46 · goal-reopen | authenticator · WEAK | The writability-restore arm asserted post-reopen SaveGoals succeeds but never showed the pre-reopen 409, so "restores writability" leaned on an untested guard; `OnlyContain` could pass vacuously. | MED | **FIXED-INLINE (#393)** — added the pre-reopen 409 contrast + `HaveCount(2)`. |
| 2026-07-20 | DF-46 · goal-reopen | authenticator · MISS | Audit arm proved `Detail.Contains(reason)` but not the BE-side `.Trim()` — a drop-`.Trim()` mutant survived (only the FE trim was proven). | LOW | **FIXED-INLINE (#393)** — reason seeded with surrounding spaces; asserts `"Reason: spaced reason"` (trimmed). |
| 2026-07-20 | DF-46 · goal-reopen | enforcer · OOL | FE `reopenGoals` typed `Observable<void>` / doc "204/void", but BE returns 200 `ApiResponse<EmployeeGoalsDto>`; FE ignores the body + reloads (harmless). Same latent drift as the `finalizeGoals` sibling. | LOW | **ACCEPTED** — cosmetic; consistent with the existing finalize sibling, feature works. |
| 2026-07-20 | DF-46 · goal-reopen | enforcer · ENH | The "Re-open" button renders for any viewer of a locked set (no FE permission gate); server 403s an unauthorized caller. Deliberate — the whole performance feature has no FE permission signal, and Finalize is likewise not FE-gated. | LOW | **ACCEPTED** — secure (server-enforced); matches the existing finalize pattern. Candidate for a future FE-permission-plumbing pass across performance actions. |
| 2026-07-20 | DF-46 · goal-reopen | enforcer · TEST-HEALTH | No full-chain controller→MediatR→ValidationBehavior→GoalService HTTP arm (validator-rejects-empty-reason proven by service+validator in isolation, not end-to-end). Repo standard (Testcontainers HTTP not sandbox-runnable). | LOW | **RESOLVED (#399)** — real-PG finalize/reopen arm (GoalFinalizeReopenPostgresTests). — folded the reopen HTTP round-trip into the existing goal-finalize Postgres/HTTP DF-47. |
| 2026-07-20 | DF-46 · goal-reopen | (decision, now built) · — | The finalize/lock had no undo (originally filed DF-46). | MED | **RESOLVED (#393)** — shipped `ReopenGoalsAsync` + endpoint + FE + 9 xUnit / 7 Karma arms; decision locked (HR or finalizing manager, mandatory reason, →Acknowledged). |
| 2026-07-20 | DF-14 · pending-approvals | authenticator · MISS | Newest-first ordering (`OrderByDescending(PayYear).ThenBy…(PayMonth)`) untested — every seeded run used the same period, so a drop-ordering mutant survived. | MED | **FIXED-INLINE (#394)** — added `GetPending_OrdersNewestPeriodFirst` (3 runs across 2 years/2 months). |
| 2026-07-20 | DF-14 · pending-approvals | authenticator · MISS | `ResolveSubmitterNamesAsync` User/email fallback branch + Employee-over-User precedence untested (only the Employee path was proven). | MED | **FIXED-INLINE (#394)** — added `…FallsBackToUserDisplayName` + `…PrefersEmployeeOverUser`. |
| 2026-07-20 | DF-14 · pending-approvals | authenticator · MISS | Distinct-approver had only an exclude arm; no positive control that a multi-step run the caller has NOT approved IS listed. | LOW | **FIXED-INLINE (#394)** — added `GetPending_IncludesMultiStepRun_CallerHasNotYetApproved`. |
| 2026-07-20 | DF-14 · pending-approvals | authenticator/enforcer · GAP | No real-Postgres/Testcontainers + HTTP-route arm for `GET /payroll/approval/pending` (isolation proven on the EF filter, not RLS; the route + `Payroll.Approve` gate only FE-mocked). | MED | **RESOLVED (#399)** — real-PG pending-approvals arm (PayrollPendingApprovalsPostgresTests). |
| 2026-07-20 | DF-14 · pending-approvals | enforcer · ISSUE | Distinct-approver check fires one `PayrollApprovalHistories` query per included multi-step run (bounded N+1); name + role lookups are batched. | LOW | **ACCEPTED** — bounded; AwaitingApproval multi-step runs are typically few. Optional pre-load noted in DF-52. |
| 2026-07-20 | DF-14 · pending-approvals | enforcer · ISSUE | Queue builds the step→role map with `ToDictionaryAsync` (throws on duplicate `StepNumber`) vs ApproveAsync's `FirstOrDefault`. | LOW | **ACCEPTED — moot** — verified a unique index on `(TenantId, StepNumber)` (`PayrollApprovalStepConfigConfiguration.cs`) makes a duplicate impossible. |
| 2026-07-20 | DF-14 · pending-approvals | (feature, now built) · — | FE Pending-Approvals queue called `GET /payroll/runs?status=…` which the BE ignored → every approver saw all runs (originally filed DF-14). | MED | **RESOLVED (#394)** — approver-scoped `GET /payroll/approval/pending` mirroring the ApproveAsync gates via a shared predicate; 12 xUnit / 3 FE arms; enforcer WIRED, authenticator 100%. |
| 2026-07-20 | DF-5 · variant-cap | enforcer · DECISION | An explicit **unlimited** override (`PlanLimitResolver` Source=Override, Value=null) collapses to plan/tenant/default(2) instead of unlimited — unlike WorkflowService which honors null as unlimited. | LOW | **ACCEPTED** — deliberate + documented DF-5 stance ("never truly unlimited" for variants); unbounded language variants is not a desirable config. |
| 2026-07-20 | DF-5 · variant-cap | enforcer/antipattern · ENH | `ResolveMaxLanguageVariantsAsync` uses raw `DateTime.UtcNow` for override effective-dating (no TimeProvider seam). | LOW | **ACCEPTED** — mirrors the `WorkflowService.MaxWorkflows` call verbatim; seaming one site alone would diverge. Repo-wide clock-seam is the DF-43 class. |
| 2026-07-20 | DF-5 · variant-cap | (feature, now built) · — | The template variant cap was a hardcoded `const int = 2`, not plan-configurable (BR-6). | S–M | **RESOLVED (#395)** — wired to `PlanLimitResolver` (new key + `SubscriptionPlan`/`Tenant` columns + migration + seed); override>plan>tenant>2. Enforcer WIRED; 2 self-authored mutation-resistant arms (override-raises + plan-lowers) green (authenticator not separately run — arms are configurability-proving by construction). |
| 2026-07-20 | DF-20 · cancel-window | authenticator · MISS | The server-side `InclusiveBetween(0,90)` bound (the real enforcement — FE `max(90)` is bypassable) was untested; the whole `UpdateOrgProfileValidator` had zero coverage. | HIGH | **FIXED-INLINE (#403)** — added `UpdateOrgProfileValidatorTests` (0–90 boundary + adjacent fiscal/probation bounds). |
| 2026-07-20 | DF-20 · cancel-window | authenticator · MISS | Exact `<=` cutoff off-by-one (`StartDate == cutoff`) unpinned; a `<=`→`<` mutant survived the +3/+10 arm. | MED | **FIXED-INLINE (#403)** — added the at-cutoff (today+5 blocked) / one-day-past (today+6 allowed) arm. |
| 2026-07-20 | DF-20 · cancel-window | authenticator · MISS | No arm proving the window gates ONLY Approved (a Pending request inside the window stays cancellable). | LOW | **FIXED-INLINE (#403)** — added `Cancel_PendingRequest_InsideTenantWindow_IsStillAllowed`. |
| 2026-07-20 | DF-20 · cancel-window | enforcer · ISSUE | No Testcontainers arm exercises the new `tenants.leave_cancellation_window_days` column (proven only InMemory-through-real-EF). | LOW | **ACCEPTED** — plain scalar-int read, no timestamptz/enum/RLS masking surface; migration+snapshot mechanically verified. |
| 2026-07-20 | DF-20 · cancel-window | (feature, now built) · — | FR-7 cancellation window was a hardcoded `const=0`, not per-tenant (ISSUE-044). | M | **RESOLVED (#403)** — `Tenant.LeaveCancellationWindowDays` (org-profile, 0–90) read at cancel; enforcer WIRED, authenticator 100% (3 MISS healed). FE proactive-disable → DF-54. |
| 2026-07-20 | DF-6 · dashboard-pdf-logo | enforcer · ENH | `ResolveLogoBytesAsync` ran for ALL export formats — csv/xlsx did a wasted tenant-storage read + image decode (only the pdf branch consumes it). | LOW | **FIXED-INLINE (#404)** — gated the logo read behind `normalized == "pdf"`. |
| 2026-07-20 | DF-6 · dashboard-pdf-logo | enforcer · TEST-HEALTH | The embed path is unit-tested with an `IFileStorage` substitute; no integration arm drives a real `LocalFileStorage` file read for the dashboard PDF. | LOW | **ACCEPTED** — embed + degrade are unit-proven (incl. a size-delta embed proof); the `LocalFileStorage` tenant-boundary is already covered by `TenantBrandingServeTests` + the DF-30 path-guard tests. |
| 2026-07-20 | DF-6 · dashboard-pdf-logo | (feature, now built) · — | Branded dashboard PDF drew only the colour band; the tenant `LogoUrl` image wasn't embedded (ISSUE-126 residual). | M | **RESOLVED (#404)** — in-process `IFileStorage` logo read (tenant-scoped, boundary-checked) embedded in the QuestPDF header, degrades to colour-only on any failure. Enforcer WIRED + tenant-secure; 3 mutation-resistant arms (size-delta embed proof + missing + corrupt fallback). TC-PRF-007-16. |
| 2026-07-20 | DF-31 · payslip-retry | authenticator/enforcer · MISS | No cross-tenant isolation arm on the retry **write** path (a tenant-A retry of tenant-B's real slip → 404 + B untouched) — the BUG-003 write-side blind spot. | HIGH | **FIXED-INLINE (#405)** — added `Retry_CrossTenant_Returns404_AndForeignSlipUntouched`. |
| 2026-07-20 | DF-31 · payslip-retry | authenticator · MISS | The BE-permissive locked decision (retry a non-Failed/Generated slip is allowed) was entirely untested; a `!= Failed → 409` regression would pass every arm. | HIGH | **FIXED-INLINE (#405)** — added `Retry_GeneratedSlip_IsAllowed_BackendIsPermissive` (asserts success + `Regenerated=true`). |
| 2026-07-20 | DF-31 · payslip-retry | authenticator · OOL | A manual per-employee retry writes NO actor-attributed audit — only the system-actor `PayslipPdfGenerated` render audit (indistinguishable from a batch render). | MED | **FILED (DF-55)** — ⚖ decision-gated (add a `PayslipRetried` actor audit?). |
| 2026-07-20 | DF-31 · payslip-retry | authenticator · MISS | No real-Postgres arm for the retry path (InMemory-only). | LOW | **FILED (DF-55)** — folded into the retry-audit follow-up; predicates are translatable. |
| 2026-07-20 | DF-31 · payslip-retry | (feature, now built) · — | `PayslipsController` had only run-level regenerate (overwrites ALL slips); FR-8 wanted individual retry (ISSUE-162). | M | **RESOLVED (#405)** — `POST runs/{runId}/payslips/{employeeId}/retry` reusing the 3-phase renderer on a single-employee plan filter. Enforcer WIRED (no run-level regression); authenticator 100% (2 HIGH healed). TC-PAY-004-05. |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | authenticator · MISS | Both escalator arms MOCKED the notification service → the actual backup-role recipient resolution (the core of FR-3) was untested; the seed was decorative. | HIGH | **FIXED-INLINE (#406)** — added 2 `RealPayrollNotificationServiceTests` arms: escalated → backup-role holders (not the general pool), and no-backup → approver-pool fallback. |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | authenticator · MISS | Step-ADVANCE re-stamp (SlaDueAt from the next step's SlaHours + EscalatedAt cleared) was untested. | MED | **FIXED-INLINE (#406)** — added `Approve_AdvancingToNextStep_ReStampsSlaDueAt…`. |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | authenticator · MISS | Step-config SLA/backup validation (SlaHours>0; BackupRoleId must hold Payroll.Approve) was untested. | MED | **FIXED-INLINE (#406)** — added `SetStepConfig_ZeroSlaHours_Returns400` + `SetStepConfig_BackupRoleLacksApprovePermission_Returns400`. |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | enforcer · TEST-HEALTH | No cross-tenant escalation isolation arm (a breached run in tenant B must not escalate under tenant A) — the write-side BUG-003 property. | LOW | **FIXED-INLINE (#406)** — added `EscalateDueSteps_IsTenantScoped_ForeignBreachedRunUntouched` (real PG). |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | enforcer · ISSUE | Reject/ReturnToHr left a stale `SlaDueAt`/`EscalatedAt` (inert — the CAS filters `Status==AwaitingApproval`). | LOW | **FIXED-INLINE (#406)** — null both on reject + return for hygiene. |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | enforcer · — | Suspected `payroll-approval-escalated` (hyphen) vs catalog underscore mismatch. | — | **FALSE ALARM** — the notification service normalizes hyphen→underscore per the existing payroll-event convention. |
| 2026-07-20 | ISSUE-173-FR3 · sla-escalation | (feature, now built) · — | US-PAY-008 FR-3 SLA auto-escalation to a backup approver was 0% for payroll (generic engine had it, payroll's forked flow didn't). | M–L | **RESOLVED (#406)** — ported into the payroll slice (config cols + run cols + migration + stamp + escalation job + notify-backup-role + Escalated history). Enforcer WIRED (no money-path regression, tenant-safe, idempotent CAS); authenticator 100% (3 MISS healed). TC-PAY-008-07 escalation half. FR-6 delegation next. |
| 2026-07-20 | ISSUE-173-FR6 · delegation | authenticator/enforcer · MISS | Step-ADVANCE delegation (the approve→step-N `ApplyDelegationAsync` call site) was untested — a mutant deleting it silently breaks delegation for every multi-step run. | HIGH | **FIXED-INLINE (#407)** — added `Approve_AdvancingToStepWithDelegatedPrimaryOnLeave_DelegatesAtThatStep`. |
| 2026-07-20 | ISSUE-173-FR6 · delegation | authenticator · MISS | The leave-DATE predicate (`StartDate<=today<=EndDate`) was unkilled — the positive + control didn't bracket the date range. | HIGH | **FIXED-INLINE (#407)** — added a leave-ended-yesterday boundary arm (→ no delegation). |
| 2026-07-20 | ISSUE-173-FR6 · delegation | authenticator · MISS | Leave-STATUS gate (only `Approved` delegates) + the `delegation_user_not_found` / `delegation_user_missing_approve_permission` validation codes were untested. | MED | **FIXED-INLINE (#407)** — added a non-approved-leave arm + the two validation arms. |
| 2026-07-20 | ISSUE-173-FR6 · delegation | enforcer · ISSUE | A delegate who is also in the approver pool receives both the pool notice AND the delegated notice (double-notify). | LOW | **ACCEPTED** — the delegated message is the actionable one; double-notify is a minor UX nit, not a defect. |
| 2026-07-20 | ISSUE-173-FR6 · delegation | (feature, now built) · — | US-PAY-008 FR-6 approval delegation was 0% for payroll. | M–L | **RESOLVED (#407)** — per-step primary+delegate USER config; delegation auto-fires when the primary is on approved leave (notification/record overlay, authz unchanged); `payroll_approval_delegated` → the delegate. Enforcer WIRED (no money-path regression, tenant-safe); authenticator 100% (2 MATERIAL + 2 MED healed). ISSUE-173 fully RESOLVED. |
| 2026-07-20 | DF-21 · mybalance-batch | enforcer · — | Verified the N+1 → batch swap is WIRED: `ComputeProratedEntitlementsBatchAsync` invoked+consumed, per-type `ComputeEffectiveEntitlementAsync` fully removed from `LeaveDashboardService`, deleted `ResolveEntitlementAsync` reference-clean, 0-fallback behaviourally equal-or-safer, no consumer depended on the old shape. | — | **NO-FINDING** — VERDICT WIRED; nothing to heal. |
| 2026-07-20 | DF-21 · mybalance-batch | authenticator · WEAK | The `Entitlement.Should().Be(14m)` value-flow assertion collided with the seeded `LeaveType.AnnualEntitlement=14`, so a mutant reading the column instead of the batch map would survive. | LOW | **FIXED-INLINE (#408)** — stubbed a distinct 13.5m (≠ seeded 14) and asserted 13.5m, so only the batch-map path can produce it. Guard test itself verified AUTHENTIC (fails on revert: batch `Received(1)` + per-type `DidNotReceive`). |
| 2026-07-21 | DF-22 · per-location-sweeps | enforcer · — | Verified WIRED: both write-path sweeps (auto-clock-out + monthly-summary) resolve policy PER LOCATION via the pre-existing `LoadAllAsync`/`For`; old per-employee tenant-default read fully removed (N+1 killed); every changed-signature caller updated; auto-clock-out query reshape drops no log + no tenant-leak; null-policy code-default fallback preserved; absenteeism untouched; no BUG-289 regression. `InternalsVisibleTo HRM.Api` judged acceptable (advised AGAINST injected-service over-engineering). | — | **NO-FINDING** — VERDICT WIRED. |
| 2026-07-21 | DF-22 · per-location-sweeps | enforcer · TEST-HEALTH | The orphan-log fallback (open log whose employee row is missing → null location → code default → still auto-closed, never skipped) — the defensive branch — had no dedicated arm. | LOW | **FIXED-INLINE (#409)** — added `AutoClockOutJob_OpenLogWithMissingEmployee_IsStillClosed_ViaCodeDefault_DF22`. |
| 2026-07-21 | DF-22 · per-location-sweeps | authenticator · AUTHENTIC | Both new arms verified DISCRIMINATING (fail on pre-fix: 0.5-vs-1.0 present-days + 479-vs-539 work-minutes each trace to a single per-location field; collapse to equal on the old tenant-default read); no existing tenant-default arm weakened; InMemory adequate (resolver's PG index/precedence contract separately covered by `AttendancePolicyResolverTests` Testcontainers). | — | **NO-FINDING** — VERDICT AUTHENTIC 100%. |
| 2026-07-21 | DF-22 · per-location-sweeps | authenticator · MISSING (optional) | No arm locking "a per-location override does NOT alter the tenant-wide absenteeism report". | LOW | **ALREADY-COVERED** — `LeaveReportServiceTests.Absenteeism_UsesTheTenantDefaultThreshold_NotALocationOverride` (line 477) already seeds a Dubai override (99m) + tenant default (3m) and asserts the report uses 3; no new arm needed. |
| 2026-07-21 | DF-8 · reporting-chain | authenticator · AUTHENTIC | All 6 initial arms verified discriminating (positional order + Name/JobTitle on the walked output; real 2-node cycle → bounded+unique; genuine absent-rung truncation; real 404/tenant-unresolved Results). InMemory adequate (pure dictionary walk, no SQL/jsonb/timestamptz). | — | **NO-FINDING** on the 6. |
| 2026-07-21 | DF-8 · reporting-chain | authenticator · MISSING | No cross-tenant isolation arm — the truncation test used a random absent id, not a real other-tenant manager, so nothing proved tenant B's manager can't leak into tenant A's chain (BUG-003 class; DirectReports has this arm, GetReportingChain didn't). | MED | **FIXED-INLINE (#410)** — added `GetReportingChain_ManagerInAnotherTenant_ChainTruncatesAtTheBoundary_NoLeak` (seeds a tenant-B manager, points a tenant-A employee at it, asserts the chain is single-element and never contains the B manager id). |
| 2026-07-21 | DF-8 · reporting-chain | enforcer · — | Verified WIRED end-to-end: route `{id:guid}/reporting-chain` → `EmployeesController.GetReportingChain` → MediatR `GetReportingChainQuery` → handler → `IReportingStructureService.GetReportingChainAsync`; DI pre-registered; no route collision; `Employee.View.All` gate matches sibling; tenant scope intact (global filter, no IgnoreQueryFilters, foreign manager truncates); N+1-clean (1 projected query + 1 batched title lookup). | — | **NO-FINDING** — VERDICT WIRED. |
| 2026-07-21 | DF-8 · reporting-chain | enforcer · INFRA | Observed the test file mid-edit (malformed `[Trait]`s inside method bodies during the orchestrator's trait-fix pass). | HIGH (transient) | **RESOLVED** — traits fixed, clean rebuild (0 errors) + 26/26 green AFTER the fixes; final malformed-trait + MUTANT sweep clean. Was a snapshot-of-in-progress-edit, not a defect. |
| 2026-07-21 | DF-8 · reporting-chain | enforcer · TEST-HEALTH | No full-chain HTTP-route regression test (route→controller→MediatR→handler); dispatch proven by inspection + the InMemory service-layer arms, not an automated end-to-end test. Applies equally to the sibling `direct-reports` endpoint (repo convention). | MED | **FILED DF-57** — endpoint-wiring coverage gap, deferred (not DF-8-specific; the walk logic + tenant-scope ARE covered by the 7 InMemory arms). |
| 2026-07-21 | DF-4 · entitlement-reconcile-sweep | enforcer · — | Verified WIRED: recurring job registered (`AddOrUpdate<LeaveEntitlementReconcileJob>` id `leave-entitlement-reconcile`, cron `30 2 * * *`, no id collision) + DI-registered; per-tenant via `ITenantJobRunner` (fresh scope, RLS-safe); tenant predicate byte-identical to `LeaveAccrualJob`; UNCONDITIONAL drain (no `written>0` gate); fast-path enqueue untouched; idempotency reliance sound; per-tenant error isolation; no BUG-289 regression; TimeProvider seam trailing-optional. | — | **NO-FINDING** — VERDICT WIRED. |
| 2026-07-21 | DF-4 · entitlement-reconcile-sweep | authenticator · AUTHENTIC | All 4 arms discriminating on REAL written ledger rows (convergence w/o enqueue; idempotent 2nd run = 1 row no double-count; 3-tenant isolation w/ distinct deltas +5/+3 + Suspended-skip negative; runner-per-active-tenant). Fast-path test untouched. InMemory verified adequate — no PG-only surface (StartsWith is LINQ-to-objects post-materialize; OccurredAt UTC-kind; no jsonb/raw SQL). | — | **NO-FINDING** — VERDICT AUTHENTIC 100%. |
| 2026-07-21 | DF-4 · entitlement-reconcile-sweep | authenticator/enforcer · MISSING | No sweep test under the RLS-ON `TenantJobRunner` path (manual tx under EnableRetryOnFailure, BUG-068 class) — only the RLS-off/InMemory short-circuit is covered. | LOW | **PARKED at RLS decision-gate** — structurally unreachable (RLS committed OFF; shared infra covered by `RlsIsolationPostgresTests`). Add a Testcontainers sweep arm with `Rls:Enabled=true` at the staging/prod RLS flip (folds into the existing RLS-flip runbook, not a new DF). |
| 2026-07-21 | DF-9 · customfields-options | enforcer · — | Verified WIRED: all READ paths (getActive, getCustomFields/toListResponse, create/update/deactivate/activate responses) apply `normalizeDefinition` (parse string→array); all WRITE paths (create/update) `toWirePayload`→`serializeOptions` (array→string); 3 consumers (wizard/profile/admin-list) unchanged + now correct; parseOptions robust (null/malformed/array-passthrough); round-trip lossless; no other consumer leaks the raw string. | — | **NO-FINDING** — VERDICT WIRED. |
| 2026-07-21 | DF-9 · customfields-options | authenticator · AUTHENTIC | Mock correction genuine (read/list flush the true wire STRING `mockWireDefinition`, assert real string[]; write asserts a STRING body vs array input) — 4/5 DF-9 arms fail on revert; the null→null arm is honest boundary/no-throw doc, not theater. No weakening; no skip markers; not tautological. Service-boundary coverage sufficient (single normalization point; component test low marginal value). | — | **NO-FINDING** — VERDICT AUTHENTIC 100%. |
| 2026-07-21 | DF-9 · customfields-options | enforcer/authenticator · MISSING | `updateCustomField` shares the `toWirePayload`/`normalizeDefinition` path but had no dedicated spec arm (covered transitively by create). | LOW | **FIXED-INLINE (#412)** — added a PUT arm asserting `body.options === '["S","M","L","XL"]'` (string) + response parsed back to array. 15/15 Karma. |
| 2026-07-21 | DF-9 · customfields-options | enforcer · TEST-HEALTH | deactivate/activate specs flush an already-array mock, so their `normalizeDefinition` call only exercises the array-passthrough branch, not the wire-string parse. | LOW | **ACCEPTED** — cosmetic; the string-parse branch is proven via the active/list/create/update arms (same helper). Not worth churn. |
| 2026-07-21 | DF-7 · redis-portal-limiter | enforcer · — | Verified WIRED: `IPortalLinkIpRateLimiter` seam registered both ways off the SAME gate var as the denylist branch; service consumes it with a byte-identical 429; inline CountAsync fully removed; per-email guard untouched; Redis uses the shared multiplexer, per-(tenant,IP) key, INCR+EXPIRE-on-first, fail-open; DB fallback semantics identical; count parity (both block at 11th, no off-by-one); no DI lifetime mismatch; config defaults 10/3600. | — | **NO-FINDING** — VERDICT WIRED. |
| 2026-07-21 | DF-7 · redis-portal-limiter | enforcer · ISSUE | The interface XML doc promised "fail-open on any backing-store error", but the DB impl has no try/catch — a CountAsync failure propagates (benign: if the DB is down the subsequent token INSERT fails anyway → no regression). | LOW | **FIXED-INLINE (#413)** — narrowed the doc: the Redis impl fails open; the DB fallback lets store errors propagate (moot, since issuance itself then fails). |
| 2026-07-21 | DF-7 · redis-portal-limiter | enforcer · DECISION | Redis fixed-window vs DB sliding-window are not byte-identical across window boundaries (fixed-window allows ~2× at a boundary burst); within a clean window both are "10 allowed / 11th blocked". | LOW | **ACCEPTED** — documented intentional trade in both impls' XML + the PR; coarse anti-abuse throttle, house-style fixed-window (matches the ASP.NET FixedWindow limiters + RedisPermissionCache). |
| 2026-07-21 | DF-7 · redis-portal-limiter | authenticator · AUTHENTIC | Mutation-verified (3 injected mutants — Redis off-by-one, EXPIRE-every-call, DB window-predicate-drop — each killed exactly the intended test, then reverted). Redis fake is a stateful dict (real INCR round-trip, not an echo); DB tests seed real rows through real EF; DI-branch tests assert the real registered ImplementationType; existing service per-IP arms re-wired unchanged. | — | **NO-FINDING** — VERDICT AUTHENTIC 100%. |
| 2026-07-21 | DF-7 · redis-portal-limiter | authenticator · MISSING | (a) No exact-edge window test for the DB path (existing used -2h, safely outside); (b) no cross-backend parity oracle proving Redis + DB both block at exactly the 11th. | LOW | (a) **FIXED-INLINE (#413)** — added `WindowEdge_OneSecondInsideCounts_OneSecondOutsideDoesNot` (fixed clock, strict boundary). (b) **ACCEPTED** — both backends individually mutation-covered; semantics confirmed equivalent by the enforcer; a shared oracle is defense-in-depth on the last low-value Tier-B item. |
| 2026-07-21 | DF-50 · dashboard-warmup-parallelize | enforcer · — | Verified WIRED + **TENANT GATE PASSES**: `ParallelTenantScopeRunner` snapshots the full parent-tenant field set before fan-out and re-seeds each child scope's `ITenantContext` (`SetTenant`/`SetSystemContext`) BEFORE any factory query — no widget path can query an unresolved (fail-open) child context; runner is request-scoped; every DB factory resolves from the child scope not parent `_db`; cache key correct in-child; bounded via SemaphoreSlim; warmup non-blocking + never crashes startup; output/order/failure equivalent to sequential. | — | **NO-FINDING** — VERDICT WIRED (gate holds). |
| 2026-07-21 | DF-50 · dashboard-warmup-parallelize | enforcer · NOTE | Forward-looking: the runner is only ever driven single-tenant (all children same parent tenant); a FUTURE cross-different-tenant fan-out would need an AsyncLocal / 2nd-level-cache-prefix re-audit. | — | **DOCUMENTED** — baked the caveat into the runner's XML remarks (single-tenant-fan-out-only). |
| 2026-07-21 | DF-50 · dashboard-warmup-parallelize | authenticator · AUTHENTIC | The cross-tenant isolation gate test is genuinely discriminating (kills the SetTenant-removal mutation on 4 assertions via the verified fail-open Employee/LeaveRequest filters + a correctly-shared InMemoryDatabaseRoot; A=3/2 vs B=7/10 distinguishes leak from correct). Runner tests strong (order/bounded-concurrency observed >1 and ≤dop/propagate). Existing dashboard tests unweakened, now run through the parallel path. | — | **NO-FINDING** — VERDICT AUTHENTIC ~100%. |
| 2026-07-21 | DF-50 · dashboard-warmup-parallelize | authenticator · WEAK/MISSING | (a) `StartAsync_DoesNotBlock` didn't actually prove fire-and-forget (the fault was swallowed inside WarmAsync, so `await WarmAsync` would pass too); (b) no `Dashboard:WarmupEnabled=false` skip-path test. | LOW | **FIXED-INLINE (#415)** — (a) added `StartAsync_IsFireAndForget_ReturnsWhileTheWarmTaskIsStillRunning` (spy scope factory blocks the warm inside CreateScope; asserts StartAsync already completed while warm is stuck) + renamed the old to `…NeverThrows_EvenWhenTheDetachedWarmFaults`; (b) added `StartAsync_WhenWarmupDisabled_NeverCreatesAScope`. |
| 2026-07-22 | DF-19 · pool-aware-carryforward | enforcer · BUG | **CRITICAL** — the DF-19 expiry job reads the persisted `ConsumedDays`, but the first cut bumped it ONLY on leave approval; HR encashment (`LeaveEncashmentService`) + both LOP paths (`LopService` compulsory + conversion) consumed carry-forward-year balance without bumping it → year-end expiry over-forfeits already-consumed carried days (money regression, balance driven negative). Backfill counts Used+Encashed; runtime didn't. | CRIT | **FIXED-INLINE (#427)** — extracted one shared `PooledLeaveLedger.AppendDeductionAsync` (tags pool + bumps `ConsumedDays` + `Undo` for rollback); routed ALL 4 consumption paths through it (approval delegates, encashment w/ adjustment-reject `Undo`, compulsory, conversion). Re-audit VERDICT **PASS** — CRITICAL closed, backfill↔runtime parity + rollback atomicity + multi-row integrity confirmed. |
| 2026-07-22 | DF-19 · pool-aware-carryforward | enforcer · BUG | Year-end auto-encashment (`LeaveCarryForwardService.cs:189-192`) writes an `Encashed` row for the forfeitable excess without bumping `ConsumedDays` — same bug class as the CRITICAL but for the batch carry/forfeit sweep; narrow config (Active `ToYear=fromYear` bucket at close). Enforcer ~35% live-reachable, defensible under strict FIFO. | LOW | **FILED DF-65** — NOT wired in #427 on purpose (sweep semantics ≠ per-transaction consumption; carried-first helper could double-count the sweep's own forfeiture). Needs-decision. |
| 2026-07-22 | DF-19 · pool-aware-carryforward | enforcer · DEAD-CODE | `LeaveCarryForwardCalculator.RemainingCarriedDays` has zero production callers after expiry switched to the inline `carried − consumed − expired`; kept alive only by its own 3 unit tests. | LOW | **ACCEPTED** — do NOT delete (still test-referenced per repo contract); owner decides whether to re-point/retire those tests later. |
| 2026-07-22 | DF-19 · pool-aware-carryforward | authenticator · AUTHENTIC | The DF-19 money arms drive the REAL approve/cancel/expiry (InMemory-through-real-EF), not hand-seeded rows; arm 1 is mutation-sensitive both ways (reverting the cancel `ConsumedDays` decrement OR reverting expiry to the derived sum turns it red); the edited `LeaveCarryForwardServiceTests` arms were adapted (same expected forfeitures), not loosened; `CancelLeaveRequestServiceTests` untouched (legacy fallback still covered). | — | **NO-FINDING** — VERDICT AUTHENTIC. |
| 2026-07-22 | DF-19 · pool-aware-carryforward | authenticator · WEAK/MISSING | (a) arm 2 didn't pin the terminally-expired bucket's `ConsumedDays`/`ExpiredDays`; (b) two edited expiry arms keep `SeedUsed == ConsumeCarried`, so they pass under both old-derived + new-counter models (discriminating power concentrated in the lifecycle arm); (c) intermediate `BalanceAfter` + the `PoolRowTickOffset` ordering seam not directly asserted; (d) no real-Postgres arm for the µs-truncation ordering + backfill. | LOW/MED | (a) **FIXED-INLINE (#427)** — pinned dead-bucket `ConsumedDays=5`/`ExpiredDays=5`. (b,c) **ACCEPTED** — the approve→cancel→expiry lifecycle arm carries the discrimination; tick-offset proven correct by construction (1µs = exact µs grid multiple). (d) **VERIFIED on real PG (2026-07-22, Docker restored)** — `FinalSettlementPostgresTests` 3/3 GREEN on a live Postgres container: exercises the encashment draw-down through `PooledLeaveLedger` (incl. the µs tick-offset row ordering) + the `20260722005029` backfill (migrations apply on container startup), money figure correct. A dedicated pool-row-ordering assertion arm stays optional defense-in-depth (ordering is correct by construction: 1µs offset = exact timestamptz µs-grid step). |
| 2026-07-22 | DF-63 · autoclockout-shift-aware | enforcer · — | Verified CONNECTED: `AutoClockOutJob` resolves `IShiftService` from the SAME per-tenant scope the `ITenantJobRunner` sets `SetTenant`/GUC on (shared scoped `AppDbContext`+`ITenantContext`), so shift reads are tenant-scoped (+ under RLS-on the GUC-in-tx applies); `ResolveForEmployeeAsync` also guards `IsResolved`. `IShiftService` registered in the real root (`DependencyInjection.cs:241`), job scheduled `Program.cs:757` → no prod orphan. Failure→null→byte-identical math; rotation reload targets the concrete step shift; cache key `(employee,date)` correct; DF-22 batch + paging intact. | — | **NO-FINDING** — VERDICT CONNECTED. |
| 2026-07-22 | DF-63 · autoclockout-shift-aware | enforcer · MISSING | The DF-63 integration arm runs InMemory with `Rls:Enabled` false, so `TenantJobRunner` short-circuits (no tx/GUC) — the RLS-on tenant fence for the new shift reads is never executed; PASS rests on shared-scope tracing, not a test. | MED | **PARKED at RLS decision-gate** — same class as the DF-4 RLS-path MED; RLS committed OFF, shared-scope traced-safe. Add a Testcontainers `Rls:Enabled=true` arm at the staging/prod RLS flip (folds into the RLS-flip runbook, not a new build). Tracked as [[DF-63-rls]]. |
| 2026-07-22 | DF-63 · autoclockout-shift-aware | enforcer · NOTE | (a) `ResolveForEmployeeAsync` issues ~6-7 queries per distinct `(employee,date)` (incl. an unused `GetAssignedCountsAsync`), not "2" — but memoization is correct/effective (declared per-tenant, caches nulls, holds across pages) and acceptable for a nightly safety-net; (b) redundant reload-by-id (the DTO already carries the knobs) — one avoidable hit, but MATCHES the `AttendanceService` clock-out pattern I mirrored; (c) UTC date basis for rotation resolution — documented tenant-timezone deferral. | LOW×3 | **ACCEPTED** — (a) memoization sound, safety-net cost tolerable; (b) kept for consistency with the existing clock-out path (enforcer: "not worth changing"); (c) revisit with tenant-timezone infra (same deferral as the close boundary). |
| 2026-07-22 | DF-63 · autoclockout-shift-aware | authenticator · AUTHENTIC | The default-shift arm's 479-vs-539 is uniquely attributable to the shift's 120 break (no Location override seeded; 479 has one arithmetic source); resolution genuinely exercised (real Employee row → FR-5 default fallback → real entity); `Substitute.For<ICurrentUser>()` verified inert (resolve path never reads `_currentUser`); 0 mocks in the SUT path; 4 pre-existing arms still meaningful (now also cover no-shift→null). Mutation (remove `shift:` → 539 RED) is the correct kill. | — | **NO-FINDING** — VERDICT AUTHENTIC 100%. |
| 2026-07-22 | DF-63 · autoclockout-shift-aware | authenticator · MISSING | The DF-63 arm uses the FR-5 default-shift fallback, so the `EmployeeShift` assignment path (FR-7, the primary production case) is unproven — a broken assignment join would keep the arm green via the default fallback. | MED | **FIXED-INLINE (#428)** — added `AutoClockOutJob_ResolvesAssignedShiftPerEmployee_AcrossOneSweep_DF63`: one sweep, employee X assigned a shift (break 90→509) vs employee Y on the default (120→479) vs tenant (60→539) — three distinct values prove per-employee FR-7 resolution. 509 has one source (the assigned shift). |
| 2026-07-22 | DF-63 · autoclockout-shift-aware | authenticator · MISSING | No DF-63-labelled explicit "null shift → byte-identical tenant math (539)" assertion — the property is only incidentally covered by the DF-22 default-location arm under DF-63 code. | LOW | **ACCEPTED** — incidentally covered (authenticator: "not strictly required"); the failure→null branch is exercised by the DF-22 orphan + no-default-shift arms. |
| 2026-07-22 | DF-65 · yearend-forfeit-supersede | enforcer · — | Verified PASS (adversarial, money path): the double-forfeiture is closed in ALL cases — (b) terminalize runs UNCONDITIONALLY after the forfeit block (covers no-forfeit-all-carried + forfeited<remaining + forfeited>=remaining; no-op when expiryMonths<12 already Expired). Balance integrity: forfeit nets currentBalance−totalForfeited, two-call encashable threading correct, forfeit rows keyed LeaveYear==fromYear. (a)+(b) same tracked bucket instance (identity map) → one coherent SaveChanges. Preview side-effect-free, idempotency guard precedes (b), new bucket (ToYear==toYear) correctly excluded. All queries tenant-scoped, no IgnoreQueryFilters. | — | **NO-FINDING** — VERDICT PASS. |
| 2026-07-22 | DF-65 · yearend-forfeit-supersede | enforcer · NOTE | LOW-1: (a) bumps ConsumedDays on a bucket (b) terminalizes in the same unit of work → cosmetic for that bucket (belt-and-suspenders, user-decided). LOW-2 (pre-existing, out of lane): the fix assumes monthly expiry runs before the sweep for expiryMonths<12 buckets; DF-65 strictly reduces double-forfeiture in every ordering, so no change. | LOW×2 | **ACCEPTED** — (a)+(b) is the user's explicit choice; LOW-2 pre-existing job-ordering assumption not introduced here. |
| 2026-07-22 | DF-65 · yearend-forfeit-supersede | authenticator · AUTHENTIC | Real domain math (re-derived unused=8→carry5/forfeit3, ConsumedDays bump, expiry 0); BOTH fix-halves independently killed on distinct assertions (revert (a)→ConsumedDays≠3; revert (b)→Status≠Consumed + double-forfeit); off-target toYear-keying mutant also killed. Fixture faithfully mirrors a real prior sweep (bucket + carry-in credit); no theater, no unfailable assertions, tick-offset path not hit (provider-safe). | — | **NO-FINDING** — VERDICT AUTHENTIC 100%. |
| 2026-07-22 | DF-65 · yearend-forfeit-supersede | authenticator · MISSING | (i) encashable two-draw path (Encashed + Expired residual balance threading) untested; (ii) ForfeitedDays==0 all-carried case proving (b) load-bearing alone; (iii) real-Postgres arm. | MED / MED-HIGH / LOW | (i)+(ii) **FIXED-INLINE (#430)** — added `…EncashableForfeit_ThreadsTwoPooledDraws…` and `…AllCarriedNothingForfeited_StillSupersedesPriorBucket…` (both mutation-relevant; the all-carried arm needed a positive-entitlement type — zero-entitlement is skipped by `AppliesTo`). (iii) **FILED [[DF-65-pg]]** — provider-agnostic per both auditors, tick-offset unexercised, 638/638 real-PG regression clean. |
| 2026-07-22 | DF-64 · regularized-ot-record | enforcer · BUG | **HIGH-1 (ship-blocking, caught pre-merge):** the OT block removed prior `Type==AutoDetected` records for the log with NO status guard → a later same-day regularization approval could DELETE an Approved/payroll-ready `OvertimeRecord` and cascade-delete its immutable `OvertimeApprovalHistory` (`OnDelete(Cascade)`). Period lock only protects locked periods; an approved-but-not-yet-locked OT block was exposed; also silently re-opened a Rejected decision. | HIGH | **FIXED-INLINE (#431)** — added a `hasTerminalDecision` guard (Approved/Rejected/IsPayrollReady → skip the whole block) + `supersededPending` filter (only Pending/Unapproved removed). **Re-audit VERDICT PASS** — HIGH-1 closed on a provably-closed 4-value status set; terminal records doubly protected. Mutation-proven arm `…PreservesApprovedRecord…`. |
| 2026-07-22 | DF-64 · regularized-ot-record | enforcer · — | Verified CONNECTED: `IOvertimeService` registered (`DependencyInjection.cs:245`) + injected; both approve paths (direct `ApproveCoreAsync` + workflow `StageRegApprovalAsync`) funnel through `ApplyToAttendanceLogAsync` → OT block; `BuildAutoDetectedAsync` returns unsaved → caller's single SaveChanges commits atomically; shared scoped `AppDbContext`; tenant-scoped; Pending/IsPayrollReady=false (not auto-paid); lock-before-mutate on both paths; new-log path has no priors → no duplicate. | — | **NO-FINDING** — VERDICT CONNECTED. |
| 2026-07-22 | DF-64 · regularized-ot-record | enforcer · NOTE | Mixed-set (a pre-existing anomalous Pending + Approved auto-detected pair on one log): the guard short-circuits → the stray Pending is left un-superseded. Harmless (only Approved is payable; the pair can't be produced by this code path). LOW-2: weekly-cap advisory double-count (RemoveRange not flushed before the weekly sum). LOW ISSUE: lock tests tenant-local reg.Date vs UTC record.Date (pre-existing clock-out property). | LOW×3 | mixed-set **ACCEPTED** (conservative-preserve, documented); weekly-cap **FILED [[DF-64-wk]]**; date-basis **ACCEPTED** (pre-existing, not introduced). |
| 2026-07-22 | DF-64 · regularized-ot-record | authenticator · AUTHENTIC | 180 OT derived through the REAL `OvertimeService.BuildAutoDetectedAsync` + `AttendanceCalculator` (660 net − 480 std), not echoed; create arm proves a payable record exists where none did (the DF-64 gap); remove arm genuinely hits the null-rebuild branch; real service (not mock) + shared scoped context mirror DI faithfully; `&&false` mutation reds both. | — | **NO-FINDING** — VERDICT AUTHENTIC. |
| 2026-07-22 | DF-64 · regularized-ot-record | authenticator · MISSING | (i) UPDATE/replace arm (prior AutoDetected exists AND new one rebuilt) — a de-dup-skip mutation survived both original arms; (ii) `RequireOvertimePreApproval=true → Unapproved` status split (LOW, OvertimeService's own concern); (iii) real-Postgres arm. | MED / LOW / MED | (i) **FIXED-INLINE (#431)** — `…ChangesOvertime_ReplacesRecord_NoDuplicate…` (120→180, ContainSingle). (ii) **ACCEPTED** — OvertimeService covers the status split. (iii) **FILED [[DF-64-pg]]**. |
| 2026-07-22 | DF-61 · payroll-rerun-dedup | enforcer · — | Verified CONNECTED + functionally sound (ship it): marker column mapped + migration + snapshot; RerunAsync set (committed with audit row, after all guard returns) / ProcessAsync clear (every completion) / sweep re-enqueue all wired; race safety holds for a STARTED worker (Processing filtered) + null-marker (processed) filtered; reset-to-now bounds re-fire; tenant-scoped; migration nullable-timestamptz Utc-safe (no BUG-289). Guard matrix + Queued path unchanged. | — | **NO-FINDING** — VERDICT CONNECTED. |
| 2026-07-22 | DF-61 · payroll-rerun-dedup | enforcer · NOTE | MED: class doc OVERSTATED race-freedom ("no longer races a live worker") — true only against a STARTED worker, not a queued-but-delayed one (enqueue→start window > StaleThreshold, no `DisableConcurrentExecution`/token; shared with DF-58, never double-PAYS). LOW: enqueue precedes the reset SaveChanges (re-fire unbounded on a save failure, inert). LOW: marker leaks on stuck-Processing/cancelled runs are filtered out (cosmetic). | MED + LOW×2 | MED **FIXED-INLINE (#432)** — softened the class doc + inline comment to the DF-58 "10-min bet" framing. LOWs **ACCEPTED** (inert). Concurrency window **FILED [[DF-61-conc]]**. |
| 2026-07-22 | DF-61 · payroll-rerun-dedup | authenticator · AUTHENTIC | 4 sweep arms drive the real job.RunAsync over real DI+EF (scheduler is the observable seam, not a mock-everything); mutation-resistant (staleness-inversion + marker-reset-drop both killed); reset assertion load-bearing; integration round-trip drives BOTH ends (RerunAsync set / ProcessAsync clear) through real MediatR + processor, would fail if the code were deleted. | — | **NO-FINDING** — VERDICT AUTHENTIC. |
| 2026-07-22 | DF-61 · payroll-rerun-dedup | authenticator · MISSING | (a) the "integration" arm is InMemory NOT real Postgres (I mislabeled it) → no real-PG coverage of the marker round-trip/staleness query; (b) mixed-batch (Queued + stale-rerun → exactly 2 enqueues); (c) two-sweep re-fire bounding; (d) rerun-path tenant isolation. | MED / MED / MED / LOW | (b,c,d) **FIXED-INLINE (#432)** — added the 3 arms (compose=2, two-sweep=Received(1), active-vs-suspended isolation). (a) **FILED [[DF-61-pg]]** — real risk LOW (Utc kinds, translatable query, column exercised as-null by 274/274 PG payroll tests). |
| 2026-07-22 | DF-61-conc · payroll-reprocess-interlock | enforcer · WIRED | Verdict WIRED (safe to merge): the per-runId advisory lock is reached before the replace-clean, disposed on every ProcessAsync return path, provider-gated (InMemory→null→unaffected), and proven by deterministic real-PG arms. `InternalsVisibleTo` present; no orphaned members. | LOW×2 | Both **ACCEPTED/FILED** (non-blocking, neither a regression from #434): retry-reconnect can move work off the lock-holding connection → **FILED [[DF-61-conc-retry]]** + XML-doc caveat inline (#434); reprocess racing into AwaitingApproval/Approved/Rejected un-submits approval (PRE-EXISTING, mirrors original Finalized/Cancelled-only guard) → **FILED [[DF-61-conc-approval-race]]**. |
| 2026-07-22 | DF-61-conc · payroll-reprocess-interlock | authenticator · AUTHENTIC | Verdict AUTHENTIC 10/10: every arm asserts committed state via a FRESH DbContext (`AsNoTracking`+reload); the contended-no-op proves the loser did NOTHING (Queued + 0 slips), the already-satisfied skip uses a real CompletedAt/TotalGross sentinel a re-run would overwrite, and different-runId non-serialization is proven by real effect (B→ReviewPending while A→Queued), not just the key formula. All mutation-sensitive. | LOW | **FILED [[DF-61-conc-slip]]** — optional real-slip duplication arm (interlock arms use zero-employee runs; contended-no-op mutant already killed by the 0-slip arms). Exact-threshold boundary noted, low value. |
| 2026-07-22 | DF-64-wk/pg · regularized-ot-record | enforcer · WIRED | Verdict WIRED: the DF-64-wk exclusion flows unbroken RegularizationApprovalService(:687)→BuildAutoDetectedAsync→EvaluateWeeklyCapAsync Where(:963); excluded ids ARE the `RemoveRange` set (same atomic commit → no wrong-suppression, 95%); `Unapproved` over-exclusion is a harmless no-op (never summed); clock-out caller unaffected by the trailing-optional-after-CT param. New PG class self-contained, trait-bound. | LOW×1 | Style note (param after CT) ACCEPTED — matches the repo's trailing-optional pattern; a future positional caller must use a named arg. TC-traceability OOL RESOLVED-INLINE (#435) — TC-ATT-006-64/65 docs + matrix rows created. |
| 2026-07-22 | DF-64-wk/pg · regularized-ot-record | authenticator · AUTHENTIC | Verdict AUTHENTIC 4/4: DF-64-wk arm mutation-sensitive (cap 300; real 180 ≤ cap AND real+superseded 360 > cap — deleting the exclusion flips WeeklyCapExceeded and fails); DF-64-pg Approved-preserve arm seeds + asserts the `OvertimeApprovalHistory` row SURVIVES (the FK-cascade proof InMemory can't give, `ON DELETE CASCADE` confirmed); supersede arm proves exactly one record; SUT + OvertimeService + EF all real. | — | **NO-FINDING** — VERDICT AUTHENTIC. No stronger assertions needed. |
| 2026-07-22 | DF-63-rls · autoclockout-rls-on | enforcer · WIRED | Verdict WIRED (no gaps, severity none): arm genuinely on the RLS-ON path — hrm_app (NOBYPASSRLS) + `Rls:Enabled=true` → `TenantJobRunner` sets `app.current_tenant`; FORCE-RLS reconciler covers all 4 tables the job reads (attendance_logs/shifts/employee_shifts/attendance_settings); assertions are fail-closed sentinels; per-test container → contamination structurally impossible; helpers reused, no orphans. | LOW×2 (both PRE-EXISTING) | **FILED [[DF-63-shiftdoc]]** (stale ShiftService "no RLS" doc) + **FILED [[DF-63-clockseam]]** (AutoClockOutJob no TimeProvider seam). Neither introduced by #436 (test-only diff). |
| 2026-07-22 | DF-63-rls · autoclockout-rls-on | authenticator · AUTHENTIC | Verdict AUTHENTIC 1/1, RLS-decisive: the 479/539/569 `TotalWorkMinutes` discriminator verified against the real AttendanceCalculator + ShiftService (own-shift 120→479 vs shift-fail-closed settings-60→539 vs SUSPENDED-30→569 cross-tenant bleed); a dropped GUC fail-closes to 0 rows → assertion (a) reds; committed state read via BYPASSRLS owner + IgnoreQueryFilters so failure can't hide. Mutation table: Rls off / un-wire shift / drop GUC / cross-tenant read all red. | — | **NO-FINDING** — VERDICT AUTHENTIC. |
| 2026-07-22 | DF-65-pg/DF-62-parity · leave-forfeiture | enforcer · WIRED | Verdict WIRED (1 traceability GAP): DF-65-pg drives the real ProcessYearEndAsync→PooledLeaveLedger→ProcessExpiryAsync (engine-stub only substitute, mutation-sensitive on the (b) supersede); DF-62-parity uses the REAL PreviewYearEndAsync + Compute (via InternalsVisibleTo) anchored to the live LeaveEncashmentService 422 boundary — no money-math file touched; vault doc accurate. | MED + LOW | MED: TC-LV-269 had no doc → **RESOLVED-INLINE (#437)** (authored TC-LV-269.md + matrix row under US-LV-008). LOW: tick-offset arm's mutation-resistance was CreatedAt-tiebreak-dependent → **FIXED-INLINE (#437)** deterministic OccurredAt-delta assertion. |
| 2026-07-22 | DF-65-pg/DF-62-parity · leave-forfeiture | authenticator · WEAK→FIXED | Parity file AUTHENTIC 2/2 (real derivations, both mutation-resistant, accepted-edge framing clear). DF-65-pg arm 1 AUTHENTIC (stronger than claimed — reverting (b) reds 3 asserts). DF-65-pg arm 2 (tick-offset) WEAK: offset=0 mutant survived probabilistically (both rows tie on OccurredAt AND CreatedAt — one batch timestamp). | LOW (arm) + LOW (MISSING) | Weak arm **FIXED-INLINE (#437)** — added the deterministic `accrualRow.OccurredAt.Should().BeAfter(...)` + ≥10-tick delta assertion; re-run 2/2 green. MISSING encashable-forfeit real-PG branch → **FILED [[DF-65-pg-encash]]**. |
| 2026-07-23 | US-PLT · encryption-key-rotation | enforcer · CONNECTED | Verdict CONNECTED: EncryptedFieldRegistry drift-guard REAL (old hand-list deleted; both DbInitializer + maintenance service enumerate it); RLS-safe (tenants RLS-exempt → per-tenant RunForTenantAsync GUC before any tenant query; new system table no-TenantId → auto-exempt); raw SQL identifiers registry-constant + values parameterized + tenant_id-scoped; permission Tenant.Lifecycle real+SystemAdmin-only; migration/snapshot consistent. | MED×2, LOW×2 | MED soft-deleted-tenant filter → **FIXED-INLINE (#438)** (IgnoreQueryFilters + new PG arm); MED reverse-drift guard → **FIXED-INLINE (#438)** (model→registry sweep + IEncryptedValueConverter marker); LOW route `api/v1/admin`→`api/v1/system` **FIXED-INLINE**; LOW AddScoped job DI **FIXED-INLINE**. HTTP-authz arm → **FILED [[DF-enc-http-authz]]**; RLS-prod grant → **FILED [[DF-enc-rls-grant]]**. |
| 2026-07-23 | US-PLT · encryption-key-rotation | authenticator · AUTHENTIC (post-heal) | Registry 8/8 + reverse-drift arm; watchdog 6/6 (>=90 boundary pinned, first-seen upsert mutant killed); re-encrypt PG 3/3 (byte-identical active-key EXACT string equality, corrupt-skip+idempotency, soft-deleted arm, exact before/after report counts) — full real SUT, zero mocks. Flagged weak model-binding arm (ProviderClrType==string) → healed via IEncryptedValueConverter marker; missing soft-deleted arm → added. | LOW | national_id startup-backfill exclusion → **FILED [[DF-enc-nationalid-backfill]]** (decision); platform TC doc structure → **FILED [[DF-plt-tc-structure]]**. |
