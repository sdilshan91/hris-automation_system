---
id: TC-PRF-002-14
user_story: US-PRF-002
module: Performance Management
priority: high
type: performance
status: blocked
created: 2026-06-16
exec_note: "P3b k6 2026-06-30: NOT MEASURED — kept blocked. Target My Review self-assessment form-load (NFR-1 <=400ms, up to 10 goals) needs performance-cycle data-volume seed (open self-assessment window + draft); Performance Management endpoints had no seeded volume in the 5k perf tenant. Perf harness exists (perf/), re-run after seeding."
---

# TC-PRF-002-14: Self-assessment form loads within 400ms P95 including all goal data (NFR-1)

## 1. Test Objective
Verify NFR-1: the My Review self-assessment form, including all assigned goals and any existing draft values, loads within 400ms at the 95th percentile under representative load.

## 2. Related Requirements
- User Story: US-PRF-002
- Non-Functional Requirements: NFR-1
- Acceptance Criteria: AC-1 (form display)

## 3. Preconditions
- acme cycle FY26-H1 self-assessment window open; Asha has a representative goal count (e.g. up to 10 goals) with an existing draft.
- A seeded performance environment with representative data volume; warmed caches per the deployed config.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Goals on form | up to 10 (BR-2 max) | representative |
| SLA | P95 <= 400ms | NFR-1 |
| Sample size | >= 200 requests | statistically meaningful P95 |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Issue >= 200 loads of the My Review form data endpoint for Asha/FY26-H1 under representative concurrency | Each response returns the full goal set + draft values. |
| 2 | Record server-side latency percentiles | P95 <= 400ms for the form-load endpoint (NFR-1). |
| 3 | Confirm queries are tenant-filtered | Each load issues tenant-scoped queries (no full-table scans across tenants); a per-employee/cycle cache (if any, S10) is tenant-keyed -- CONDITIONAL, asserted as tenant-filtered queries if computed on demand. |
| 4 | Verify P95 holds at the 10-goal upper bound | The SLA is met even for the maximum goal count. |

## 6. Postconditions
- The self-assessment form meets the 400ms P95 load SLA with tenant-scoped data access.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [x] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
