---
id: TC-PRF-005-14
user_story: US-PRF-005
module: Performance Management
priority: high
type: functional
status: blocked
created: 2026-06-16
---

# TC-PRF-005-14: Boundary — minimum reviewers per category enforced; radar renders with 3/5/10 competencies (FR-3, AC-4)

## 1. Test Objective
Verify FR-3 and AC-4 at the boundaries: the configurable minimum number of reviewers per category is enforced (e.g. at least 2 peers — exactly the minimum is accepted, one below is flagged), and the aggregated results radar/per-competency view renders correctly across varying competency counts (3, 5, 10) without breaking the chart or aggregation.

## 2. Related Requirements
- User Story: US-PRF-005
- Acceptance Criteria: AC-4
- Functional Requirements: FR-3, FR-4

## 3. Preconditions
- 360-enabled cycle "FY26 Annual Review" active for acme; minimum peer reviewers configured = 2.
- Seeded employees with feedback sets that hit each boundary (1 peer vs 2 peers; competency sets of 3, 5, 10).
- HR Officer "Nadia Khan" (`Performance.Review.All`) authenticated.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Min peers | 2 | FR-3 |
| Peer counts tested | 1 (below), 2 (exactly min), 3 (above) | boundary |
| Competency counts | 3, 5, 10 | radar render range |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Configure 2 as the peer minimum; submit feedback with exactly 2 peers | Results release is permitted at exactly the minimum (cross-ref TC-PRF-005-04); per-category averages compute over 2 peers. |
| 2 | Reduce to 1 submitted peer | Below-minimum is flagged/blocked at release (BR-4) — boundary of "one fewer than required". |
| 3 | Render the results radar with a 3-competency cycle | Radar renders 3 axes correctly; averages per competency shown; no chart error. |
| 4 | Render with 5 competencies | Radar renders 5 axes; legend distinguishes self/manager/peer/report. |
| 5 | Render with 10 competencies | Radar renders 10 axes without overflow/clipping; per-competency bars remain legible; performance stays within NFR-4 (cross-ref TC-PRF-005-11). |
| 6 | Category with exactly 1 reviewer (e.g. 1 direct report) vs 0 reviewers | A single-reviewer category averages that one rating; a zero-reviewer category is handled per the documented rule (excluded/renormalized), consistent with TC-PRF-005-08 step 5. |

## 6. Postconditions
- Per-category minimums enforced at the boundary; radar/aggregation correct for 3, 5, and 10 competencies.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [x] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test

## 8. Execution Log — 2026-09-02 (`@test-runner`, REPORT-ONLY)

**Verdict: BLOCKED.** The threshold-boundary half (steps 1-2) is green at the automated layer; the radar
render half (steps 3-6) was NOT executed at all. No arm FAILED. **Step 1 is additionally blocked by a
product gap, not just by budget** — see ISSUE-432.

**Environment:** `/health` → **200** (`2026-09-01T23:56:09.75Z`), backend healthy on :5000.

| Step | Layer executed | Result | Evidence |
|---|---|---|---|
| 1 (configure min = 2; exactly 2 peers → release permitted) | Automated (integration) for the *enforcement*; the *configuration* action is **impossible** | **PARTIAL** | `Integration.Feedback360IntegrationTests.Release_ExactlyAtMinimumPeers_Succeeds` → **Passed** (`>=` boundary correct). But "Configure 2 as the peer minimum" cannot be performed by any user: `AppraisalCycle.Min360PeerReviewers` is settable **only** by the EF default of 2 — see ISSUE-432. |
| 2 (reduce to 1 peer → blocked at release) | Automated (integration) | **PASS at that layer** | `Release_BelowPeerThreshold_Returns422_AndWritesNoRow` → **Passed**; `Release_BelowPeerThreshold_SendsNoNotification` → **Passed** |
| 3 (radar, 3 competencies) | — | **NOT EXECUTED** | no seeded competency sets; UI never driven. |
| 4 (radar, 5 competencies + legend) | — | **NOT EXECUTED** | as above. |
| 5 (radar, 10 competencies, no clipping) | — | **NOT EXECUTED** | as above. |
| 6 (1-reviewer vs 0-reviewer category) | — | **NOT EXECUTED** | partially adjacent: `Unit.Feedback360ServiceTests.Results_AggregatesPerCompetency_PerCategory_AndComposite` → **Passed**, but it does not cover the 0-reviewer renormalization rule this step asks for. |

**Automated suite run:** `bash scripts/run-backend-tests.sh src/backend/HRM.sln --filter
"FullyQualifiedName~Feedback360" -v n` → **Total tests: 74 · Passed: 74 · Failed: 0**, exit 0,
no ISSUE-312 abort marker.

**Blocking reasons:**
1. `blocked: no-seed-data` — steps 3-5 need three 360 cycles with 3 / 5 / 10 competencies and submitted
   feedback. This DB has **0 appraisal cycles** and 2 employees at start; building even one cycle consumed
   the run's remaining budget (and surfaced BUG-431 on the way).
2. `blocked: not-configurable` — step 1's precondition ("minimum peer reviewers configured = 2") cannot be
   *configured*; it is only ever the schema default. Filed as **ISSUE-432**. The boundary is still testable
   at the default value of 2, which is what the green automated arms above exercise.

**To finish:** expose/seed `Min360PeerReviewers`, seed the three competency-count cycles with feedback, then
drive `GET .../360/cycles/{cycleId}/employees/{employeeId}/results` + the radar UI for 3/5/10 axes.
