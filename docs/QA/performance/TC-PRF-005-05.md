---
id: TC-PRF-005-05
user_story: US-PRF-005
module: Performance Management
priority: critical
type: security
status: blocked
created: 2026-06-16
---

# TC-PRF-005-05: Unauthorized user configuring 360 / submitting on another's behalf is rejected (authz)

## 1. Test Objective
Verify authorization on the 360 surface: only an HR Officer with `Performance.Review.All` (or the reviewee's reporting manager, per AC-1) may configure 360 reviewers and release results; a regular employee, a non-managing manager, or an unauthenticated caller is blocked. A reviewer may only submit feedback for an assignment that belongs to them (no submitting on another reviewer's behalf, incl. IDOR).

## 2. Related Requirements
- User Story: US-PRF-005
- Acceptance Criteria: AC-1, AC-2, AC-3
- Functional Requirements: FR-1, FR-2
- Non-Functional Requirements: NFR-2

## 3. Preconditions
- 360-enabled cycle "FY26 Annual Review" active for acme; Liam Carter under review with assignments created.
- Employee "Priya Nair" (no Performance permissions) authenticated in acme.
- A manager "Raj Patel" who is NOT Liam's manager and lacks `.All`, authenticated in acme.
- Peer reviewer "Ava Reyes" and a different reviewer "Ben Cho", both authenticated in acme.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Unauthorized employee | Priya Nair | no perms |
| Non-managing manager | Raj Patel | not Liam's manager, no .All |
| Reviewer A / B | Ava Reyes / Ben Cho | distinct assignments |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | As Priya, call `POST .../performance/360/{liamId}/assignments` and `GET` the 360 config UI | 403 Forbidden; the config UI is not reachable (role guard) — no reviewer can be configured. |
| 2 | As Raj (non-managing manager without `.All`), attempt to configure Liam's 360 and to release results | 403 Forbidden — AC-1 limits configuration to HR `.All` or the reviewee's own reporting manager. |
| 3 | Unauthenticated: call the 360 config/submit/release endpoints with no bearer | 401 Unauthorized. |
| 4 | As Ava, attempt to submit feedback against Ben's assignment id (`POST .../feedback/{benAssignmentId}/submit`) | 403/404 — a reviewer can only submit for their OWN assignment; IDOR by assignment id is blocked. |
| 5 | As Ava, submit against her own assignment | 200 — legitimate path still works. |

## 6. Postconditions
- Only authorized users configure/release 360; reviewers submit only their own feedback; no cross-reviewer submission.

## 7. Test Category Tags
- [ ] Happy path
- [x] Negative test
- [ ] Boundary test
- [x] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test

## 8. Execution Log — 2026-09-02 (`@test-runner`, REPORT-ONLY)

**Verdict: BLOCKED — one arm executed live and PASSED, four arms persona-gapped.** No arm FAILED. This is
a CRITICAL security TC, so a partial live run is deliberately NOT recorded as a pass.

**Environment:** `/health` → **200** (`2026-09-01T23:56:09.75Z`), backend `hris-backend-1` healthy on :5000.

| Step | Layer executed | Result | Evidence |
|---|---|---|---|
| 1 (unauthorized employee configures 360 → 403) | Automated only | **PASS at that layer**; live NOT executed | `Integration.Http.Feedback360ManagerConfigApiTests.Persona_WithoutPerformanceReviewPermission_IsStoppedAtControllerGate_403` → **Passed**; `Unit.Feedback360ConfigAuthorizationTests.Caller_WithoutReviewPermission_IsDenied_Forbidden` → **Passed** |
| 2 (non-managing manager configures/releases → 403) | Automated only | **PASS at that layer**; live NOT executed | `Integration.Feedback360IntegrationTests.Release_ByNonManagingManager_ReviewTeam_Denied_NotTeamManager`, `Release_ByCaller_WithNeitherPermission_Denied`, `Integration.Http.Feedback360ManagerConfigApiTests.Manager_ConfiguringNonReport_PassesControllerGate_ButServiceDenies_NotTeamManager`, `Integration.Http.Feedback360ReleaseApiTests.Persona_WithoutPerformanceReviewPermission_PostRelease_IsStoppedAtControllerGate_403` → all **Passed** |
| 3 (unauthenticated → 401) | **LIVE API (:5000)** | **PASS** | see the five probes below — all **401** |
| 4 (IDOR: submit against another reviewer's assignment) | Automated only (unit, InMemory) | **PASS at that layer**; live NOT executed | `Unit.Feedback360FormTests.SubmitByAssignment_NonOwner_IsDenied_NotAssigned` → **Passed**; `Unit.Feedback360ServiceTests.Submit_Rejected_WhenReviewerNotAssigned` → **Passed** |
| 5 (own assignment submit → 200) | Automated only (unit, InMemory) | **PASS at that layer**; live NOT executed | `Unit.Feedback360FormTests.SubmitByAssignment_RoundTrips_HydratesAnswersByGoalId_AndLocksForm` → **Passed** |

**Step 3 — live unauthenticated probes (no `Authorization` header, `X-Tenant-Subdomain: techoneglobal`),
all executed 2026-09-02 against http://localhost:5000:**

```
POST /api/v1/tenant/performance/360/cycles/{guid}/employees/{guid}/release            -> HTTP 401 (empty body)
POST /api/v1/tenant/performance/360/cycles/{guid}/employees/{guid}/reviewers          -> HTTP 401 (empty body)
GET  /api/v1/tenant/performance/feedback-360/employees/{guid}/config                  -> HTTP 401 (empty body)
POST /api/v1/tenant/performance/feedback-360/assignments/{guid}/submit                -> HTTP 401 (empty body)
GET  /api/v1/tenant/performance/360/cycles/{guid}/employees/{guid}/results            -> HTTP 401 (empty body)
```
No information disclosure in any 401 body. Config, submit AND release are all authenticated — step 3 PASS.

**Automated suite run:** `bash scripts/run-backend-tests.sh src/backend/HRM.sln --filter
"FullyQualifiedName~Feedback360" -v n` → **74/74 Passed**, exit 0, no ISSUE-312 abort marker.

**Blocking reason for steps 1/2/4/5 (`blocked: persona-gap`):** the personas these arms need
(no-permission employee, non-managing manager, two distinct peer reviewers) do not exist on this stack —
the DB holds **3 users total** (`admin@hrm.local`/platform, `sachithra@techoneglobal.org`, `owner@e2e.test`)
and **no `acme` tenant**. New login-capable users cannot be minted: `POST /api/v1/tenant/users/invite`
stores only a BCrypt hash (`user_invitations.token_hash`) and the raw token is deliberately never logged,
so `POST /api/v1/auth/accept-invitation` cannot be driven. Cross-tenant reach is correctly refused —
`admin@hrm.local` + `X-Tenant-Subdomain: techoneglobal` → **403 `cross_tenant_denied`** (BUG-003 guard holding).

**To finish:** provision 4 personas with real credentials (HR `.All`, plain employee, non-managing manager
with `Review.Team`, 2 peer reviewers linked to employee rows) and re-run arms 1/2/4/5 at the live API.
