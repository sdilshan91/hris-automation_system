---
id: TC-PRF-005-04
user_story: US-PRF-005
module: Performance Management
priority: high
type: functional
status: blocked
created: 2026-06-16
---

# TC-PRF-005-04: Releasing 360 results below the minimum peer threshold warns/blocks HR (BR-4, FR-3)

## 1. Test Objective
Verify BR-4 / FR-3: the configurable minimum number of peer reviewers must be met before 360 results are released; otherwise HR is warned (and release is blocked / requires an explicit override per the implementation contract). Protects anonymity and statistical validity by not exposing results derived from too few peers.

## 2. Related Requirements
- User Story: US-PRF-005
- Acceptance Criteria: AC-4
- Functional Requirements: FR-3
- Business Rules: BR-4

## 3. Preconditions
- 360-enabled cycle "FY26 Annual Review" active for acme; Liam Carter under review.
- Tenant-configured minimum peer reviewers = 2 (FR-3).
- Only ONE peer (Ava Reyes) has submitted; the second peer (Ben Cho) has NOT submitted.
- HR Officer "Nadia Khan" authenticated with `Performance.Review.All`.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Min peers required | 2 | FR-3 tenant config |
| Peers submitted | 1 (Ava) | below threshold |
| Action | Release 360 results | BR-4 |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | As HR, attempt to release Liam's 360 results with only 1 of 2 required peers submitted | The system warns that the minimum peer threshold is not met (e.g. "At least 2 peer reviews are required before releasing results") and does NOT release results to the employee/manager (BR-4). |
| 2 | Bypass the UI: `POST .../performance/360/cycles/{cycleId}/employees/{liamId}/release` | Server rejects with **422** and error code `min_peer_threshold_not_met`, naming the required count; **no `feedback_360_release` row is written**; results remain unreleased; no results-available notification is sent. |
| 3 | Have Ben Cho submit so 2 peers are now complete; retry release | Release now succeeds (200); results become viewable; per the implementation contract any documented HR override path is exercised and the override is audited. |
| 4 | Confirm anonymity safety | With the threshold met, individual peer comments are shown only in aggregate/anonymized form per the anonymity setting (cross-ref TC-PRF-005-06). |

## 6. Postconditions
- Results are not released below the peer threshold; once met, release succeeds and notifies the reviewee/manager.

## 7. Test Category Tags
- [ ] Happy path
- [x] Negative test
- [x] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test

## 8. Execution Log — 2026-09-02 (`@test-runner`, REPORT-ONLY)

**Verdict: BLOCKED — partial execution.** The live-API arm (step 2/3) was NOT executed end-to-end; the
run was cut off during fixture setup. Recorded exactly as far as it got. No arm FAILED.

**Environment (re-confirmed before starting):** `GET http://localhost:5000/health` → **200**
`{"status":"healthy","timestamp":"2026-09-01T23:56:09.75Z"}`; `hris-backend-1` up 2h (healthy),
`hris-frontend-1` :4200, `hris-postgres-1` :5432. Endpoint reachability confirmed independently:
`POST /api/v1/tenant/performance/360/cycles/{guid}/employees/{guid}/release` with no bearer → **401**
(route exists and is authenticated).

| Step | Layer executed | Result | Evidence |
|---|---|---|---|
| 1 (UI warn as HR) | — | **NOT EXECUTED** | UI never driven; ran out of budget during fixture setup. |
| 2 (API: release below threshold → 422 `min_peer_threshold_not_met`, no row) | Automated (integration, InMemory) | **PASS at that layer**, live API not reached | `HRM.Tests.Integration.Feedback360IntegrationTests.Release_BelowPeerThreshold_Returns422_AndWritesNoRow` → **Passed**; `...Release_BelowPeerThreshold_SendsNoNotification` → **Passed** |
| 3 (2nd peer submits → release 200) | Automated (integration + HTTP) | **PASS at that layer**, live API not reached | `Release_AbovePeerThreshold_Succeeds`, `Release_ExactlyAtMinimumPeers_Succeeds`, `Release_Twice_IsIdempotent_OneRow`, `Release_OnSuccess_Notifies_Reviewee_And_ReportingManager` → all **Passed**; HTTP layer `Integration.Http.Feedback360ReleaseApiTests.Hr_WithReviewAll_PostRelease_ClearsFilterAndHandler_200` → **Passed** |
| 4 (anonymity in aggregate) | — | **NOT EXECUTED** | cross-ref TC-PRF-005-06. |

**Automated suite run (bound evidence):**
`bash scripts/run-backend-tests.sh src/backend/HRM.sln --filter "FullyQualifiedName~Feedback360" -v n`
→ **Total tests: 74 · Passed: 74 · Failed: 0**, wrapper exit `0`, no ISSUE-312 abort marker.
Includes the real-PostgreSQL/Testcontainers class `Feedback360ReleaseConcurrencyPostgresTests` (3/3 Passed).

**Why the live arm is unfinished (not a product defect):** this DB has **no `acme` tenant, 0 appraisal
cycles, 2 employees**, so the TC preconditions had to be built from scratch. Built via the product's own
API in the `platform` tenant: 3 employees — Liam Carter `01a05f81-3e3f-7610-a032-f4d77ced5090`, Ava Reyes
`01a05f81-3f7d-7609-884f-4e5c20b18e96`, Ben Cho `01a05f81-3fa6-7418-b12d-c4bb050228c9` — and a 360-enabled
cycle `01a05f81-ffb9-767c-9adc-96ef041e0f6f` ("FY26 Annual Review (QA TC-PRF-005)", `is360Enabled: true`,
status `Draft`, **201 Created**). Reviewer assignment + peer feedback submission + the release call were
not reached. Peer feedback additionally needs reviewer **user** accounts, which cannot be created here:
invitation tokens are BCrypt-hashed in `user_invitations.token_hash` and deliberately never logged
(`LogOnlyUserManagementNotificationService`), and the real SMTP sender is the registered one.

**Fixture residue (NOT cleaned up — report-only, no destructive ops):** in tenant `platform` —
3 employees above, 1 cycle above (+ 3 failed `QA … date-only` create attempts that 500'd and wrote nothing).

**To finish this TC in one pass:** seed a tenant with a 360 cycle, a reviewee, and **two peer reviewers that
have login-capable user accounts linked to their employee rows**, then run steps 2→3 against :5000.
