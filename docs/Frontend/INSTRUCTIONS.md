# Frontend — INSTRUCTIONS

- **Implement UI:** `@frontend-dev` (Angular 20). Lean on the vendored **`angular-developer`** skill (`.claude/skills/angular-developer/`) for signals/forms/DI/routing/testing/Tailwind reference.
- **Run/build/test:** see [`../DEV/INSTRUCTIONS.md`](../DEV/INSTRUCTIONS.md).
- **Debug the running UI:** `/debug-ui {symptom|URL}` (Playwright MCP, read-only) via `@browser-debugger`.
- **Unit tests:** Karma + Jasmine; the vendored `karma-skill` (`.claude/skills/karma-skill/`) has `TestBed`/`HttpTestingController`/`fakeAsync` patterns.
