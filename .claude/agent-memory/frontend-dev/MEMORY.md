- [Leave apply-form spec gotcha](leave-apply-form-spec-gotcha.md) — document-required block fires before any balance/LOP path in submit(); fixtures must avoid lt-2 for balance tests
- [Geolocation test mocking](geolocation-test-mocking.md) — mock navigator.geolocation via Object.defineProperty (getter-only); never mix async+fakeAsync (Promise resolves on microtask flushed by tick())
- [Signal async DOM detectChanges](signal-async-dom-detectchanges.md) — in fakeAsync, after tick() flushes an observable that sets a signal, call fixture.detectChanges() before asserting DOM textContent
- [Right-drawer form pattern](right-drawer-form-pattern.md) — Notion right slide-in drawer (full-screen mobile): fixed wrap justify-end pointer-events-none + panel pointer-events-auto, @drawer translateX anim, separate backdrop
- [Jasmine optional-arg spy](jasmine-optional-arg-spy.md) — toHaveBeenCalledWith matches exact arg count; don't assert trailing `undefined` for an omitted optional service arg
- [Spec append wrong describe](spec-append-wrong-describe.md) — attendance.service.spec has 2 top-level describes (HTTP+httpMock vs pure-fn); append HTTP tests to the FIRST, not EOF
- [Overtime weekly progress helper](overtime-weekly-progress-helper.md) — US-ATT-006 weekly bar is FE-derived (pure helper + hardcoded 20h cap, no policy endpoint); overtime detail is a dedicated list not on the clock-in card
- [Blob export download pattern](blob-export-download-pattern.md) — server-generated exports return HttpResponse<Blob>+Content-Disposition filename; how to mock in service + component specs
- [Spec default-month signal](spec-default-month-signal.md) — HR views default month()/period to CURRENT month; assert service args against component.month(), not a hardcoded yyyy-MM literal
- [Dashboard polling fakeAsync](dashboard-polling-fakeasync.md) — test interval()-based 30s polling: initial=1 call, tick(30000)=2, then destroy()+tick proves takeUntil teardown
- [Rich text editor no dep](rich-text-editor-no-dep.md) — for RTE fields build a small in-repo contenteditable CVA (execCommand) not ngx-editor/TipTap; Angular [innerHTML] sanitizes output (NFR-4)
- [Multipart upload progress testing](multipart-upload-progress-testing.md) — test reportProgress uploads via req.event(UploadProgress)+req.flush; assert FormData with body.get(); fake File size via Object.defineProperty
- [RouterLink breaks sibling spec](routerlink-breaks-sibling-spec.md) — adding routerLink to a component breaks its existing spec (NG0201 ActivatedRoute); add provideRouter([]) + run FULL ng test
- [RouterLink + Router-spy conflict](routerlink-router-spy-conflict.md) — a routerLink template + full Router spy = createUrlTree TypeError on every detectChanges; use real provideRouter + spyOn(router,'navigate')
- [CDK dropEvent test mocking](cdk-dropevent-test-mocking.md) — mock CdkDragDrop via `as unknown as`; same-column reorder needs the SAME container object for both prev/container refs
- [No chart lib — SVG/CSS or table](no-chart-lib-comparison-table.md) — FE has no chart.js/ngx-charts/swimlane; draw core charts in pure SVG/CSS (polyline/bars, helpers in models.ts, like US-ATT-010/US-PAY-009); table only for optional radar
- [FE IUser has no employeeId](fe-iuser-no-employeeid.md) — ownership/assignment gates (anti-bias etc.) can't be derived client-side; backend must return an explicit flag the FE reflects
- [Cross-feature EmploymentType import depth](cross-feature-employmenttype-import.md) — reusing Core HR EmploymentType: components/<name>/ needs ../../../core-hr, models/ needs ../../core-hr
- [Employee-profile tab extension](employee-profile-tab-extension.md) — add a tab via self-contained child cmp ([employeeId]); profile's badge/skeleton/btn-spinner are scoped (re-declare); new tab bumps sectionList.length spec assertion
- [timer(0,n) polling fakeAsync](timer-polling-fakeasync-tick0.md) — a timer(0,interval) poll stream needs tick(0) before the FIRST expectOne (doesn't fire on subscribe); takeWhile(pred,true) emits terminal snapshot then completes
- [Employee typeahead reuse](employee-typeahead-reuse.md) — reuse Core HR EmployeeService.searchActiveEmployees for an employee picker; debounce + min-2-char live in the component, not the service
- [New injected service breaks spec](new-injected-service-breaks-spec.md) — adding an inject()ed dep to a component breaks EVERY TestBed setup in its spec (incl. private setupX helpers); update all + run FULL ng test
- [Empty of() stream collapses busy flag](empty-of-stream-collapses-flag.md) — a method that sets busy=true then subscribes to a poll stream sees busy=false instantly if the stream spy returns of() (completes sync); use an open Subject to keep busy true
- [FormArray toSignal sum-gate](formarray-toSignal-sum-gate.md) — live-validate a cross-row FormArray aggregate (weights=100%) via toSignal(valueChanges)+computed; keep the rule a pure helper; disabled controls drop from .value
- [Performance dashboard scope + charts](performance-dashboard-scope-and-charts.md) — US-PRF-007: HR-vs-manager scope is server-authoritative (overview.scope), FE only does the /my-review redirect; donut=SVG arcs, trend=inline-SVG polyline (no chart.js)
- [Admin Console API root](admin-console-api-root.md) — System Admin Console endpoints are at /api/admin (NOT /api/v1); derive via apiBaseUrl.replace(/\/v1$/,'')+'/admin'; route admin/tenants gated by roleGuard(['System Admin'])
- [Karma headless hang fix](karma-headless-nosandbox-hang.md) — plain ChromeHeadless hangs (0 output, 30s disconnect) on this Windows box; use the ChromeHeadlessNoSandbox launcher (karma.conf.js + angular.json karmaConfig)
- [admin/users route collision](admin-users-route-collision.md) — new admin/users lazy feature must sit BELOW existing admin/users/lockout + admin/users/:userId/sessions leaf routes; tenant user-mgmt API is /api/v1/users + /invitations (not /system)
- [test-guard blocks "pending"](test-guard-pending-keyword.md) — test-integrity-guard rejects spec Writes containing the bare word "pending" (Jasmine marker regex); name signals pendingAction not pending
- [Fake translate loader drops interpolation](fake-translate-loader-no-interpolation.md) — provideTranslateService() spec loader returns the bare KEY and skips {{param}} interpolation; assert the bound signal/input, not interpolated DOM text
- [US-ONB-001 FE missing on branch](onb-001-fe-not-on-branch.md) — a follow-up onboarding story's branch may LACK the prior story's FE code despite STATUS=done (unmerged stacked PRs); verify features/onboarding/ exists, else restore from the impl commit + re-apply shared-file edits
- [SignalR hub client pattern](signalr-hub-client-pattern.md) — hub URL at host root (strip /api/v1), JWT via accessTokenFactory, degrade to timer(0,30s) polling; spec: spyOn HubConnectionBuilder.PROTOTYPE.build/withUrl (ESM import not spyable)
- [Debounced preview fakeAsync leak](debounced-preview-fakeasync.md) — a setTimeout-debounced HTTP call leaks through flush() and fails httpMock.verify(); tick(debounce)+match/flush before final flush; expectNone-only spec needs a real expect()
- [timer(0,n) not sync on subscribe](timer0-not-sync-on-subscribe.md) — timer(0,n) first emit is a macrotask, so an of()-mocked initial load is empty after plain detectChanges(); load() directly in ctor + interval(n) for the poll
- [esbuild host-build platform mismatch](esbuild-host-build-platform-mismatch.md) — host-Linux FE build/test fails on win32 esbuild binary (NTFS shared drive); fix with `npm install --no-save @esbuild/linux-x64@<ver>`, no full npm ci
- [Child/parent circular import](child-parent-circular-import.md) — a child cmp importing a helper from its parent cmp (which imports the child) = runtime "Cannot access X before initialization" (ng test only, build passes); inline the helper
- [Sentry ESM inject seam](sentry-esm-inject-seam.md) — @sentry/angular exports are frozen ESM; spyOn(Sentry,'init') throws; expose an injectable SentryApi seam (default realSentry) to unit-test init/setTag wiring
- [Payroll defaulting rule](feedback-payroll-defaulting.md) — on money paths a mapper default must be the least-claiming value; never assert money moved, consent given, or work finished
- [Payroll statutory contract traps](project-payroll-statutory-contract-traps.md) — the list DTO carries no taxSlabs/socialSecurity; hydrating the editor from it opens empty and saving destroys the bands
