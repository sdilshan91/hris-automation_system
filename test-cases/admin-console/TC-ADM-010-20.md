---
id: TC-ADM-010-20
user_story: US-ADM-010
module: Admin Console
priority: medium
type: integration
status: blocked
created: 2026-06-17
---

# TC-ADM-010-20: [DEFERRED] Uploaded-documents ZIP subtree organized by entity

## 1. Test Objective
Cover FR-4: uploaded documents are collected from the tenant-scoped storage path (`{tenantId}/documents/`) and included in a `documents/` subdirectory within the export ZIP, organized by entity type (e.g. `documents/employees/{employee_id}/`, `documents/payslips/{payslip_id}/`).

DEFERRED — status: blocked. No blob/document storage is wired today (same blob-storage gap flagged in US-ADM-004 file-deletion TC-ADM-004-19). The export bundle today contains per-entity CSVs + audit_log.jsonl + manifest (run-green, TC-ADM-010-01); the documents subtree is deferred until document storage exists.

## 2. Related Requirements
- User Story: US-ADM-010
- Acceptance Criteria: AC-2 (documents ZIP)
- Functional Requirements: FR-4 (documents/ subtree organized by entity)

## 3. Preconditions
- (Deferred prerequisite) Tenant-scoped document/blob storage at `{tenantId}/documents/` with seeded files.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| documents path | {tenantId}/documents/ | not wired today |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Confirm today's behavior | No document storage; the bundle has no `documents/` subtree; CSV/audit/manifest are run-green (TC-ADM-010-01). |
| 2 | (When storage wired) Full export | Bundle contains `documents/` organized by entity (`documents/employees/{id}/`, `documents/payslips/{id}/`). |
| 3 | Until implemented | Expected behavior: "Not available — uploaded-document export requires blob storage (same gap as TC-ADM-004-19); deferred." |

## 6. Postconditions
- Deferred; no documents subtree asserted today.

## 7. Test Category Tags
- [x] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
