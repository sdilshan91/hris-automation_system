---
id: TC-ADM-010-17
user_story: US-ADM-010
module: Admin Console
priority: medium
type: functional
status: blocked
exec_note: "S1: needs-CODE (feature-not-built/RLS) — dev backlog."
created: 2026-06-17
---

# TC-ADM-010-17: [DEFERRED] Schema-documentation PDF + PII fields "clearly marked"

## 1. Test Objective
Cover AC-2 / FR-2 (§10): the bundle includes a schema documentation file (PDF describing column definitions) in which PII fields are clearly marked.

DEFERRED — status: blocked. Per §10 the schema PDF is a STATIC document generated at build time describing the standard entity schema, not produced dynamically per export. Today it is a static stub (or absent); the dynamic/marked-PII PDF is deferred. The CSVs + audit_log.jsonl + manifest + checksums are run-green; PII INCLUSION in CSVs is covered run-green in TC-ADM-010-05 (only the "clearly marked in the schema doc" portion is deferred here).

## 2. Related Requirements
- User Story: US-ADM-010
- Acceptance Criteria: AC-2 (schema documentation file)
- Functional Requirements: FR-2 (schema doc), FR-8 (PII "clearly marked in the schema documentation")
- §10 (static build-time PDF)

## 3. Preconditions
- (Deferred prerequisite) A static schema-documentation PDF generated at build time, with PII fields annotated.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| schema_doc | schema.pdf (static) | §10 build-time artifact |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Confirm today's behavior | The schema PDF is a static stub (or not yet bundled); per-export dynamic generation is not implemented. |
| 2 | (When implemented) Open the bundle's schema PDF | Describes each entity's column definitions; PII fields (national ID, bank account) are clearly marked. |
| 3 | Until implemented | Expected behavior: "Not available — the schema-documentation PDF is a static build-time stub per §10; dynamic generation + PII annotation are deferred. PII inclusion in CSVs is run-green in TC-ADM-010-05." |

## 6. Postconditions
- Deferred; no dynamic schema PDF asserted today.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [x] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
