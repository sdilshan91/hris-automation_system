---
id: TC-ADM-002-17
user_story: US-ADM-002
module: Admin Console
priority: medium
type: functional
status: blocked
created: 2026-06-16
---

# TC-ADM-002-17: [DEFERRED] SLA uptime % per tenant against plan SLA tier

> **status: Deferred / Blocked — requires observability pipeline (health-probe history / uptime aggregation).** SLA uptime % requires a stored history of health-check probes per tenant, which is not yet collected. Expected behavior today is a `Not available — requires observability pipeline` placeholder, never a fabricated uptime figure (e.g. a hardcoded 100%). Traces FR-7; NOT run-green today.

## 1. Test Objective
Verify FR-7: the dashboard shows uptime percentage per tenant, calculated from health-check probes, compared against the tenant's plan SLA tier. Because probe history is not retained yet, the immediate verifiable behavior is the placeholder/disabled state.

## 2. Related Requirements
- User Story: US-ADM-002
- Functional Requirements: FR-7
- Blocking dependency: retained health-probe history + SLA tier on plan

## 3. Preconditions
- System Admin authenticated.
- No health-probe time-series retained (current state).

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Uptime source | probe history (absent) | deferred |
| SLA tier | from plan | comparison target |
| Expected today | "Not available" placeholder | no fabricated uptime |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Locate the SLA uptime indicator (dashboard or tenant detail) | Shows "Not available — requires observability pipeline" — not a fabricated 100% / static value. |
| 2 | Inspect the backing API payload | Uptime field is null/flagged-unavailable. |
| 3 | (Future, when pipeline lands) Compare computed uptime to probe history | Uptime % matches the probe-derived value and is correctly compared to the plan SLA tier (e.g. flagged when below tier). |

## 6. Postconditions
- No fabricated SLA uptime is shown. Verification blocked on probe-history retention.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
