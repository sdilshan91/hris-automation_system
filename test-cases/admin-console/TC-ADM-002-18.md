---
id: TC-ADM-002-18
user_story: US-ADM-002
module: Admin Console
priority: medium
type: functional
status: blocked
created: 2026-06-16
---

# TC-ADM-002-18: [DEFERRED] Storage / API-call / email-send usage gauges vs plan limit

> **status: Deferred / Blocked — requires observability pipeline (Redis usage counters `t:{tenantId}:usage:*` or materialized views).** Storage-used, API-calls-this-month, and email-sends-this-month usage counters are not incremented by application operations yet, so these three gauges have no data source. Expected behavior today is a `Not available — requires usage counters` placeholder per gauge, never a fabricated value. Traces AC-2 / FR-2; NOT run-green today. The EMPLOYEE gauge IS implemented and is covered by TC-ADM-002-02/03/04.

## 1. Test Objective
Verify the non-employee usage dimensions of AC-2 / FR-2: per-tenant gauges for storage used vs limit (GB), API calls this month vs limit, and email sends this month vs limit, with 80% warning / 100% breach highlighting. Because the underlying usage counters are not yet incremented, the immediate verifiable behavior is the placeholder/disabled state for these three dimensions.

## 2. Related Requirements
- User Story: US-ADM-002
- Acceptance Criteria: AC-2
- Functional Requirements: FR-2
- Blocking dependency: Redis usage counters (`t:{tenantId}:usage:storage_bytes`, `:api_calls`, `:email_sends`) or materialized views, incremented by app operations

## 3. Preconditions
- System Admin authenticated; tenant usage table open.
- Storage/API/email usage counters NOT incremented (current state).

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Storage gauge | absent | deferred |
| API-calls gauge | absent | deferred |
| Email-sends gauge | absent | deferred |
| Expected today | "Not available" placeholder | no fabricated usage |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | In the tenant usage row, locate the storage / API-call / email-send gauges | Each shows "Not available — requires usage counters" — not a fabricated 0% or random fill. |
| 2 | Inspect the usage API payload | storage/api/email usage fields are null/absent/flagged-unavailable; only the employee dimension carries real numbers. |
| 3 | (Future, when counters land) Drive usage to 80% / 100% of each limit | Each gauge shows the correct ratio with amber warning at 80-94% and red breach at 95-100%, consistent with the employee gauge behavior. |

## 6. Postconditions
- No fabricated storage/API/email usage. Only the employee gauge is real today; the rest are placeholdered.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
