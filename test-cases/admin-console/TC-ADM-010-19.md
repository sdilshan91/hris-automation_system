---
id: TC-ADM-010-19
user_story: US-ADM-010
module: Admin Console
priority: medium
type: performance
status: blocked
created: 2026-06-17
exec_note: "P3b k6 2026-06-30: NOT MEASURED AT REQUIRED SCALE — kept blocked. TC requires full tenant export <=30min at 50,000 employees / 10GB documents (read-replica + streaming serialization); the P3b single report-export passed at 80ms but only the small per-report path, NOT the whole-tenant 50k/10GB export. Perf harness exists (perf/), re-run after seeding a 50k/10GB tenant."

# TC-ADM-010-19: [DEFERRED] Large-tenant performance — 50k records / 10GB in 30 min + streaming serialization

## 1. Test Objective
Cover NFR-1 / NFR-2: the export completes within 30 minutes for a tenant with up to 50,000 employee records and 10 GB of documents, without significant production-DB impact, using read replicas and streaming serialization to avoid loading all data into memory.

DEFERRED — status: blocked. Today generation uses `AsNoTracking()` (the NFR-2 no-tracking property IS satisfied and is implicit in the run-green full-export path TC-ADM-010-01), but read-replica routing and CHUNKED/streaming serialization for very large tenants are deferred, and the 30-minute / 50k-record performance target requires a performance-representative environment. Document inclusion (FR-4 documents/ subtree) is also part of the 10GB target and is not wired today.

## 2. Related Requirements
- User Story: US-ADM-010
- Non-Functional Requirements: NFR-1 (<= 30 min @ 50k employees / 10 GB), NFR-2 (AsNoTracking + read replica + streaming)
- Functional Requirements: FR-4 (documents subtree — part of the 10GB target)

## 3. Preconditions
- (Deferred prerequisite) A perf-representative dataset (~50k employees, ~10 GB documents) + a read replica + streaming serialization path.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| employees | 50,000 | NFR-1 target |
| documents | 10 GB | NFR-1 target |
| time budget | 30 min | NFR-1 |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Confirm today's behavior | Generation uses AsNoTracking (no-tracking property satisfied); read-replica + chunked streaming + document inclusion are not wired; correctness is covered by TC-ADM-010-01. |
| 2 | (When perf env ready) Run a full export at 50k employees / 10 GB | Completes within 30 minutes. |
| 3 | (When perf env ready) Monitor production DB during export | No significant impact (streaming, no full in-memory load; read replica if available). |
| 4 | Until implemented | Expected behavior: "Not available — 30-min/50k-record NFR + read-replica/streaming require a perf-representative environment; AsNoTracking is in use today and correctness is covered by TC-ADM-010-01." |

## 6. Postconditions
- Deferred; perf NFR not asserted today.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [x] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
