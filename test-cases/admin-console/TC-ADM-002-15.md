---
id: TC-ADM-002-15
user_story: US-ADM-002
module: Admin Console
priority: high
type: functional
status: blocked
created: 2026-06-16
---

# TC-ADM-002-15: [DEFERRED] Error-rate "Attention Required" queue

> **status: Deferred / Blocked — requires observability pipeline (OpenTelemetry metrics).** Per-tenant error-rate data does not exist yet, so the error-rate "Attention Required" queue cannot be populated. Expected behavior today is a `Not available — requires observability pipeline` placeholder (or an empty/disabled panel labeled as such), never fabricated entries. Traces AC-3 / FR-1 for honest coverage; NOT run-green today. (Note: the employee QUOTA-breach queue is the real, implemented queue — see TC-ADM-002-04; this is the separate ERROR-RATE queue.)

## 1. Test Objective
Verify AC-3: a tenant whose error rate exceeds 5% over 10 minutes appears in the "Attention Required" queue with its elevated error rate, last error sample, and a link to its detail/debug view. Because per-tenant error metrics are not yet collected, the immediate verifiable behavior is the placeholder/disabled state.

## 2. Related Requirements
- User Story: US-ADM-002
- Acceptance Criteria: AC-3
- Functional Requirements: FR-1
- Blocking dependency: OpenTelemetry per-tenant error metrics

## 3. Preconditions
- System Admin authenticated.
- Error-rate metrics NOT collected (current state).

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| Threshold | > 5% error over 10 min | AC-3 trigger |
| Entry fields | error rate, last error sample, detail link | when data exists |
| Expected today | "Not available" placeholder | no fabricated entries |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Open the "Attention Required" (error-rate) queue | Shows "Not available — requires observability pipeline" / disabled panel — no fabricated tenant entries. |
| 2 | Inspect the API payload backing that panel | No synthesized error-rate entries; field is null/flagged-unavailable. |
| 3 | (Future, when pipeline lands) Drive a tenant to >5% error over 10 min | That tenant appears in the queue with elevated error rate, last error sample, and a working link to its detail/debug view. |

## 6. Postconditions
- No fabricated attention entries. Verification blocked on observability pipeline.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [ ] Security test
- [ ] Multi-tenant isolation
- [x] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
