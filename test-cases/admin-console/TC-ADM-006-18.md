---
id: TC-ADM-006-18
user_story: US-ADM-006
module: Admin Console
priority: medium
type: integration
status: blocked
created: 2026-06-17
---

# TC-ADM-006-18: [DEFERRED] Real blob/object-storage persistence of branding files (S3/Azure)

## 1. Test Objective
Verify FR-2's "platform-configured storage provider (local/S3/Azure Blob)" persistence and tenant-scoped signed URLs / CDN paths against a REAL object store. **DEFERRED** — no S3/Azure blob provider is wired today; branding files persist to a LOCAL/stub store. The server-side VALIDATION (magic bytes + size, TC-ADM-006-04/-05) and the tenant-scoped PATH PREFIX `{tenantId}/branding/` (TC-ADM-006-03/-06, TC-ADM-ISO-015) are real and run-green now; only the cloud-backed persistence + signed-URL generation are deferred.

## 2. Related Requirements
- User Story: US-ADM-006
- Acceptance Criteria: AC-2
- Functional Requirements: FR-2 (signed URLs / CDN paths from configured provider)
- Assumptions/Constraints: §10 (configured storage provider local/S3/Azure)

## 3. Preconditions
- A configured S3/Azure Blob provider with tenant-prefixed buckets/containers — NOT present in the current platform.

## 4. Test Data
| Field | Value | Notes |
|-------|-------|-------|
| storage provider | S3 / Azure Blob | deferred |
| signed URL | time-limited, tenant-scoped | deferred |
| in force today | local/stub store + `{tenantId}/branding/` prefix | TC-ADM-006-03/-06 |

## 5. Test Steps
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Upload a logo; confirm it lands in the tenant's bucket/container prefix in the cloud store | DEFERRED — once a blob provider is wired: object exists under the tenant prefix; a time-limited signed URL is returned. |
| 2 | Verify the returned URL is tenant-scoped and expires | DEFERRED — signed URL is scoped + short-lived. |
| 3 | Until a blob provider is wired | Rely on TC-ADM-006-03/-06 (validation + local persistence + tenant path) and TC-ADM-ISO-015 (path isolation). |

## 6. Postconditions
- No mutation. Stays `blocked` pending object-storage integration; validation + path-prefix isolation are covered run-green elsewhere.

## 7. Test Category Tags
- [ ] Happy path
- [ ] Negative test
- [ ] Boundary test
- [x] Security test
- [x] Multi-tenant isolation
- [ ] Performance test
- [ ] Accessibility test
- [ ] Cross-browser test
