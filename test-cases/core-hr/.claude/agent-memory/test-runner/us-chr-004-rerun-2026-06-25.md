---
name: us-chr-004-rerun-2026-06-25
description: US-CHR-004 departments debugger-free re-run — BUG-012 retracted, 2 NEW manager-FK bugs (BUG-014 cross-tenant, BUG-015 500), final 28P/3F/7B
metadata:
  type: project
---

US-CHR-004 (departments) debugger-free RE-RUN 2026-06-25 cleared all 26 false `server-down (BUG-012)` blocks.

**Final: 28 PASS / 3 FAIL / 7 BLOCKED of 38 TCs. 0 server-down remaining.**

**NEW findings (the manager FK is the un-validated soft spot):**
- **BUG-014 HIGH** — `managerId` on dept create/update is NOT tenant-validated. A techoneglobal employee id set as an acme dept's manager → HTTP 200 (only the GLOBAL Postgres FK `fk_departments_employees_manager_id` constrains it, no tenant predicate). Contrast: `parentDepartmentId` IS tenant-validated (bogus/cross-tenant → clean 400). Distinct from BUG-003 (this fires with the CORRECT subdomain).
- **BUG-015 MED** — non-existent `managerId` → HTTP 500 (`Npgsql 23503` FK violation, RequestId `0HNMIFE5GI134`, surfaced by `ExceptionHandlingMiddleware.cs:27`) instead of 400. Same root as BUG-014 (no pre-validation). NOT a debugger artifact — real 500 in 58ms.
- **ISSUE-020 LOW** — deactivate audited as `Department.Update` + delta summary `{"IsActive":false}`, not a distinct `Department.Deactivate` action with before/after snapshots. NFR-5 core (actor/tenant/ts/resourceId) IS met → TC-31 still PASS.

**Standing (not retracted):** BUG-003 (CRIT subdomain-header cross-tenant, departments surface, TC-ISO-001), BUG-013 (MED case-insensitive dup, TC-22).

**Method notes that worked:** for any manager/parent FK assign, ALWAYS run 3 arms — valid same-tenant / cross-tenant id / bogus id. parent path was clean; manager path leaked + 500'd. k6 IS wired (v2.0.0) — dept endpoints P95 all <55ms on ~40-dept dataset (TC-26 PASS); full-scale 100/500-dept (TC-28 NFR-4) still dataset-blocked. RLS(ISO-003)/Redis(ISO-004)/FE-platform-bound(27/29/30/34) are the legit blocks.

**Cleanup:** all throwaway acme depts (Cyc/HQ/Lvl/MgrDept/Stamp/Audit/Perf/XSS/SQLI/etc.) deactivated; acme active baseline = engineering, Engineering, Sales. ZERO writes to techoneglobal (read-only id-discovery only; cross-tenant manager FK was written onto an ACME row then reset null). See [[qa-personas-reseed-2026-06-25]].
