# Memory Index

- [Mutation campaign safety](mutation-campaign-safety.md) — restore mutants per-batch, grep the INDEX+HEAD not just the tree; a concurrent session committed 8 of mine into `0cc05dea`.
